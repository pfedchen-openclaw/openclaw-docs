# SESSION 6c.11 — Trust foundation: reliability core built + verified; OAuth/offset handed off — CLOSURE
_2026-06-27. 6c.11 executed the trust-foundation programme (WS-0/1/2/8/9). **Session constraint: SSH ∴ no gateway restart / LaunchAgent bootstrap (D20); heartbeats stay PAUSED ([P6-77]) — Peter confirmed the Anthropic workspace cap is still in place until ~1 Jul.** So the work split cleanly: reversible workspace/script edits done + verified this session; restart-gated and Peter-action items handed off with precise briefs. New codes **P6-78/79/80**. **`openclaw.json` UNCHANGED** (`d14bb41a3cc4`/9963B — no config mutation; all work was workspace files + the watchdog script). v7/Andreas remain gated behind the acceptance gate (6c.14) — untouched._

## What shipped (reversible, live, verified)
- **WS-1 durable task store** — `TASKS.md` for both agents (Marie `41274dfbac07`, Charlotte `8db2465b49e4`), a ledger with a mandatory **proof-of-done** field that operationalises done-by-verification ([F26]-for-tasks). Caught + fixed a real gap: Charlotte's HEARTBEAT had **no** Tasks-check or write-on-accept (6c.10's edits were pa-only) → her ledger would never have been read; wired it (`workspace-pro/HEARTBEAT.md` `0bdef99de68a`). ([P6-78])
- **WS-2 doctrine verify + memory sweep** — confirmed the judgement doctrine is LIVE in both agents (SOUL/AGENTS §Judgement + autonomy-overlay + natural-cadence/MEMORY protocol + competence standards + corrected booking-flow/Failures-never-silent). Swept memory: reconciled the stale "every 15 min" cadence → the live 30m scheduler with rationale (`workspace-pa` `AGENTS.md` `780f2434253d`, `HEARTBEAT.md` `3dddeb41b879`); deliberately left the "no reliable calendar access" line (stale but protective while OAuth is dead). ([P6-78])
- **WS-8/9 watchdog redesign** — `scripts/heartbeat-watchdog.sh` `22873b50073e`→**`7d0545f4d307`**: (A) episode-throttled fallback alerts (storm fix), (B) **real-blocker escalation** (OAuth `invalid_grant`) as a new outbound via Marie's working channel — fixes the spam-on-fallback/silent-on-Charlotte's-outage inversion, (C) a **context-bloat/cost canary** off the session store. Verified by standalone dry-run: the canary fired correctly on Marie's real 244K-token/3-compaction bloat, ignored Charlotte at 94K — and the dry-run **caught a bug pre-ship** (keyed on `contextTokens` = window capacity → would false-fire; corrected to `inputTokens`). launchd runs the edited file (path verified); dormant under the pause, active on resume. ([P6-79])

## What was handed off (Peter-action / restart-gated — carried with briefs)
- **OAuth flip (WS-0, keystone)** — recon done; **key finding: "Internal" can't serve the consumer pfedchen@gmail.com** Marie reads today (Internal authorises only Workspace-org users). Durable fix today = owned GCP project + External "In production"; graduate to Internal when marie@fedchenkov.com lands (6c.12). Full step-by-step in **`6c11-OAUTH-FLIP-BRIEF.md`** — needs Peter's fork pick + GCP action + the GUI consent step (D20). ([P6-80])
- **Charlotte's frozen Telegram offset (WS-0)** — diagnosed (frozen 17 Jun; 17–24 Jun msgs unrecoverable to Telegram's ~24h retention); definitive fix = gateway restart to re-init her poller = **D20/GUI**, not doable over SSH. She's dark until the cap resets regardless. Carried to the resume GUI session (+ a real inbound from Peter to verify, [F26]). ([P6-80])
- **Per-turn tool/token ceiling (WS-9)** — runtime work, no config knob in 2026.4.22 → 6c.12; the canary is the deployed detective stand-in. ([P6-79])
- **Full Telegram back-scroll (WS-1)** — Bot-API-limited + transcript not in-sandbox → runtime work, 6c.12. ([P6-78])

## End-state (anchors)
- **`openclaw.json`: `d14bb41a3cc4` / 9963 B — UNCHANGED** (no config mutation; A-7 stays clean — live == last-good).
- **Pin 2026.4.22; both containers `capable-2026-06`; gateway gui/502 pid 31827; GPT-5 fallback in place; heartbeats PAUSED (sentinel intact, watchdog enforcing).**
- **Changed files (all SHA-anchored above):** `workspace-pa/{TASKS.md,AGENTS.md,HEARTBEAT.md}`, `workspace-pro/{TASKS.md,HEARTBEAT.md}`, `scripts/heartbeat-watchdog.sh`; new docs `6c11-OAUTH-FLIP-BRIEF.md`, this closure, `SESSION-6c.12-OPENER.md`. **Deviations:** P6-78/79/80 appended (additive-in-place).

## Honest note
The session's centre of gravity — the reliability core (durable tasks + done-by-verification) and the alerting/cost redesign — is **built and inspection/dry-run-verified**, and it caught two real defects in passing (Charlotte's unread ledger wiring; the canary's window-vs-usage bug). But the F26 bar is "a real delivered artefact through the runtime," and **that cannot be met this session**: heartbeats are paused, the cap forces GPT-5, and it's an SSH session (no restart). So everything here is verified by inspection/dry-run and is **armed for resume**, not exercised live. The first real test is the heartbeat-resume GUI session after the ~1 Jul cap reset — which also closes Charlotte's offset and OAuth. No live agent turns were spent (cost discipline held; [P6-51]).

---
_Attachments for 6c.12: this closure + `SESSION-6c.12-OPENER.md` + `6c11-OAUTH-FLIP-BRIEF.md` + `JUDGEMENT-DOCTRINE.md` + `PHASE-6-DEVIATIONS-LOG.md` (P6-78/79/80) + `PRE-COS-BACKLOG.md`._
