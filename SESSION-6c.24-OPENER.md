# SESSION 6c.24 — Memory v2: the aggressive frozen-core slice + its billed retrieval-usage verify turn, media-caps before/after, dreaming-on

_Opener · generated at 6c.23 close (2026-07-05). 6c.23 **measured the applied levers** (the config levers + the daily freshstart clear killed the catastrophic tail — p95 3.76M→289K, max 16.4M→289K, 51→0 compactions — but confounded; **byte-cap truncation 20%, median ≈3.8× target, and organic recall 0 persist** → the reliability gate stays OPEN), **corrected item-4** (the freshstart LaunchAgent is already LIVE — it cleared pa:main 2.73M on 07-04; the real gap is **intra-day** :main bloat: 0→1.43M from one browser turn), **shipped the conservative restructure** (core 50K→48.7K, −1.3K, zero risk; new indexed `memory/reference-tools.md`), and **applied the media-caps pass** (`browser.snapshotDefaults.mode=efficient` + image/pdf caps; openclaw.json `d702ae330bb7`→`eb08cfa7d7b6`/10654B; gateway restarted; `mode:efficient` free-verified). **Read [P6-120] first**, then this opener. GUI/Screen-Sharing (D20) for any restart/browser/2FA._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`eb08cfa7d7b6`/10654B**, 600/staff. Pin 2026.4.22 (00bd2cf). Sandboxes `capable-2026-06`. (openclaw.json is **categorically Bash-deny-listed** to Claude Code — config edits go via `scripts/apply-*.sh` under explicit per-message permission, or Peter runs them. NEVER weaken the F21 backstop unilaterally.)
- **Deviations currency:** **P6-1…120 gap-free**, no dangling refs (run the [P6-101] carry-chain check at close too). Pre-existing 21/22 dupes are historical — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `managername=Aqua`, `gui/502` reachable) before assuming.
- **★ Media caps still live?** `config get browser.snapshotDefaults.mode`→`efficient`; `agents.defaults.imageMaxDimensionPx`=1024, `pdfMaxPages`=15, `pdfMaxBytesMb`=8. **toolResultMaxChars stays unset** (16K dist default — do NOT lower). 6c.22 levers still live (compaction.model=`anthropic/claude-sonnet-4-6`, keepRecentTokens=40000, contextInjection=continuation-skip, mmr/temporalDecay under `…query.hybrid.…`=true).
- **★ Index + canary:** `memory status` → pa **35/35·125** (includes `reference-tools.md`), pro 31/31·36, `Dirty: no`. `launchctl print gui/502/ai.openclaw.memory-health` loaded; `state/memory-health/daily.log` accreting. **Freshstart is LIVE** (`launchctl print-disabled` shows it NOT disabled; 06:55 daily) — don't "re-enable" it.

## Stage 1 — the LEAD: the aggressive slice + prove retrieval is used (the still-open gate signal)
Recall is **0** — organic `memory_search` is not being triggered because the injected core still holds everything. The conservative slice (6c.23) was zero-risk but only −1.3K; **the real reduction is the aggressive slice, and it MUST be verified with one billed turn** (recall=0 means moving hot content to retrieval-only is a real regression risk until proven).
1. **Aggressive frozen-core slice (workspace edits, live bind, no restart) — the highest-value structural step.** Move the TOOLS.md verb-tables + the verbose AGENTS.md procedural blocks to retrieval-only indexed files (extend `memory/reference-tools.md` + a `reference-agents.md`), leaving the injected core = IDENTITY+SOUL+lean-AGENTS(hot rules only)+MEMORY-as-index+lean-TOOLS(permissions+creds+cheat-sheet)+HEARTBEAT, target **≤ ~30K**. Backup-first, re-index, verify retrievability (free). **This makes recall REQUIRE search.**
2. **The billed verify turn (Peter cost-OK required, [P6-51]).** ONE fresh-session Marie turn on the **real gateway path** (Opus — per [embedded-CLI-mistests-model]; a cheaper model is a false-negative risk), **browser-bounded**, on a recall question whose answer is NOT in recent context: does she call `memory_search`? does she still follow the hot rules (receipt/gate/cadence)? Fold in the **Porsche re-deliver** so one turn clears three signals. Verify post-hoc from the trajectory (memory_search fired) + delivery receipt. **Surface the estimate + get Peter's explicit go before spending** — this is the one authorised-spend of the session.
3. **Media-caps before/after (free, inspection).** Read `state/memory-health/daily.log` across the days since 6c.23: did median/p95/trunc fall once real browser/PDF turns exercised `mode:efficient`? Recalibrate §8 off the real "after". (The 6c.23 restart happened on a quiet day — the caps' effect needs browser turns to show.)
4. **Intra-day :main-bloat durable fix.** The 06:55 freshstart bounds overnight only; pa:main hit 1.43M from ONE browser turn mid-afternoon. Add a **threshold-triggered** reset (watchdog already fires at 250K → wire it to an archive-first `session-freshstart --force --agent <id>` on breach, or a structured checkpoint) so a single heavy turn can't balloon `:main`. Reversible; measure via canary/watchdog.
5. **Dreaming ON (cron) — still blocked pending first `DREAMS.md`.** `Dreaming: off`, diary absent, 0 corpus files. Enable `plugins.entries.memory-core.config.dreaming` (cheap model) + review the first output before trusting promotion. Then the §8 acceptance-gate week-clock can start.

_All Andreas-owned (`PRE-COS-BACKLOG` §A.4). The aggressive slice + its verify turn is the exemplar of his "refactor faulty foundations" mandate — advance as far as Peter's presence + the cost-OK allow._

## Stage 2 — carried (secondary — do not let these crowd out Stage 1)
- **Doctor hygiene** — 23 orphan transcript files in `agents/pa/sessions` (freshstart-archive leftovers) + 1 agent dir without an `agents.list` entry; clean on Peter's say-so (`rm` deny-listed — propose, don't route around).
- **Payments real-charge F26** (plain merchant, Marie gateway turn, Peter 2FA) — acceptance-gate blocker, [P6-99]/[P6-117]. Hard gateways stay drive-to-page + surface.
- **Reply-watcher** — host poller on the agents' live Gmail cred (skip gog, [P6-109]) → `openclaw agent … --deliver`.
- **generate-mode credential store** — round out [P6-115] (only `existing` proven live).
- **Battery re-run** (judgement class T3/T6/digest) via the real Telegram path; cheapest model, ~$5 cap ([P6-51]).

## Closed at 6c.23 (do not re-carry)
Canary before/after DONE (levers helpful, confounded; residue mapped); item-4 CORRECTED (freshstart already live); conservative restructure SHIPPED (−1.3K, indexed reference, zero risk); media-caps pass APPLIED (`mode:efficient` + image/pdf caps, restarted, free-verified) — [P6-120].

## Safety rails
Reversible build runs without prompting (aggressive restructure with backup, canary reads, threshold-reset wiring). **Surface irreversible/destructive:** openclaw.json changes + the gateway restart (Peter-run script or explicit per-message permission — F21 backstop categorical, never route around), version-pin/D1, real charges, 1Password writes/deletes, depairing, reboots, **and the billed verify turn (get Peter's cost-OK first)**. **Cost ([P6-51]):** inspection/host-drive first; the ONE billed turn is Opus/real-path/browser-bounded/fresh-session — estimate + authorise before spending; no loops/Opus-batches. **Secrets ([F21/F51]):** op host-only; display-immune checks; never read openclaw.json/agent-creds/** into context. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN — retrieval live + levers applied + conservative restructure + media caps + canary measuring; the week-clock starts once (a) the aggressive slice proves organic recall > 0, (b) the caps' before/after shows the residue falling, and (c) dreaming is on.
- **Pre-Andreas:** memory architecture is de-risked (retrieval live, dedup done, levers+caps applied, observability wired, conservative restructure shipped); the remainder is the aggressive slice + its verify turn + the intra-day-reset + dreaming + the week-clock (PRE-COS-BACKLOG §A.4). Then v7 capstone reconciliation ([P6-76]) → 6d Andreas.
- **Andreas mandate:** first-principles review/refactor of faulty foundations (Peter, extends P6-49) — the memory saga is the exemplar; he inherits the full v2 plan + working retrieval + a measuring canary + a de-risked conservative baseline + the aggressive slice teed up.
