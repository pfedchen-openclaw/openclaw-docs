# SESSION 6c.28 — Bootstrap chat-bloat auto-heal (Peter/GUI) + re-test T3 & Wikimedia on the fixed infra + observe; aggressive slice + dreaming + brief-budget + v7 capstone remain ANDREAS/milestone

_Opener · generated at 6c.27 close (2026-07-08). 6c.27 shipped: **reply-watcher flipped to NOTIFY + verified** (tightened with agent-self-send exclusion; 1 clean send, dedup works, £0); **generate-mode cred store LIVE-PROVEN + CLOSED** ([P6-115]; drain→op-generate→display-immune receipt; throwaway deleted+verified); and a long **Peter-directed Marie live-incident remediation** — **browser recovered** (was dead→gateway restart; proven on the real Wikimedia £10 form), **payment root fixed** (she used aria+CSS on the `browser` tool → guidance corrected to efficient-snapshot + BY-REF, tied to `pay-fill.sh`), **session-bloat sticks band-aided** (archive-first resets; re-bloats → structural), **`sessions_send` config cleaned** (F17 apply script), **exec reflex re-tightened**, **Playwright backend deferred** (Peter: leave as-is), and the **chat-bloat auto-heal IMPLEMENTED + STAGED**. **Read [P6-129] first**, then this. openclaw.json CHANGED once → **`3267edd8b6aa`/10489B, last-good synced (A-7 clean)**. GUI/Screen-Sharing (D20) for bootstrap/restart/browser/2FA. **Cadence: ride the window ([[ride-context-dont-close-early]]).**

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`3267edd8b6aa`/10489B** (changed 6c.27: sessions_send removed from 5 agents' tools.alsoAllow — [P6-129]). 600/staff. Pin 2026.4.22 (00bd2cf). Levers unchanged: `snapshotDefaults.mode=efficient`, `contextTokens=120000`. (openclaw.json Bash-deny-listed — A-7 via applied-lever `config get`, structural jq/python for edits only, never a raw dump; NEVER weaken the F21 backstop.)
- **Deviations currency:** **P6-1…129 gap-free**, no dangling refs (run [P6-101] at close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA/bootstrap.
- **★ LaunchAgents:** reply-watcher now **notify** (`~/Library/LaunchAgents/ai.openclaw.reply-watcher.plist` = **`9e6b476f2de7`/2218B**; `launchctl print gui/502/ai.openclaw.reply-watcher` → LOADED, exit0, `REPLYWATCH_MODE=>notify`, query has `-from:marie@fedchenkov.com`, 900s). Reset-net scripts: `heartbeat-watchdog.sh` **`183896a33dd2`**, `session-freshstart.sh` **`55020ef7acd6`** — DRYRUN self-check healthy. **Do NOT run reply-watcher.sh / chat-bloat-reset.sh with side-effects from this session** (reply-watcher reads agent-creds F21/F51; both are launchd-only by design — dry-run/inspect only).
- **★ chat-bloat-reset STAGED, not bootstrapped:** `scripts/chat-bloat-reset.sh` **`93bb06e42841`/4939B**, staged plist **`497ccbd67145`/1623B**. Inert until Peter bootstraps (Stage 1.0).
- **★ Marie briefs (live, outside docs git — verify by direct sha):** `workspace-pa/TOOLS.md` **`f519f15fcfe9`/15875B** (15698 chars, **63 under** the 15765 clip ceiling — browser-refs rule + sharpened exec note); `reference-tools.md` **`c3594d05c767`/4338B** (efficient-refs + pay-fill handoff); `AGENTS.md` `db8c512bd962`, `HEARTBEAT.md` `6b9d50031098` (P6-128, unchanged).
- **★ Marie session health:** expect `:main` lean (was 63K at close) + a fresh/low `telegram:direct`. If `telegram:direct` totalTokens is climbing toward 130K, that is the [P6-129] stick pathology — chat-bloat-reset (once bootstrapped) heals it. Index/media/freshstart unchanged.

## ⛔ FLAGGED FOR ANDREAS / milestone — do NOT attempt in interim sessions
- **Aggressive frozen-core slice** (verb-tables + procedural blocks → retrieval-only, ≤30K) + its billed verify turn. **This is the REAL fix for the bloat/stick** (the ~25K/turn brief injection drives the 150K sessions); chat-bloat-reset is only a self-heal backstop. `PRE-COS-BACKLOG §A.4 item 3`.
- **Dreaming ON**; **`:telegram:direct` compaction bounding** (60s hardcap has no config knob — [P6-129]); **brief-budget policy** (pa's brief at ceiling); **Playwright browser-backend swap** (deferred 6c.27 — risks the isolated payment-critical profile). §A.4.
- **v7 CAPSTONE / pre-6d consolidation ([P6-76]/[P6-127]) — OVERDUE milestone gate before Andreas** (author ARCHITECTURE v6→v7 + reconcile STATE-OF-NATION/RUNBOOK from Phase-6 deltas; canonical docs stale since 2026-06-11). NOT an Andreas task; Peter to prioritise timing.

## Stage 1 — the LEAD: activate the durable stick-fix, then confirm the payment + responsiveness fixes end-to-end
0. **★ Bootstrap `chat-bloat-reset` (Peter, GUI — launchctl is deny-listed to the ops session).** `cp scripts/ai.openclaw.chat-bloat-reset.plist.staged ~/Library/LaunchAgents/ai.openclaw.chat-bloat-reset.plist` → `launchctl bootstrap gui/502 …` → `launchctl kickstart -k gui/502/ai.openclaw.chat-bloat-reset`. Verify `tail logs/chat-bloat-reset.log` (first run should SKIP not-bloated on a fresh chat). This makes the "goes quiet" self-heal live. Tunables via env: `CHAT_RESET_TOKENS`(130000)/`CHAT_IDLE_MIN`(20)/`CHAT_COOLDOWN_MIN`(120).
1. **★ Re-test T3 + Wikimedia on the fixed infra (the real proof).** (a) Peter messages Marie (lean session — should respond, no stick). (b) Peter asks Marie to drive the **£10 Wikimedia donation** — she should now efficient-snapshot, click `£10` by ref, reach checkout, screenshot + surface refs; then the host `pay-fill.sh` fills the card by ref on approval. Watch `logs/gateway.err.log` for any recurrence of aria/CSS/stick. If clean = payment + responsiveness confirmed; if not, capture the exact failure (do NOT synthesise). Irreversible charge → prepare-and-surface, Peter's card+2FA.
2. **Observe chat-bloat-reset in the wild** — once bootstrapped, confirm it SKIPs while healthy and (if the chat bloats) fires exactly once with the guards. Confirm it never interrupts a live turn.
3. **Reply-watcher notify utility** — read `logs/reply-watcher.log` for match-rate/noise since the flip; is the scope right? `poke` only if Peter authorises + scope tight.
4. **Reset-net production F26 + media after-window** — still carried (free inspection); do NOT synthesise a `:main` breach.

## Stage 2 — carried (secondary — don't crowd out Stage 1)
- **Battery** — T1 reliability; T4/T5 capability/payment wall (autonomy decision). T3/T6 essentially covered once 6c.28 Stage-1 re-test passes.
- **Hard code-level pulse send-gate** ([P6-128]) — still soft (HEARTBEAT rule only); a real gate is Andreas.

## Safety rails
Reversible observation/reads + host-side builds + host-side `openclaw browser` inspection + launchd **drains** run without prompting. **Surface irreversible/destructive:** openclaw.json changes + gateway restart (prefer `openclaw gateway restart` or `session-freshstart.sh` — **raw `launchctl bootout/bootstrap/kickstart` and `op` mutations + `rm` are DENY-listed to the ops session** → Peter runs via `!`, never route around), version-pin/D1, real charges (incl. Wikimedia), **1Password writes/deletes** (drain/Peter), depairing, reboots. **Aggressive slice + dreaming + compaction lever + brief-budget + Playwright swap + v7 capstone stay ring-fenced.** **Any billed turn = Peter's cost-OK first ([P6-51]); NEVER Opus for tests.** **Secrets [F21/F51]:** never read openclaw.json / agent-creds/** into context; structural jq/python only; display-immune checks. GUI/Aqua for bootstrap + restart + browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN — remaining: T3/T1 re-tests (T3 re-test is Stage 1.1), T4/T5 capability/payment decision, organic sample, reply-watcher notify utility.
- **★ PATH TO 6d ([P6-76]):** 1) close the acceptance gate; 2) **v7 CAPSTONE** (author ARCHITECTURE v6→v7 + reconcile canonical docs — OVERDUE, stale since 2026-06-11, do NOT build Andreas against unreconciled docs); 3) **6d Andreas** on the proven pair + reconciled spec.
- **Pre-Andreas status:** reply-watcher ⑧ live in **notify**; generate-mode cred store **CLOSED**; browser + payment path **fixed + guidance-corrected**; chat-bloat self-heal **staged** (bootstrap = Stage 1.0). The bloat/stick ROOT (compaction hardcap + brief injection) is Andreas's aggressive-slice mandate — the self-heal is a backstop, not the elimination.
