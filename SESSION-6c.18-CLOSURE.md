# SESSION 6c.18 — Overnight verdict (real) + P6-93 root-caused + transactional-autonomy re-scoped — CLOSURE
_2026-07-03. **SSH — `op` admin denied here, no gateway restart / config mutation this session.** Both lead builds (capped card, battery re-run) are GUI-/cost-gated and correctly did NOT execute over SSH. What shipped: the first real overnight verdict read + verifier fixed; [P6-93] "she forgets" root-caused empirically to the bootstrap injection budget; the card build re-scoped (host-mediated op-get, Peter-confirmed); three decisions taken. New codes **[P6-96/97]**. `openclaw.json` **UNCHANGED** (`8a6e2a19d9d5`/10096B, A-7 clean). **Zero billed agent turns.**_

## Stage 0 — done
- **A-7 clean:** live `8a6e2a19d9d5`/10096B, 600/staff. Pin 2026.4.22. No config mutation.
- **★ Overnight verdict — first real read ([P6-96]):** the one-shot **fired as designed** 07:15 03-Jul, reached Peter (Telegram msg 947), **self-removed** (plist gone, `launchctl` empty). Its **PARTIAL (3/6) was WRONG** — 3 false-negative greps (em-dash `.`-byte match under C locale ×2; multi-line RESET line with no leading timestamp). **The automation actually PASSED 6/6:** 23:00 suppress, 07:00 restore, **06:55 dual `:main` reset + gateway restart + sentinel re-assert**, archives. Greps fixed → re-runs PASS 6/6; script annotated + committed (config repo `25077c9`). **The 6c.15 cost-hardening automation is confirmed working end-to-end.**
- **TOOLS-slim SHAs anchored + no capability lost:** pa TOOLS `7162f0708804`/16525B, pro TOOLS `df7cfee555f6`/13078B, PRO-PLAYBOOK `3287a1052f0c`/11222B all match 6c.17. PA-PLAYBOOK drifted `fcabd04ec93c`→`40edfd843709`/19435B — **expected** (P6-95 §Accounts & credentials added late-6c.17, +1192B), verified. All key verbs grep-present in both TOOLS.md; pro never had `calendar-*` (uses `calendar-prepare` via google_helper) — no slim loss.
- **Truncation canary → root cause:** see [P6-97].

## What shipped
1. **[P6-96] overnight verifier fixed + committed** — reference artifact now scores correctly; reusable lesson logged (don't grep openclaw logs on multi-byte separators / positional pre-filters).
2. **[P6-97] [P6-93] ROOT-CAUSED (the session's headline).** Not the never-reset-session theory. The runtime injects bootstrap files under a **single total char budget** (`agents.defaults.bootstrapTotalMaxChars`, resolver default 60000; per-file `bootstrapMaxChars`) **consumed in fixed order** `AGENTS→SOUL→TOOLS→IDENTITY→…→USER` (+ extra files); once the residual `< 64` chars, **remaining files are skipped**. Proven on **fresh post-reset** sessions 03-Jul: pro TOOLS.md `12940→limit 5809` (half dropped); pa:main skips everything after USER.md. Footprints measured: **pa 92,766 / pro 73,995 chars** across 11 files each, no per-agent override. **Snapshot `bootstrapTotalMaxChars=75000` is stale vs live** (pro's 74K would otherwise fit) — live value must be read in the GUI session. Load-order corollary: cutting **early** big files frees budget for the **late** files Marie forgets.
3. **Decisions taken (AskUserQuestion) — see [P6-97]:** (a) bootstrap = **BOTH tight slim + modest raise** (GUI-staged); (b) card = **host-mediated op-get** (F70 resolved → auth-profiles premise dropped; card already in 1Password); (c) battery = **hold until budget + card land**.

## End-state (anchors)
- **`openclaw.json`: `8a6e2a19d9d5`/10096B** — A-7 clean, live==last-good, 600/staff. No config mutation.
- **Config repo:** `overnight-verify.sh` `4f8462f0f30b`→`9186db494779`/6635B, committed `25077c9` (P6-96, incl. the carried 6c.17 dryrun guard).
- **Deviations:** [P6-96/97] appended additive → log grew to `<re-anchor at commit>`. **Briefs UNCHANGED this session** (slim staged to 6c.19). Sanitised snapshot NOT refreshed (no config change).

## Carried → 6c.19 (all Peter-/GUI-gated — legitimately not runnable over SSH)
- **★ LEAD build (GUI/Screen-Sharing, Peter present):** (1) **bootstrap slim + raise together, verified on the post-change restart** (F26 = truncation-canary clears) — read the LIVE `bootstrapTotalMaxChars` first; slim early/large files (TOOLS lead, prose→PLAYBOOK) + lift the budget; Peter reviews the slim diff. (2) **Marie's capped card via host-mediated op-get** + the Peter-supplied-credential actuation ([P6-95] item 1) — 2FA-in-Revolut pause, Finance log, graduate the payment posture; irreversible-class → confirm before first live use.
- **Then battery re-run** (cheapest model, ~$5 cap) once briefs inject fully — the fair judgement-class test (T3/T6/digest). **T1 now:** confirm the rental reminder fired 19:00 Fri 3-Jul with proof-of-done.
- Standing: v7 capstone (fold P6-77…P6-97); Andreas backlog (org OAuth app, context-wipe experiment quality-watch, per-task cost tracker); DMARC tighten; 2026.4.22 pinned.

## Honest note
The one thing 6c.17 carried as unfinished — the overnight verdict — is now read, and it's good news dressed as bad: the automation worked perfectly; only the *verifier* was buggy. The session's real prize is [P6-93]: after two sessions theorising about "she forgets," the mechanism is now nailed to a specific budget/order in the build, with measured footprints and a stale-config flag — which is what makes the fix decidable. I did **not** ship the SSH brief-slim I'd offered when I asked Peter: the load-order + stale-75K discovery (both found *after* the question) made the budget **raise** the load-bearing lever and argued for doing slim+raise **together**, verified on a real restart (F26), with Peter's eyes on the diff — not rushed at the tail of a long SSH session. I surfaced that change of plan explicitly rather than deferring silently. Nothing marked done that wasn't: the overnight is proven 6/6 against real logs, the verifier fix is committed, and every gated build is carried with its exact gate.

---
_Attachments for 6c.19: this closure + `SESSION-6c.19-OPENER.md` + `PHASE-6-DEVIATIONS-LOG.md` (through P6-97) + `ACCEPTANCE-GATE-SCORECARD.md`._
