# SESSION 6c.13 — Capabilities II: cost-hardening FINISHED + payments tightened + 1Password access & account-provisioning shipped — CLOSURE
_2026-07-02. GUI/Screen-Sharing session (`launchctl managername`=Aqua ∴ D20 gateway-restart / LaunchAgent / consent ops all available). New codes **P6-86/87/88**. **`openclaw.json` UNCHANGED** (`a8783dcd17dc`/9993B — the restart only APPLIED the pre-staged [P6-85] config). WS-4 agent email was **deliberately deferred** by Peter to a focused next session (it's the only carried item). Every other objective shipped and was verified end-to-end from real runtime — no billed test turns._

## Stage 0 — ALL-GREEN
A-7 clean (live==last-good==`a8783dcd17dc`/9993B, 600/staff); pin **2026.4.22** (no bump); both containers `capable-2026-06` (up 5d); heartbeat 2h/Sonnet live; 120K cap staged; browser `hostnameAllowlist:[]` ([P6-84] holds); OAuth alive (drive-list exit 0); watchdog + colima-socket-watchdog clean; google_helper bind intact. Deviations currency: P6-83/84/85 present; log re-anchored `33c232cb4ab6`.

## What shipped (reversible, live, verified)
- **[P6-85] cost-hardening COMPLETED ([P6-86]) — all three levers live + confirmed from natural runtime.**
  - **Overnight suppression** — `heartbeat-nightwindow.sh` + LaunchAgent (23:00 off / 07:00 on; night sentinel + resume marker; manual-pause precedence). Validated via real `launchctl kickstart`.
  - **Watchdog re-tune** — STALL_MIN 70→150 (2h beats), night-sentinel skip, resume-marker anchor. Resume-anchor unit-tested; live-confirmed post-restart.
  - **120K cap applied** via gateway restart (pid →17110). **Proof:** the natural 13:57 pa beat (=11:57+2h, confirming 2h cadence) recreated **pa:main at contextTokens=120000, totalTokens 49942 (was 106031)**. Refines [P6-82]: compaction manifests on the NEXT turn, not instantly on restart.
  - **Daily fresh-session** — `session-freshstart.sh` + LaunchAgent @06:55. Empirical caveat resolved live (`--force`, Peter-authorised): the `:main` clear SURVIVES the restart → mechanism EFFECTIVE. Leaned pa (106K + a **12.5 MB** transcript) and pro (148886) to zero, archived (recoverable). Scheduled daily (Peter OK'd the reboot).
- **Stage 1 Payments tightened ([P6-87]) — F26 PASS.** §Payments rewritten so **v1 = prepare-and-surface is unambiguous** (complete checkable proposal → surface → never auto-execute; value gated at the capped card, not agent logic; the 2FA-pause/Revolut-initiate flow quarantined into deferred L3+). AGENTS.md made consistent. F26: Marie's real Opus/Telegram turn refused to auto-pay, didn't hallucinate details, offered pre-submit-screen prep, and **flagged £45 vs €45** — exact currency discipline with judgement.
- **Stage 2 [F70] resolved + 1Password access & account-provisioning ([P6-88]) — verified end-to-end.** Service-account = unattended auth; 0600 token file closes the launchd gap; `.zshrc` now single-sources it. **op host-side only, never in the sandbox** (least privilege). `op-get.sh` (retrieval) + `op-put.sh` (account creation, `op item create --generate-password`, structured Login schema, dedicated **Agent-Provisioned** vault) both live-tested (read OpenClaw ✓; create+delete Agent-Provisioned ✓), display-immune throughout. Peter decisions: SA kept **read+write** (Andreas + account-write path); new dedicated vault; new SA **openclaw-agents** rotated in (old one to be deleted).

## End-state (anchors)
- **`openclaw.json`: `a8783dcd17dc`/9993B — UNCHANGED this session.** A-7 clean (live==last-good), 600/staff.
- **Pin 2026.4.22; both containers `capable-2026-06`; gateway pid 17110; heartbeats 2h (Sonnet), overnight-off armed, 120K cap live, daily fresh-session @06:55; watchdog re-tuned + resume-anchor live.**
- **Changed/new files (SHA-anchored):** `scripts/heartbeat-nightwindow.sh` `b69bd286969b`; `scripts/heartbeat-watchdog.sh` `2e93b6c40910`; `scripts/session-freshstart.sh` `fc75bc0d5133`; `scripts/op-get.sh` `2ee53f2071ab`; `scripts/op-put.sh` `566f3612da9b`; `workspace-pa/PA-PLAYBOOK.md` `f106201d9c84`; `workspace-pa/AGENTS.md` `bc3d88f47390`; `openclaw.sanitised.json` `defb8cf2c12e`. New LaunchAgents: `ai.openclaw.heartbeat-nightwindow`, `ai.openclaw.session-freshstart`. New secret file: `agent-creds/op-service-account.token` (0600, deny-Read, value never read). **Deviations:** P6-86/87/88 appended additive-in-place.

## Carried (deliberately, not lost)
- **WS-4 agent email `marie@fedchenkov.com` → 6c.14** (Peter chose to wrap 6c.13 cleanly; WS-4 = paid Workspace signup + browser + GUI OAuth, deserves a focused run). Decisions still locked (peter@ super-admin + marie@ standard; Cloudflare; Business Starter). Phase-5 wiring brief ready (`6c11-OAUTH-FLIP-BRIEF.md` Path B; identity swap-vs-second-mailbox decision pending).
- **Peter action:** delete the retired 1Password SA (`WUE7…`); optionally verify Agent-Provisioned vault grants.
- Standing: acceptance gate + v7 capstone (pre-Andreas, [P6-76]); Travel = post-6d ([P6-85]); [F59]/[P6-66] rotations; SR-1/D23; 2026.4.22 pinned.

## Honest note
The headline of the session — the steady-state cost fix — is now not just applied but *proven from natural operation*: a real 2h beat rebuilt pa:main lean under the 120K cap with zero test spend, and the fresh-session mechanism was confirmed effective (the clear survives a restart) rather than assumed. The one place I corrected myself: an early reading that the beat hot-reload hadn't taken — the timeline showed it had; only the cap needed the restart. Payments and the full 1Password access + provisioning pattern shipped and verified end-to-end, with every secret handled env→file / display-immune (no token value ever entered context, F21/F51 held throughout the SA rotation). Least-privilege was a live negotiation: I recommended a read-only SA, Peter chose read+write for a real account-provisioning use case, and we bounded the blast radius with a dedicated vault + the L1 surface-gate instead. WS-4 was the right thing to defer — it's a paid, browser-heavy, self-contained job that shouldn't be rushed at the tail of a full session.

---
_Attachments for 6c.14: this closure + `SESSION-6c.14-OPENER.md` + `6c11-OAUTH-FLIP-BRIEF.md` (WS-4 Phase-5) + `JUDGEMENT-DOCTRINE.md` + `PHASE-6-DEVIATIONS-LOG.md` (P6-86/87/88) + `CAPABILITIES-ROADMAP-TO-6D.md`._
