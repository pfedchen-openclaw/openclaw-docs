# SESSION 6c.30 — Verify the 6c.29 brief fixes held (free) + close the acceptance gate's last live items (T5 autonomy decision, T1 reliability) + DECIDE on starting the v7 capstone. Aggressive slice + dreaming + hosted-field payments remain ANDREAS

_Opener · generated at 6c.29 close (2026-07-09). 6c.29 shipped: **T3 responsiveness PASS** (organic, healed lean session) + **3-task enumeration PASS** (all three, none dropped, TASKS.md row) + **Wikimedia donor-flow PASS** (drove to pre-pay, correctly stopped at card — the T4 drive-and-surface shape is cleared on a real site); **two live-caught defects fixed** — **[P6-133]** memory-flush `.append` rejection (durable lessons were bouncing; L42 clarified) and **[P6-134]** the CeFoLiAc case: **NOT a fabrication** (real send proven by two toolResults; the mirror rule worked), Peter was silently dropped from the cc by a `gmail_helper` `--cc` last-wins bug — **tool fixed live + recipient-verify brief note**. **Wikimedia card-injection is structurally blocked by Gr4vy cross-origin hosted-field iframes → [P6-135]/[P6-130] Andreas.** **Read [P6-133]/[P6-134]/[P6-135] first**, then this. openclaw.json **UNCHANGED** → `3267edd8b6aa`/10489B, last-good synced (A-7 clean). GUI/Screen-Sharing (D20) for any restart/browser/2FA. **Cadence: ride the window ([[ride-context-dont-close-early]]).**

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`3267edd8b6aa`/10489B**, 600/staff. Pin 2026.4.22 (00bd2cf). Levers `contextTokens=120000`, `browser.snapshotDefaults.mode=efficient`. (openclaw.json Bash-deny-listed — A-7 via applied-lever `config get` + `shasum`; structural jq/python for edits only; NEVER weaken the F21 backstop.)
- **Deviations currency:** **P6-1…135 gap-free**, no dangling refs (run [P6-101] at close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA.
- **★ Marie briefs (verify by direct sha):** `AGENTS.md` **`8ab05e5f7838`/16673B** (16455 chars — 6c.29 added the P6-133 L42 `.append` fix + P6-134 L21 recipient-verify note; **1B under the 16674B proven-safe ceiling — any further growth needs displacement**); `TOOLS.md` **`f4167b1c15c7`/15935B** (15758 chars, 7 under the 15765 clip ceiling — unchanged; **tight**); `reference-tools.md` `c3594d05c767`/4338B; `HEARTBEAT.md` `6b9d50031098`.
- **★ Shared tool — `gmail_helper.py` (bind source `agent-creds/gmail_helper.py` = container `/opt/gmail_helper.py`, serves BOTH agents):** **`f14489444597`/13596B** post-[P6-134] (`--to/--cc` now `action="append"`). Backup `scripts/gmail_helper.py.pre-6c29-p6134`. Verify by `shasum` only (agent-creds is deny-Read; never dump the `.json` creds beside it).
- **★ LaunchAgents:** reply-watcher **notify** (`9e6b476f2de7`/2218B, 900s, exit0); **chat-bloat-reset** (script `93bb06e42841`/4939B, plist `497ccbd67145`/1623B, 900s, exit0, steady-state SKIP — no loop); reset-net `heartbeat-watchdog.sh` + `session-freshstart.sh` loaded/exit0. **Do NOT run reply-watcher.sh / chat-bloat-reset.sh with side-effects** (launchd-only; dry-run/inspect only).
- **★ Marie session health:** expect `:main` lean + a low `telegram:direct`. If it climbs toward 130K, chat-bloat-reset heals it on the idle+cooldown window — observe, don't intervene.

## Stage 1 — the LEAD (all free / decision-only; no billed turn required this session)
1. **★ Verify the 6c.29 brief fixes injected + holding (free inspection).** Both take effect on Marie's next compaction/session. **F26 signals, no billed turn:** (a) **[P6-133]** — grep `gateway.err.log` for NEW `Memory flush writes are restricted` / `.append` rejections after 2026-07-09 12:25 — expect **none**; (b) **[P6-134]** — on Marie's next multi-recipient send, confirm the sent message's Cc carries **all** addresses (a `list`/`get` of the sent msg, or Peter simply sees himself cc'd). If either regresses, re-open the entry.
2. **★ Acceptance gate — the last live items (mostly Peter's).** Today cleared **T3** (PASS) + the **T4 drive-to-checkout-and-surface** shape (PASS on Wikimedia). **Remaining, per ACCEPTANCE-GATE-SCORECARD:**
   - **T5 account-create — a Peter AUTONOMY DECISION, not a build.** Both attempts (AbeBooks 20-Jun, iGolf 2-Jul) hard-blocked at the browser L1 credential/sign-up refusal. Needs Peter's call on the autonomy posture for account provisioning (the payments-autonomy model [[agent-payments-autonomy-model]] is the template) — surface it, don't improvise.
   - **T1 timed-reminder reliability** — the durable-task ledger ([P6-78]) is built; a live re-test (a reminder that must fire on time and close only on proof) is the outstanding reliability evidence. Peter-driven, billed → cost-OK first.
3. **★ DECISION POINT — start the v7 CAPSTONE? ([P6-76]/[P6-127], OVERDUE).** With the gate nearly closed and the pair demonstrably working, the capstone (author ARCHITECTURE v6→v7 + reconcile STATE-OF-NATION/RUNBOOK from Phase-6 deltas + S11 inter-agent loop-protection design) is the **last milestone before 6d Andreas**. It is a large host-side authoring effort (no billed turns) — **Peter to green-light the timing**; do NOT build Andreas against unreconciled docs.

## ⛔ FLAGGED FOR ANDREAS / milestone — do NOT attempt in interim sessions
- **Aggressive frozen-core slice** (verb-tables/procedural blocks → retrieval-only, ≤30K) + its billed verify — the REAL bloat/stick cure (~25K/turn brief injection drives the 150K sessions); chat-bloat-reset is the proven backstop. `PRE-COS-BACKLOG §A.4`.
- **Dreaming ON**; `:telegram:direct` compaction bounding (`compaction.model`/`keepRecentTokens` tunable); brief-budget policy (both briefs at ceiling).
- **★ Cross-site browsing / forms / payment robustness — Andreas ONGOING REMIT ([P6-130] §C).** Now includes **[P6-135]: hosted-field payment processors (Gr4vy/Stripe-Elements/Braintree) split card fields into cross-origin iframes — `pay-fill.sh`'s single-`--target-id` model cannot reach them; needs a multi-target/per-iframe extension.** Plus the `refs=aria`/`efficient` backend gap → **Playwright-launched-Chromium swap**; per-site recipes; `browser-dep-repair.sh` post-update guardrail; the benign `tools.allow (browser) unavailable` validation warning.
- **Hard code-level pulse send-gate** ([P6-128]) — still soft (HEARTBEAT rule only).

## Safety rails
Reversible observation/reads + host-side builds + host-side `openclaw browser` inspection + launchd drains run without prompting. **Surface irreversible/destructive:** openclaw.json changes + gateway restart (prefer `openclaw gateway restart` / `session-freshstart.sh` — raw `launchctl bootout/bootstrap/kickstart` + `op` mutations + `rm` + `docker exec` into sandboxes are DENY-listed → Peter runs via `!`, never route around), version-pin/D1, real charges, 1Password writes/deletes, depairing, reboots. **Any billed turn = Peter's cost-OK first ([P6-51]); NEVER Opus for tests** ([[no-unauthorized-test-spend]]) — the exception is a Peter-driven real-chat re-test on Marie's own Opus (T1/T3), which he authorises explicitly. **Secrets [F21/F51]:** never read openclaw.json / agent-creds/** into context; structural jq/python + `shasum` display-immune checks only. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward — ★ PATH TO 6d ([P6-76]), three tranches
1. **Acceptance/usefulness gate** — T3 ✔ + T4-drive ✔ (6c.29); **remaining: T5 autonomy decision, T1 reliability, memory-v2 reliability gate** (zero-truncation / retrieval-hit / cost-under-target). Judgement axis already materially stronger (T6 FAIL→PASS 6c.25).
2. **v7 CAPSTONE spec-authoring** — author ARCHITECTURE v6→v7; **S11 inter-agent loop-protection = highest-risk undesigned gap**; reconcile STATE-OF-NATION/RUNBOOK from Phase-6 deltas; SR-1 doc-autopilot; P6-26 derive Sophie/Andreas briefs from the CURRENT Marie/Charlotte pair. **OVERDUE — do not build against drift.**
3. **6d build-start prereqs** — F2/F26 Andreas pairing; re-enable descoped launchd jobs; F44 OAuth Internal-flip; payment + browser-repair guardrails ([P6-45], [P6-40]/[P6-42]); the [P6-135] hosted-field payment work.
- **Pre-Andreas status:** brief fixes [P6-133]/[P6-134] shipped (verify organically); browser/payment donor-flow + surface **works**, hosted-field last-mile is Andreas; chat-bloat self-heal **LIVE + PROVEN**; multi-task enumeration **PASS**; honesty/mirror discipline **validated in the wild** ([P6-134] — refuted a false self-accusation). The bloat/stick ROOT (compaction hardcap + brief injection) is Andreas's aggressive-slice mandate.
