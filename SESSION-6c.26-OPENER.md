# SESSION 6c.26 — Reply-watcher activation (Peter-gated) + continue reset-net / media-caps observation; aggressive slice + dreaming remain ANDREAS-ONLY

_Opener · generated at 6c.25 close (2026-07-05). 6c.25 was an observation + hand-off session that Peter widened mid-run to the non-payment Stage-2 billed items. **Shipped:** Stage-0/1 all-green re-verify; reset-net confirmed **armed but not-yet-fired on a real breach** (the 16:20 log activity was 6c.24 fixtures — [P6-123]); media after-window **still closed** (carried read); doctor-hygiene **holds**; **battery T6 FAIL→PASS** on a single Sonnet turn ([P6-123]); **reply-watcher (⑧) built + logic-verified, staged not bootstrapped** ([P6-124]) — with an honest **incident**: a PATH-stub test leak fired 1 real Opus poke + 3 spurious sends (corrected, msg 1114; lesson in memory). openclaw.json **UNCHANGED** (`eb08cfa7d7b6`/10654B; A-7 clean). **Read [P6-123]+[P6-124] first**, then this. GUI/Screen-Sharing (D20) for any bootstrap/restart/browser/2FA._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`eb08cfa7d7b6`/10654B**, 600/staff. Pin 2026.4.22 (00bd2cf). Sandboxes `capable-2026-06`. (openclaw.json is Bash-deny-listed to Claude Code — A-7 via applied-lever `config get`, never a direct file hash; config edits go via `scripts/apply-*.sh` under explicit permission or Peter runs them. NEVER weaken the F21 backstop.)
- **Deviations currency:** **P6-1…124 gap-free**, no dangling refs (run the [P6-101] carry-chain check at close). Pre-existing 21/22 dupes are historical — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA.
- **★ Reset net live?** `scripts/heartbeat-watchdog.sh` = **`183896a33dd2`/20438B**, `scripts/session-freshstart.sh` = **`55020ef7acd6`/6637B**. Self-check: `WATCHDOG_DRYRUN=1 zsh scripts/heartbeat-watchdog.sh` → healthy `:main` emits no RESET. Benign residue: `logs/heartbeat-watchdog.reset-pa.state`=`0` (6c.24 test seed; does NOT suppress a real reset — leave it).
- **★ Media caps + memory index** unchanged from 6c.25: `snapshotDefaults.mode=efficient`, image 1024 / pdf 15p / 8MB, `contextTokens=120000`, compaction safeguard/40000/sonnet-4-6, hybrid mmr+temporalDecay; `toolResultMaxChars` **unset**. Index pa 35/35·125, pro 31/31·36 (Dirty no, Dreaming off, recall 12·0). Freshstart LIVE 06:55.
- **★ Reply-watcher staged (NOT live):** `scripts/reply-watcher.sh` = **`7763696a30f8`/5840B**; `scripts/ai.openclaw.reply-watcher.plist.staged` = **`d3ed81e12992`/1858B**. Confirm `launchctl print gui/502/ai.openclaw.reply-watcher` = **not loaded** and no plist in `~/Library/LaunchAgents` (staged only) unless Peter activated it since.

## ⛔ FLAGGED FOR ANDREAS — do NOT attempt in interim sessions (Peter, 6c.23-followup)
Unchanged. Both are quality-impacting substrate changes reserved for the CoS; do not attempt even though reversible:
- **Aggressive frozen-core slice** (verb-tables + procedural blocks → retrieval-only, ≤30K). With recall=0, moving hot content out is a regression risk until a **billed fresh-session turn** proves organic search. `PRE-COS-BACKLOG §A.4 item 3`.
- **Dreaming ON** (nightly episodic→semantic promotion). `PRE-COS-BACKLOG §A.4 item 5`.
- **The billed retrieval-usage verify turn** is coupled to the slice → also Andreas's.

## Stage 1 — the LEAD: reply-watcher activation (Peter-gated) + continue observation
1. **Reply-watcher activation — surface + let Peter choose, do NOT enable unilaterally.** The poller is built + logic-verified + proven end-to-end; it is **staged**, targeting Marie/pa (Opus) for pokes. Peter decides: (a) **scope query** (the default is `is:unread newer_than:2d -category:{promotions,social,updates,forums}` — he may want tighter, e.g. a label or a sender allowlist); (b) **mode** — `dryrun` (verify the live poll cost-free), `notify` (free Telegram heads-up), or `poke` (BILLED, capped `REPLYWATCH_MAX_PER_DAY`=4). Activation one-liners are in the staged plist header; **bootstrap is GUI-bound (D20)**. A good first step is a `dryrun` bootstrap + `kickstart` to prove the live-cred Gmail poll under launchd (cost-free), then flip to `notify`. **Cost-safety learned ([P6-124]):** never PATH-stub `openclaw` in a test — run DRYRUN paths or inspect; the real CLI will fire if a branch calls it.
2. **Reset-net production F26 (free inspection)** — once a genuine intra-day `:main` breach occurs, confirm the self-heal fired: `logs/heartbeat-watchdog.log` shows `reset applied` → archive under `agents/<id>/sessions/archive/<date>/` → sentinel epoch updated → Peter notified → `:main` lean. **If still no real breach, say so and keep watching.** Do NOT synthesise one.
3. **Media-caps before/after ([P6-120] item-1)** — read `state/memory-health/daily.log` over the days since 6c.23 once real `mode:efficient` browser/PDF turns land; recalibrate §8 off the real "after" (still frozen at 6c.25).

## Stage 2 — carried (secondary — do not let these crowd out Stage 1)
- **Battery** — **T3** (Marie/Opus) re-test via **Peter's own Telegram** (faithful path; never-Opus-for-tests bars me firing it) or a future authorised Sonnet-equivalent; **T1** reliability re-test; **T4/T5** capability/payment wall (autonomy decision). T6 is PASS ([P6-123]).
- **Payment — Wikimedia £10 donation** teed up as a real payments-F26: Marie drives `donate.wikimedia.org` to a £10 payment page in her isolated profile, then surfaces for Peter's card+2FA. Billed browser-heavy Opus turn → fire on Peter's explicit go. Irreversible → prepare-and-surface, never auto-execute.
- **generate-mode credential store** — round out [P6-115] (only `existing` proven live).

## Closed at 6c.25 (do not re-carry)
Battery T6 = PASS ([P6-123]); reply-watcher = built + logic-verified + staged ([P6-124], activation carried, not the build). Doctor-hygiene = holding ([P6-122]/[P6-123]).

## Safety rails
Reversible observation/reads + host-side builds run without prompting. **Aggressive core-slice + dreaming stay ring-fenced to Andreas — do NOT attempt.** **Surface irreversible/destructive:** openclaw.json changes + gateway restart (Peter-run script or explicit per-message permission — F21 backstop categorical), version-pin/D1, real charges (incl. the Wikimedia donation), 1Password writes/deletes, depairing, reboots, `rm` (deny-listed → Peter/`trash`), **and any billed turn (Peter's cost-OK first, [P6-51] — and NEVER Opus for tests; target `--agent pro`/Sonnet).** Do NOT synthesise a live `:main` breach or manual restart to "test" the reset net. **Do NOT PATH-stub `openclaw` in tests ([P6-124]) — the real CLI fires; run DRYRUN or inspect.** **Secrets ([F21/F51]):** op host-only; display-immune checks; never read openclaw.json / agent-creds/** into context (even `ls` of agent-creds is blocked — reference by path). GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN. 6c.25 strengthened the judgement axis (T6 PASS). Remaining gate signals: T3/T1 re-tests, T4/T5 capability/payment decision, organic sample, + the Andreas-owned/billed memory signals (organic recall>0, media after-window falling, dreaming-on + week-clock).
- **Pre-Andreas:** memory architecture de-risked (retrieval live, dedup done, levers+caps applied, observability wired, conservative restructure shipped, intra-day + overnight `:main` bounding both live); reply-watcher ⑧ now built (activation pending). Remainder = aggressive slice + verify turn + dreaming + week-clock (`PRE-COS-BACKLOG §A.4`). Then v7 capstone reconciliation ([P6-76]) → 6d Andreas.
- **Andreas mandate:** first-principles review/refactor of faulty foundations (Peter, extends P6-49) — inherits the full v2 plan + working retrieval + a measuring canary + de-risked baseline + the aggressive slice teed up + a self-healing (overnight+intra-day) `:main` reset net + a built reply-watcher.
