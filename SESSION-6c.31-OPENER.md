# SESSION 6c.31 — v7 CAPSTONE / pre-6d CONSOLIDATION. The acceptance gate is **PASS on all agent-quality dimensions** (6c.30); now author ARCHITECTURE v6→v7, design **S11 inter-agent loop-protection**, reconcile STATE-OF-NATION/RUNBOOK from the Phase-6 deltas, and derive the **Sophie/Andreas briefs** from the CURRENT Marie/Charlotte pair. CAPTCHA-solver + hosted-field payments + aggressive slice + dreaming remain ANDREAS.

_Opener · generated at 6c.30 close (2026-07-09). **6c.30 landed the whole acceptance gate** — per Peter's "pass through all prior gates in this one" instruction. **[P6-136]:** the T5 account-create "L1 wall" was researched to ground (**brief-policy + model-reticence, NOT a runtime guard**), Peter set **"gate at value"**, the brief enablement shipped + Sonnet-validated, **`cred-fill.sh` was built**, and the **live E2E RAN** (Peter-waived Opus, 3 bounded turns on Letterboxd): Marie **autonomously drove the signup** and the op-generated password was **stored in 1Password + host-filled into the form, Marie never seeing it** — the **L1 wall is SOLVED end-to-end**; only **hCaptcha on submit** blocks a created account (the deferred CAPTCHA-solver [P6-39]). **[P6-137]:** memory-v2 **RATIFIED PASS** (the two 'fails' were gate-mismeasurement — a 256KB telemetry log cap, not context loss; retrieval returns hits when queried). **[P6-138]:** a 2-week Marie/Telegram forensic — ~30 glitches, all fixed or Andreas-deferred; the one unowned gap (silent outbound bounces) got an interim HEARTBEAT bounce-check rule. **T1** reliability verified (fired 19:57, row held open). **£0 host-side; openclaw.json UNCHANGED** (`3267edd8b6aa`/10489B, A-7 clean, no model swap). **Read [P6-136]/[P6-137]/[P6-138] first**, then this. GUI/Screen-Sharing (D20) for any restart/browser/2FA. **Cadence: ride the window ([[ride-context-dont-close-early]]).**_

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`3267edd8b6aa`/10489B**, 600/staff. Pin 2026.4.22 (00bd2cf). Levers `contextTokens=120000`, `browser.snapshotDefaults.mode=efficient`. pa model **`anthropic/claude-opus-4-7`** (unchanged — 6c.30's authorised Sonnet swap never persisted; the T5 E2E ran on Marie's Opus under Peter's one-off never-Opus waiver). (openclaw.json Bash-deny-listed for jq/mv/cp — A-7 via `shasum` + `config get`; any model swap is **Peter-run via `!`**, revert from `openclaw.json.last-good`.)
- **Deviations currency:** **P6-1…138 gap-free**, no dangling refs (`re.findall` audit clean at 6c.30 close; run [P6-101] again at this close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA.
- **★ Marie briefs (verify by direct sha):** `AGENTS.md` **`2445fd82d3a1`/16671B** (16452 chars — 6c.30 P6-136 gate-at-value line; **3B under the 16674B ceiling — any growth needs displacement**); `TOOLS.md` **`f4167b1c15c7`/15935B** (unchanged; **tight**, 7 under clip ceiling); `PA-PLAYBOOK.md` **`7eb2d59fbe42`/27401B** (6c.30 §Accounts gate-at-value rewrite + cred-fill handoff; not truncation-injected, has room); `MEMORY.md` **`4cb72ea15dcd`/5547B** (6c.30 added "Recall first", then linter/Peter trimmed back under the soft-cap; a RATIONALE-rotation curation pass is still a nice-to-have); `HEARTBEAT.md` **`f580d9152b1e`/2918B** (6c.30 P6-138 bounce-check rule); `reference-tools.md` `c3594d05c767`/4338B.
- **★ Host scripts (6c.30):** `scripts/cred-fill.sh` **`2b81d5a6c4e6`/5314B** — generated-password → signup-form fill (create-side twin of `pay-fill.sh`); **proven in the T5 E2E** (rc=0, filled without the model seeing the value). **⚠ `pay-fill.sh` likely carries the same latent PATH bug** cred-fill had (hardcoded PATH missing `~/.npm-global/bin`) — verify + fix as part of the Andreas payment work. **Memory index fresh** (6c.30 reindex: pa 39/39·314 chunks, pro 35/35).
- **★ Shared tool — `gmail_helper.py`** (bind source `agent-creds/gmail_helper.py` = container `/opt/gmail_helper.py`, both agents): **`f14489444597`/13596B** (post-[P6-134] `--to/--cc append`). Backup `scripts/gmail_helper.py.pre-6c29-p6134`. Verify by `shasum` only (agent-creds deny-Read).
- **★ Backups from 6c.30:** `workspace-pa/AGENTS.md.pre-6c30-p6136` (`8ab05e5f7838`), `workspace-pa/PA-PLAYBOOK.md.pre-6c30-p6136` (`23a571ff1817`).
- **★ LaunchAgents:** reply-watcher notify, chat-bloat-reset (steady-state SKIP, no loop), reset-net (heartbeat-watchdog + session-freshstart) — all loaded/exit0. **Do NOT run reply-watcher.sh / chat-bloat-reset.sh with side-effects** (launchd-only; dry-run/inspect).
- **★ Marie session health:** expect `:main` lean + a low `telegram:direct`. chat-bloat-reset heals on idle+cooldown if it climbs — observe, don't intervene.

## Stage 1 — the LEAD: v7 CAPSTONE / pre-6d consolidation ([P6-76]/[P6-127], OVERDUE — canonical docs stale since 2026-06-11)
_The gate is landed; this is the milestone-boundary consolidation that must precede any Andreas build. **This is a doc/spec-authoring session — largely £0, no billed agent turns needed.** Do NOT build Andreas against unreconciled docs._
1. **★★ ARCHITECTURE v6→v7.** Author the v7 architecture from the accumulated Phase-6 reality: the 2-tier browser model + host-mediated secret-fill (`pay-fill`/`cred-fill` + op-get/op-put, `op` host-only), gate-at-value autonomy, the durable-task ledger, the chat-bloat-reset self-heal net, memory-v2 (recalibrated), the payments prepare-and-surface model. Supersede `MASTER-ARCHITECTURE-v6.md` additively; anchor every subsystem to its P6 evidence.
2. **★★ S11 inter-agent loop-protection — the highest-risk UNDESIGNED gap.** Before Sophie/Andreas add agent-to-agent messaging, design the loop/storm protection (message budgets, cycle detection, TTL/hop-count, a kill-switch). This is the one architectural hole that can't ship unaddressed.
3. **★ Reconcile the snapshot docs from the Phase-6 deltas.** `PROJECT-STATE-OF-NATION.md` + `PROJECT-RUNBOOK.md` are snapshots, stale since 6c.10-ish; reconcile them from the P6-code deltas (per the doc-tier protocol). Update the lessons/patterns docs.
4. **★ Derive the Sophie + Andreas briefs from the CURRENT Marie/Charlotte pair (P6-26).** AGENTS.md/TOOLS.md/HEARTBEAT.md for Sophie (family coordinator) and Andreas (CoS) derive from the **live** Marie/Charlotte canonical versions — never a stale template. Locate the exact live paths on the Mini and read them fresh; do not reinvent.
5. **★ SR-1 doc-autopilot design** — the loop by which Andreas owns doc-currency + the carry-chain [P6-101] check from 6d. Spec it here.
6. **★ Re-reconcile the open backlog ([P6-101] milestone step).** Fold every still-open item into `PRE-COS-BACKLOG.md` so the chain doesn't drift between consolidations (the per-opener chain alone drifted 6c.17/6c.19). The Andreas remit list below is the input.

## ⛔ FLAGGED FOR ANDREAS / milestone — do NOT attempt in interim sessions
- **★ CAPTCHA-solver ([P6-39]) — the acceptance gate's sole remaining last-mile.** Blocks a fully-*created* T5 account (Letterboxd hCaptcha, 3-Jul Opera) AND a completed T4 payment on CAPTCHA-gated checkouts. The mechanisms up to the wall are proven; only the CAPTCHA solve is missing. External-capability, not agent behaviour.
- **★ Cross-site browsing / forms / payment robustness — Andreas ONGOING REMIT ([P6-130] §C).** [P6-135] hosted-field processors (Gr4vy/Stripe-Elements/Braintree) split card fields into cross-origin iframes → `pay-fill.sh`'s single-`--target-id` model can't reach them; needs multi-target/per-iframe. **⚠ + the `pay-fill.sh` PATH bug** (same class as the cred-fill bug fixed live 6c.30 — verify + fix before the payment work). Plus the `refs=aria`/`efficient` backend gap → **Playwright-launched-Chromium swap**; per-site recipes; `browser-dep-repair.sh` post-update guardrail; the benign `tools.allow (browser) unavailable` validation warning.
- **★ Robust outbound-bounce/DSN detection ([P6-138]).** Extend `reply-watcher` into a proper inbound-DSN sweep (structured bounce parse + task-linkage) — the 6c.30 HEARTBEAT bounce-check clause is the interim heuristic, not the durable solution.
- **Hard code-level pulse send-gate** ([P6-128]) — still soft (HEARTBEAT drafts-never-sends is brief-only; a real false send occurred earlier — [P6-138]).
- **Aggressive frozen-core slice** (verb-tables/procedural blocks → retrieval-only, ≤30K) + its billed verify — the REAL bloat/stick cure; chat-bloat-reset is the proven backstop. `PRE-COS-BACKLOG §A.4`.
- **Dreaming ON**; `:telegram:direct` compaction bounding (`compaction.model`/`keepRecentTokens`); brief-budget policy (both briefs at/near ceiling).
- **`cred-fill` productionisation (optional):** the core capability is BUILT + proven; only a full autonomous-trigger (a queue/watcher that fills the pw mid-turn without a host operator, vs today's host-operator/hook invocation like `pay-fill`) could join §C later.

## Safety rails
Reversible observation/reads + host-side builds + doc-authoring + host-side `openclaw browser` inspection + launchd drains run without prompting. **Surface irreversible/destructive:** openclaw.json changes + gateway restart (prefer `openclaw gateway restart` / `session-freshstart.sh`; raw `launchctl bootout/bootstrap/kickstart` + `op` mutations + `rm` + `docker exec` into sandboxes + `jq`/`mv`/`cp` on openclaw.json are DENY-listed → Peter runs via `!`, never route around) + version-pin/D1 + real charges + 1Password writes/deletes + depairing + reboots. **This is primarily a doc session — expect £0 billed.** Any billed turn = Peter's cost-OK first ([P6-51]); **NEVER Opus for tests** ([[no-unauthorized-test-spend]]) — the 6c.30 T5 Opus run was a one-off Peter-authorised waiver, not a standing licence. **Secrets [F21/F51]:** never read openclaw.json / agent-creds/** into context; structural `config get`/`jq`-on-stdin + `shasum` display-immune checks only. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward — ★ PATH TO 6d ([P6-76]), three tranches
1. **✔ Acceptance/usefulness gate — PASS on all agent-quality dimensions (6c.30).** T1 reliability ✔, T2 ✔, T3 ✔, T4-drive ✔, **T5 mechanism + gate-at-value autonomy ✔ (proven end-to-end)**, T6 ✔, memory-v2 ✔, honesty/mirror ✔. Sole residual = the CAPTCHA/hosted-field transactional last-mile (Andreas infrastructure, mechanisms proven).
2. **v7 CAPSTONE spec-authoring — THIS SESSION'S LEAD.** ARCHITECTURE v6→v7; S11 inter-agent loop-protection (highest-risk undesigned gap); reconcile STATE-OF-NATION/RUNBOOK from Phase-6 deltas; SR-1 doc-autopilot; P6-26 derive Sophie/Andreas briefs from the CURRENT Marie/Charlotte pair. **OVERDUE — do not build against drift.**
3. **6d build-start prereqs** — F2/F26 Andreas pairing; re-enable descoped launchd jobs; F44 OAuth Internal-flip; payment + browser-repair guardrails; the [P6-135] hosted-field + CAPTCHA-solver work.
- **Pre-Andreas status:** the assistant trio's usefulness gate is **cleared**; the transactional axis (T4/T5) is cleanly blocked only on **Andreas-class infrastructure** (CAPTCHA solve + cross-origin hosted-field fill), mechanisms proven; honesty/mirror validated in the wild ([P6-134]); chat-bloat self-heal LIVE+PROVEN; memory-v2 PASS; outbound-bounce interim-guarded ([P6-138]). Ready to consolidate docs → then pair Andreas.
- **Optional reversible follow-ups (Peter-run, non-blocking):** bound Marie's telegram session (`CHAT_RESET_TOKENS`→~70000 via plist env + reload); periodic reindex job; clean up orphan 1Password item `efrhqyifslubmapond7wb6mmee` (surface-first `op delete`); MEMORY.md RATIONALE-rotation.
