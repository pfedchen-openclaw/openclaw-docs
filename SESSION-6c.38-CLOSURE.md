# SESSION 6c.38 — CLOSURE: A2A agent-side emitter convention AUTHORED (hub + spoke) + proven host-valid £0 — the [P6-152] host gates now have a client; real-agent dryrun (F26) stays Peter-gated

_Stage 2's main build landed: the agent-side envelope emitter — unbuilt since [P6-152] — is now authored into all three agents' briefs and **proven host-valid two ways, £0** (pure-function replay + a staged real-launchd dryrun script). Stage 1 (verify the 6c.37 mitigations in production) done inspection-only. **£0 — no billed agent turns.** **openclaw.json UNTOUCHED (`24715f4045bd`/10748B, A-7 clean).** A2A stays INERT. Deviations: **P6-160..161 new (additive-in-place)**. Stage 3 (Andreas roots) — established as genuinely gated (not just deferred), see §Andreas-track note; carried._

## Done
- **Stage 0 GREEN.** SSH empty + user `openclaw` + `gui/502` (restart-capable, D20 ok); A-7 clean (live==last-good==`24715f4045bd`/10748B/600/staff); carry-chain PASS (P6-1..159 gap-free at open); version `2026.4.22 (00bd2cf)`; dist patch persists (`reply-media-paths.runtime-DegAVXTm.js`=`6dfbfe762078`/6847B); all four 6c.37 mitigation scripts match the opener SHAs (`memory-dedup 4f5494c5c27d`, `open-loops-watch 84715da28689`, `chat-bloat-reset e4a6bd9a4b40`, `memory-health 969c99ea31e8`); AGENTS.md `2721b4a909ff`; A2A INERT (HALT present, `a2a-watch.py 96aa0247e52a`).
- **Stage 1 — 6c.37 mitigations verified in production (inspection, £0).**
  - ✔ **Dedup holding** — `memory-dedup --agent pa` = **0 removable today AND corpus-wide** (`--all-days`); the autoheal + intra-day hook are keeping it clean.
  - ✔ **Reset 90K + graceful heads-up** — `logs/chat-bloat-reset.log` confirms the 14:49 production reset (the [P6-155] compaction-spiral recovery): `RESET tokens=179098>=90000 … cooldown-clear` → Telegram heads-up sent (msg 2017) → archived → gateway restart. Currently `SKIP no-chat-session` (Marie idle/clean).
  - ◐ **Open-loops watcher** — LIVE in dryrun, polling clean (`no SLA breach mode=dryrun`), but `open-loops.json.loops = []` (empty). Marie hasn't registered a loop since the 14:49 reset (no task cycle yet). **Escalate flip stays deferred** until she populates one on a real task — carried, not dropped.
  - ✗ **Delta-only flush root NOT applied** — conclusive: the apply script targets `.agents.defaults.compaction.memoryFlush.prompt` in openclaw.json, and openclaw.json is byte-identical (`24715f4045bd`) to the 6c.37 staged state. Peter-gated handoff (apply at next restart; dedup autoheal holds it clean meanwhile, so not urgent).
- **★ Stage 2 step 1 — A2A emitter convention AUTHORED ([P6-160]).** Derived from the CURRENT COS/PA/PRO playbooks ([P6-26]); host contract read from `a2a-watch.py` + `shared/a2a/README.md` (envelope L1, gate-(a) inline reply, gate-(b) host-authoritative hop/path, result-receipt shapes).
  - **Hub (Andreas):** full recipe in `COS-PLAYBOOK §S11 → "Emitter mechanics"` + a pointer in `cos/TOOLS.md §Control-plane`.
  - **Spokes (Marie/Charlotte):** core guardrail in each hot `AGENTS.md` (**answer in-turn, never message the hub back, never re-send** — the host relays replies inline); rare originate mechanics in `PA-PLAYBOOK §Coordination` / `pro/AGENTS.md §Coordination`. Bloat-conscious: Marie's hot file grew only +690B (detail pushed to the PLAYBOOK).
  - **Key correctness point:** the recipe tells agents to set the semantic fields ONLY and **omit `hop`/`path`** — the host reconstructs them (gate (b)). Proven correct.
- **★ Stage 2 step 1 — verified £0, two ways, zero state mutation.**
  - (1) **Pure-function replay** (`scratchpad/verify_convention.py`): the recipe's envelope (no hop/path) → host injects `hop=1/path=[self]` → `validate_envelope`=ok + `check_budgets`=ok for hub cos→pa AND spoke pa→pro; 3 negative controls (missing task_ref/origin, empty message) correctly REFUSE. Ledger unchanged, HALT present, no results/ written.
  - (2) **Real-launchd dryrun script STAGED** — `apply-6c38-a2a-convention-dryrun.sh` (`dc0edebe71c5`/5810B/755, Peter-run via `!`): clones the proven `apply-6c36` self-restoring bootstrap but seeds the NEW documented shape (no hop/path) to prove the FULL watcher pipeline (`handle_one`→reconstruct→validate→budgets→`write_result`) accepts the agent-written envelope over real launchd, then restores INERT. £0 (dryrun never dispatches). `bash -n` clean; seed shape verified in a temp dir (real outbox untouched, 0 pending).
- **★ Stage 2 hardening ([P6-161]) — `hop_spoof` refined + convention institutionalised, £0.** The recipe's "omit hop/path" made every honest emit trip `hop_spoof=true` (pure-observability flag, gates nothing). Refined `handle_one` so an ABSENT claim is not a spoof (only a supplied-and-differing value is) — malice signal preserved, honest emits clean. **Institutionalised as TEST 6** in `a2a-watch-test.py`: the recipe envelope is allowed/`would-dispatch`, host-reconstructs `hop1/path[cos]`, `hop_spoof=false` on omit, `true` on a differing assertion. **Full suite 20→26 checks, all PASS** (backward-compatible). Today's one-off proof is now a permanent regression guard.
- **★ Andreas-track note — the deep Marie cures are GATED, not merely deferred (established this session).** The #1 bloat cure (aggressive frozen-core slice) is blocked on retrieval-invocation: [P6-137] proves the index returns hits on demand but **production recall = 0/day** (agents don't organically call `memory_search`; a brief instruction is necessary-but-insufficient — proven on pro for weeks). Moving hot-core content to retrievable memory would make it unreachable during Marie's real turns → capability loss on a LIVE agent. The unblock is the `active-memory` plugin (auto pre-turn retrieval) = **openclaw.json + restart** (Peter declined it 6c.30 on metrics grounds — but the framing is now "the prerequisite for the actual bloat cure", a genuine re-decision). Confirming any slice worked also needs a **billed** fresh-session turn. So "trust your judgement" correctly does not unlock this — surfaced as a decision, not force-built.

## State (SHA-anchored)
- **openclaw.json** `24715f4045bd`/10748B, 600/staff, live==last-good (A-7 clean). **Untouched this session.**
- **Dist patch (persists):** `reply-media-paths.runtime-DegAVXTm.js`=`6dfbfe762078`/6847B.
- **Changed briefs (6c.38):** `workspace-cos/COS-PLAYBOOK.md` `6fd7e058c11b`→**`e269fa662ebc`**/24735B (2 edits: emitter recipe [P6-160] + hop_spoof phrasing [P6-161]) · `workspace-cos/TOOLS.md` `13ef069624d0`→**`f81ea6883a67`**/8816B · `workspace-pa/AGENTS.md` `2721b4a909ff`→**`226681c1420f`**/20278B · `workspace-pa/PA-PLAYBOOK.md` `115571f78e8a`→**`ad7f9ad05f83`**/34538B · `workspace-pro/AGENTS.md` `ef594735f48e`→**`bafe6eaf93cb`**/14756B.
- **Changed watcher ([P6-161]):** `scripts/a2a-watch.py` `96aa0247e52a`→**`ec873a68177e`**/24744B (hop_spoof refine — **new resting anchor**) · `scripts/a2a-watch-test.py` `cb2496341265`→**`3308e2b09569`**/13799B (TEST 6; 26 checks).
- **New (6c.38):** `scripts/apply-6c38-a2a-convention-dryrun.sh` **`dc0edebe71c5`**/5810B/755 · `_backups-6c38-a2a-emitter-brief/` (5 pre-edit briefs + `scripts~a2a-watch.py.pre-hopspoof`) · `scratchpad/verify_convention.py` (host-valid replay harness).
- **Unchanged:** all 6c.37 mitigation scripts; A2A INERT (HALT present, plist staged-not-loaded, real ledger 3 lines `19bc6da01ac9`, `sessions_send/spawn` OFF).
- **Agents:** `pa`=Marie/opus-4-7, `pro`=Charlotte/sonnet-4-6, `cos`=Andreas/opus-4-7/paired/**A2A-OFF**. Gateway healthy.

## Peter-gated to apply (handoff — GUI/D20, Peter runs via `!`)
1. **A2A convention real-launchd dryrun (OPTIONAL, £0)** — `./scripts/apply-6c38-a2a-convention-dryrun.sh` — real-launchd confirmation the watcher accepts the documented shape; self-restores INERT. Zero spend. Do this before the billed step below if you want the extra assurance.
2. **A2A real-agent dryrun (F26, BILLED — cost-authorised)** — the genuine "does Andreas actually emit correctly" test: bootstrap the staged plist `A2A_MODE=dryrun`, remove HALT (posture change), drive ONE real hub→spoke ask (cheap/short turn — but Andreas is opus-4-7, so agree the model/cost envelope first per [[no-unauthorized-test-spend]]), verify `ledger.jsonl` over genuine agent output, then re-HALT + bootout. **Not run this session — needs Peter's cost OK + GUI.**
3. **Delta-only flush prompt** ([P6-154]) — `./scripts/apply-6c37-memory-flush-prompt.sh apply` + `openclaw gateway restart` at your next natural restart (dedup autoheal holds the corpus clean meanwhile; low urgency).
4. **Open-loops watcher → escalate** ([P6-156]) — after Marie registers a real loop and a clean dryrun shakedown, flip `OPENLOOPS_MODE=escalate` in the plist (bootout/bootstrap/kickstart). Currently correct-but-idle (empty register).
5. **★ DECISION — `active-memory` plugin (the unblock for the Marie bloat cure).** The aggressive frozen-core slice (the real cost/latency/stick cure — Marie 128K/3-compactions/$0.99 per turn vs Andreas 44K/0/$0.025, same model+cap) is BLOCKED because production recall = 0/day (agents don't organically `memory_search`; brief instruction proven insufficient). `active-memory` does auto pre-turn retrieval → makes moved content reachable → unblocks the slice. **Activation (corrected empirically, [P6-162]): `plugins.slots.memory="active-memory"`** — a **fleet-global slot swap** off the current `memory-core` (NOT [P6-137]'s assumed `plugins.entries.active-memory.enabled`), openclaw.json + restart. **Tradeoff now legible:** it's a **per-reply bounded-blocking sub-agent** (billed sub-agent turn + latency on every eligible reply, all agents), trading brief-injection tokens for per-turn sub-agent cost. Peter declined it 6c.30 for **metrics** reasons; re-decision framing = **"the prerequisite for the actual bloat problem."** **Unknown to resolve before apply:** does the slot swap retain the memory-core index + `memory_search` tools or replace them? **If Peter says go, I resolve that (dist-read + dryrun), then stage the F17 apply-script + a billed fresh-session verify.** (Retrieval index currently `Dirty: yes` both agents — a periodic reindex job becomes worthwhile once retrieval is operationally used.)

## Carried → 6c.39
1. **★ A2A Stage 2 steps 2–3 (Peter-gated).** Convention is authored + host-valid; the remaining gates are the F26 real-agent dryrun (handoff #2) → then the live flip (`A2A_MODE=live` + remove HALT + grant `sessions_send`, hard-gate `sessions_spawn`; openclaw.json + restart). Kill lever: `touch shared/a2a/HALT`. **Do NOT flip live** unless the real-agent dryrun is clean AND Peter authorises.
2. **★ Andreas track proper (Stage 3) — the four durable Marie roots ([P6-153]):** aggressive frozen-core brief-slice (§A.4 HEADLINE — bloat/latency/stick cure); engine live-chat keep-alive + bounded worker-session isolation; production retrieval recall activation ([P6-118]/[P6-137]) — also the [P6-157] micro-skills-recall enabler; hard pulse send-gate ([P6-128]) + per-task model routing ([P6-28]/[P6-144]).
3. **Mitigations still-settling (inspection next time):** open-loops watcher fires clean once Marie populates a loop (then escalate flip); a real flush post-restart leaves `dup×1` (once flush-prompt applied).
4. **Lower-priority carries:** `@fedchenkov` deliverability (Andreas stewardship, DNS propose-only); in-place `draft-update` verb ([P6-158], optional Peter-gated); restaurant/place screenshot store (Travel-gated ~early Aug, [P6-147]); Andreas avatar (optional reversible); cost-model reconciliation awaiting Peter's Admin API key ([P6-148]); Charlotte `digest-<date>.md` handoff.

## Discipline
Cost: **£0** — host-side authoring + pure-function replay + a staged Peter-run dryrun script; no billed agent turns, no Opus test-loops, no dispatch ([[no-unauthorized-test-spend]]). No secrets read (openclaw.json by sha only via `shasum`; `agents.defaults` inspected structurally via `config get`, no value dump; no `agent-creds/**` touched). openclaw.json untouched (A-7 byte-clean). Every brief change SHA-anchored + reversible (backup-first `_backups-6c38-a2a-emitter-brief/`). The convention was proven host-valid BEFORE being handed on. A2A resting posture INERT throughout (HALT present, real ledger unchanged, no dispatch; the watcher edit is pure-observability, gates nothing, full suite 26/26). Deny-list respected (a compound with `rm -rf` correctly refused → re-issued without `rm`; launchctl/HALT-removal/openclaw.json left to Peter). The deep Marie cures were established as genuinely gated and surfaced as a decision, not force-built on a live agent. Deviations additive-in-place (P6-160..161). Carry-chain audit PASS at open; re-run at close.
