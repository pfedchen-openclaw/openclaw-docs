# SESSION 6c.39 — CLOSURE: A2A went LIVE and is validated end-to-end (the milestone) · active-memory applied the RIGHT way (coexists with memory-core) · a live Andreas incident diagnosed + brief-fixed + a pre-spend live-dispatch bug caught

_Both headline stages shipped. **Stage 1 (A2A dryrun→live) is COMPLETE + proven through the real runtime (F26):** hub→host-meter→spoke→inline-reply→relay, `rc=0`, a real 2722-char Charlotte reply, relayed to Peter — no reverse envelope. **Stage 2 (active-memory) applied the RIGHT way:** entry-hook (NOT the [P6-163] slot swap), `memory_search` survived. Mid-session Peter flagged Andreas "stuck + core-functionality issues" → root-caused to a **stale briefing brief** (pointed at the retired+unreachable `shared/daily-digests/`); fixed. A pre-live-flip diligence read caught a **`--deliver` bug in the never-run live-dispatch path** and fixed it before any spend. One over-assertion (AGENTS.md truncation) self-corrected. **openclaw.json `24715f4045bd`→`0f57e26c1391`/11086B (A-7 clean — the session's ONE deliberate change, the active-memory entry).** Deviations: **P6-165..168 new** (+ live-confirm updates on 166 & 168). ~£0.30–0.60 of Peter-authorised A2A spend; £0 of test-loop waste._

## Done
- **Stage 0 GREEN.** SSH empty + user `openclaw` + `gui/502` (Aqua, restart-capable, D20 ok); A-7 clean at open (`24715f4045bd`); carry-chain PASS (P6-1..164 gap-free); version `2026.4.22 (00bd2cf)`; dist patch persists (`reply-media-paths.runtime-DegAVXTm.js`=`6dfbfe762078`); A2A INERT at open (HALT present, `a2a-watch.py ec873a68177e`).
- **★ Stage 1 — A2A LIVE + validated end-to-end ([P6-165]/[P6-168]).**
  - **Posture flip (reversible, Claude).** 4 briefs "off"→"ACTIVE" (A2A-via-mailbox active; `sessions_send`/`sessions_spawn` tool-grants correctly kept OFF): `workspace-cos/COS-PLAYBOOK.md` §S11+footer, `workspace-cos/TOOLS.md` header, `workspace-pa/AGENTS.md` + `workspace-pro/AGENTS.md` spoke headings. Backups `_backups-6c39-a2a-posture-flip/`.
  - **Bring-up lifecycle script** `scripts/apply-6c39-a2a-bringup.sh` **`c11386523790`** (`up`/`status`/`live --confirm-live`/`down`; PlistBuddy dryrun→live; guarded).
  - **Dryrun verify PASSED** — Peter `up` + Telegram ping → Andreas emitted a convention-clean envelope (`6c39-02-cos-pro`, `hop_spoof:false`, attributed message) → ledger `would-allow`; **the agent-side emitter is proven end-to-end by a real agent** (closes [P6-160]→[P6-164]). Andreas correctly detected dryrun + surfaced to Peter in real time — no silent stall.
  - **★ `--deliver` bug caught pre-spend + fixed ([P6-168]).** The never-run `dispatch()` used `openclaw agent --agent <to> --deliver …` — the P6-164 malformed shape (no `--reply-to`) → would `dispatch-failed` and/or fire an unintended outbound. GATE (a) already captures the reply from **stdout**, so removed `--deliver`. `scripts/a2a-watch.py` `ec873a68177e`→**`27a2ce99c9d0`** (backup `.pre-6c39-deliverfix`); test suite **26/26**.
  - **★ LIVE confirmed.** Peter flipped `live`; Andreas re-emitted `6c39-03-cos-pro` → ledger **`event=allow, mode=live, dispatch=rc=0`**; result **`decision=dispatched, from=pro, reply=2722 chars`** (Charlotte scanned the Soveren inbox for real) → Andreas relayed to Peter. The `--deliver` removal validated (full reply, not empty). Live budget counter tracks separately.
- **★ Stage 2 — active-memory applied the RIGHT way ([P6-166]).** Dist-read: active-memory is a `before_prompt_build` **entry-hook** whose sub-agent CALLS `memory_search` (a client of memory-core, not a slot owner) → the [P6-163] slot swap was self-defeating. Corrected script `scripts/apply-6c39-active-memory-scoped.sh` **`62c513149cc7`** (entry-only + slot-guard; supersedes the 6c.38 slot version). Peter applied + restarted (gateway pid 91072→ then 11:30 reload, 7 plugins incl. active-memory, no error). **Verified:** `plugins.slots.memory` still unset (memory-core owner), active-memory entry live (`enabled, agents:[pa], haiku-4-5`), **`memory_search` SURVIVED** (39/48 files, 314 chunks, Vector+FTS ready). Coexistence proven — the 6c.38 failure mode avoided.
- **★ Andreas core-functionality incident diagnosed + fixed ([P6-167]).** Root cause = a **stale briefing brief**: `AGENTS.md`/`COS-PLAYBOOK.md` still said "*I* assemble the 08:00 briefing from `shared/daily-digests/<date>/*.md`" — but 6c.35 ([P6-148]/[P6-150]) moved it to a **host cron `cos-briefing.py`** reading `workspace-<id>/memory/digest-<date>.md`; `shared/` is unreachable from his sandbox (only `workspace-cos → /workspace`). Fixed 6 refs: `workspace-cos/AGENTS.md` `09a1aeb76564`→**`4c93299501af`**, `workspace-cos/COS-PLAYBOOK.md` `c11ebbf27252`→**`3e0c5fe49da7`**. Backups `_backups-6c39-cos-briefing-brief-fix/`. Andreas recovered cleanly post-fix. **Self-corrected over-assertion:** the bootstrap-truncation is **middle-out** (`HEAD .75`/`TAIL .25`) so the tail "Failures — never silent" is preserved, not cut; and `AGENTS.md` (17,674) is under the 30k per-file cap — it only truncated because that session was bloated. Judgement: **no trim** (bloat-conditional, low-value, anti-churn).

## State (SHA-anchored)
- **openclaw.json** `0f57e26c1391`/11086B, 600/staff, live==last-good (A-7 clean). ONE change this session: `plugins.entries.active-memory` added. `agents.defaults.contextTokens=120000` (the old 120K cap is in place).
- **A2A resting posture — NOW LIVE (changed from INERT).** `scripts/a2a-watch.py` **`27a2ce99c9d0`**; plist INSTALLED + LOADED (`~/Library/LaunchAgents/ai.openclaw.a2a-watch.plist`, RunAtLoad + 60s poll, persists across reboot), `A2A_MODE=live`, **HALT absent**. Kill lever `touch shared/a2a/HALT`; teardown `apply-6c39-a2a-bringup.sh down`. `a2a-watch-test.py` `3308e2b09569` (26/26). Real ledger now includes 3 live-day dryrun lines + the `6c39-03` live `allow` dispatch.
- **Dist patch (persists):** `reply-media-paths.runtime-DegAVXTm.js`=`6dfbfe762078`/6847B.
- **Changed briefs (6c.39):** cos/AGENTS.md **`4c93299501af`** · cos/COS-PLAYBOOK.md **`3e0c5fe49da7`** · cos/TOOLS.md **`4a396dd13dbc`** · pa/AGENTS.md **`795ccd069dbc`** · pro/AGENTS.md **`6dea36edf60e`**.
- **New scripts (6c.39):** `apply-6c39-a2a-bringup.sh` `c11386523790` · `apply-6c39-active-memory-scoped.sh` `62c513149cc7`. **Backups:** `_backups-6c39-a2a-posture-flip/`, `_backups-6c39-cos-briefing-brief-fix/`, `scripts/a2a-watch.py.pre-6c39-deliverfix`.
- **Agents:** `pa`=Marie/opus-4-7 (active-memory recall now on her direct turns, Haiku sub-agent), `pro`=Charlotte/sonnet-4-6, `cos`=Andreas/opus-4-7/paired/**A2A-LIVE**. Gateway healthy (pid 91072).

## Peter-gated / natural next (handoff)
1. **active-memory sub-agent-fires check (£0, Peter-natural).** On Peter's next real Marie **direct** Telegram message (post-11:30), Claude inspects the log for the pre-turn Haiku recall sub-agent firing + its cost. No dedicated billed test. Only THEN is the frozen-core slice safe to design.
2. **Retrieval reindex** — `memory status` shows `Dirty: yes` (pa 39/48). Now that active-memory queries `memory_search` every Marie direct turn, a stale index degrades recall → a reindex + a periodic reindex job is now worthwhile.
3. **Delta-only flush prompt ([P6-154]) — STILL unapplied** (config `memoryFlush.prompt` empty; the 11:30 restart didn't apply it). Run `apply-6c37-memory-flush-prompt.sh apply` at the next restart (dedup autoheal holds the corpus meanwhile; low urgency).
4. **A2A live — light supervision.** First real multi-hop days: watch the rate governor / per-pair + daily budgets / the 6c.9-sawtooth caution. Kill lever ready.

## Carried → 6c.40
1. **★ Stage 3 — the frozen-core slice (THE headline, now UNBLOCKED).** active-memory (auto pre-turn recall) is live, so moving hot-core Marie content to retrievable memory no longer makes it unreachable. Once the fires-on-turn check passes (#1 above), the aggressive frozen-core brief-slice — the real bloat/latency/stick cure (Marie 128K/3-compactions/$0.99 vs Andreas 44K/0/$0.025) — becomes safe. Plus the other three durable Marie roots ([P6-153]): engine live-chat keep-alive + bounded worker-session isolation ([P6-149]); production retrieval recall (active-memory is now the enabler — verify it lifts organic recall); hard pulse send-gate ([P6-128]) + per-task model routing ([P6-28]/[P6-144]).
2. **★ Digest-curation lapse (from the [P6-167] incident) — real functional gap.** No agent writes a curated `digest-<date>.md` (last 16 Jul); Charlotte's raw digest lands 08:17, *after* the 08:00 cron → thin/empty briefings (the incident's proximate cause). Needs: curation-before-08:00 (Marie/Charlotte brief habit or a pre-cron step) + `cos-briefing.py` timing/fallback hardening. Overlaps the long-standing "Charlotte `digest-<date>.md` handoff" carry.
3. **active-memory follow-through:** fires-on-turn confirm (#1) → observe recall quality + Haiku cost → then the frozen-core slice; the reindex (#2).
4. **Mitigations still-settling:** open-loops watcher → escalate flip ([P6-156]) once Marie registers a real loop; delta-only flush prompt apply ([P6-154], next restart).
5. **AGENTS.md middle-truncation** — bloat-conditional, low-urgency; folds into the frozen-core work (leaner briefs → no truncation even when bloated). Note, don't act standalone.
6. **Lower-priority (persistent):** `@fedchenkov` deliverability (Andreas, DNS propose-only); in-place `draft-update` verb ([P6-158]); restaurant/place screenshot store (Travel-gated ~early Aug, [P6-147]); Andreas avatar (optional); cost-model reconciliation awaiting Peter's Admin API key ([P6-148]).

## Discipline
Cost: **~£0.30–0.60** — the Peter-authorised A2A live round-trip (Andreas re-emit + Charlotte scan); the dryrun emits were £0 (no dispatch). Zero test-loop / Opus-test waste ([[no-unauthorized-test-spend]] respected — the sub-agent-fires + recall checks were deliberately deferred to Peter-natural Marie turns, no dedicated billed tests). openclaw.json ONE deliberate F17 change (active-memory entry, `24715f4045bd`→`0f57e26c1391`, A-7 clean, live==last-good, no rollback). No secrets read (config by `config get`/`shasum` only; no `agent-creds/**`). Every brief + script change SHA-anchored + backed up + reversible. **The `--deliver` bug was caught by A-12 diligence BEFORE spending** (the P6-164 lesson applied). **An over-assertion (truncation) was self-corrected before acting on it** ([[dont-overassert-researched-conclusions]]). A2A go-live surfaced as the trust change it is; the live flip was Peter's explicit `!`. Deviations additive-in-place (P6-165..168 + updates). Carry-chain audit PASS at open (P6-1..164) and close (P6-1..168 gap-free).
