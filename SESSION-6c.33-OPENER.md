# SESSION 6c.33 — **6d ANDREAS PAIRING (the irreversible/billed cluster).** The reversible foundation is BUILT (6c.32, [P6-140]): Andreas's live `workspace-cos/` brief-set, the S11 host-mailbox/watcher (L1–L5, 14/14 dryrun-tested, INERT), the sessions_spawn per-window budget (same watcher), and the SR-1 `carry-chain-audit.py` (dogfooded — it caught + I fixed a false-negative in its own append-only check). This session does the part that needs **Peter's hand + billed turns**: register the `cos` agent, pair his Telegram bot, and F26 him live — then enable A2A incrementally. **Hard ordering holds: S11 is already built; keep A2A OFF until it is bootstrapped in dryrun and proven over real traffic before `live`.**

_**Scope resolved at 6c.32 (Peter, AskUserQuestion):** this session = "full reversible foundation, stop before pairing" is DONE; 6c.33 is the pairing. **F26 = Sonnet smoke-test only** (a model override for the pairing test — NEVER Opus, even though Andreas is Opus-default; [[no-unauthorized-test-spend]]). Everything here is GUI/D20 + surfaced/Peter-run: openclaw.json edits are Bash-deny-listed (jq/mv/cp) → Peter applies via `!`; the gateway restart + BotFather + 2FA are GUI-bound._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`3267edd8b6aa`/10489B**, 600/staff. Pin 2026.4.22. Levers `contextTokens=120000`, `browser.snapshotDefaults.mode=efficient`. pa model `opus-4-7`. (openclaw.json Bash-deny-listed — verify via `shasum` + `config get`, never Read; any change Peter-run via `!`, revert from `openclaw.json.last-good`.)
- **Deviations currency:** **P6-1…141 gap-free**, no dangling refs — re-run `scripts/carry-chain-audit.py --log _session-docs/PHASE-6-DEVIATIONS-LOG.md --repo _session-docs` at this open AND close (it's now the scripted [P6-101] check; the append-only path bug was fixed 6c.32). Known 21/22 header dups — leave them.
- **Context check first ([P6-117]):** confirm GUI empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/pairing/2FA. **This session IS billed** (F26 Sonnet turn) and irreversible (a real bot, a config change) — get Peter's go per step.
- **★ Foundation to verify unchanged (6c.32 [P6-140]):** `workspace-cos/` AGENTS.md `bbff1a0184f4`/16729B, TOOLS.md `13ef069624d0`, HEARTBEAT.md `7a8a09d6872c`, SOUL.md `7669d69566e1`, COS-PLAYBOOK.md `c05bfb71baa4`, USER-CONTEXT `e9be26588752`, MEMORY `d9ec7b085323`; `scripts/a2a-watch.py` `a8b0c2f68aed` (+ staged plist `666e5e868e6f`, INERT), `scripts/carry-chain-audit.py` `d71982c77be9`; `shared/a2a/` (HALT present=fail-closed, ledger empty, README `f08b7fa26028`).
- **★ Not yet committed (6c.32):** the build artefacts + close docs are authored but **uncommitted** — commit is the first act here (build artefacts → `openclaw-config`; `_session-docs/` → `openclaw-docs`; **push surfaced**).

## Stage 1 — the LEAD: pair Andreas (billed + irreversible; each step surfaced/Peter-gated)
1. **★★ Complete/activate the EXISTING openclaw.json `cos` block** (6c.32 finding [P6-140]: `cos` is **already registered** — one of the 9 scaffolded roster agents, near-complete but DORMANT/unpaired; NOT a greenfield add). First **inspect it structurally** — `openclaw config get agents.list` (ids only) + jq-on-keys via Peter (jq-on-openclaw.json is deny-listed) — and record what's already set vs missing. Then stage the delta as `openclaw.json.new` (F17: same-dir staged edit, atomic `mv`, `chmod 600 && chgrp staff`; **Peter applies via `!`**). Target state, mirroring the live `pa`/`pro` blocks' shape (P6-24 — per-agent blocks carry a full sandbox block): name/display "Andreas", channel Telegram `accountId:"cos"`, model `opus-4-7`, sandbox `mode:"all"`/`scope:"agent"`, browser SSRF `dangerouslyAllowPrivateNetwork:false`, workspace `workspace-cos`, creds `agents/cos/agent/auth-profiles.json` (pre-exists). **CRITICAL — verify tool grants: A2A OFF on the existing block** (confirm `sessions_send`/`sessions_spawn` are NOT already enabled from the scaffold; `gateway` denied). Grant `cron` + read Google + `web_search`/browser + `llm_task` + Telegram + `message send`. Conservative cadence (few lightweight beats + the 08:00 cron). **Verify A-7 delta** (new sha ≠ old; re-sync `last-good` after a clean apply).
2. **★★ Gateway restart** (GUI/D20, Peter-run) to load the `cos` agent. Confirm it registers (`config get` structure, logs) — not by a billed turn yet.
3. **★★ BotFather pairing (F2).** New bot, the 4-scope profile, `openclaw agents set-identity`; avatar = the **first male portrait** (generate at docs-lock per `BRIEF-DERIVATION-ANDREAS.md §(e)` — keep the Style+Composition invariants verbatim, against the locked **Charlotte** exemplar; gold "A" pin; red lobster brooch).
4. **★★ F26 — one Sonnet smoke-test** (model override; NOT Opus). A single short turn through the real Telegram→gateway→`cos` path: confirm Andreas boots his brief, replies in-voice (as-himself, no sign-off in DM), and honours the propose-not-execute posture. Verify through the actual runtime (F26), not the embedded `--local` CLI ([[embedded-cli-mistests-model]]). **This is the session's only authorised spend.**
5. **★ Enable A2A incrementally (S11 built + hardened 6c.32, [P6-140]/[P6-142]).** **FIRST resolve the [P6-142] must-resolve-before-live design gates** (`shared/a2a/README.md §Open`): (a) the **envelope-construction + topology** decision — hub-synchronous vs peer-relay, define who mints `conversation_id`/increments `hop`/appends `path`, and reconcile the L1 cycle-check vs the per-pair budget (the #1 gate — A2A may not function otherwise); (b) **provenance hardening** (host-reconstruct from the ledger vs accept the accidental-loop-only threat model). THEN bootstrap the **staged** `ai.openclaw.a2a-watch.plist` (GUI/D20) in **`A2A_MODE=dryrun`** and verify `shared/a2a/ledger.jsonl` over real traffic. Only then: flip `A2A_MODE=live` **and** remove `shared/a2a/HALT` **and** enable `sessions_send` (+ hard-gated `sessions_spawn`) in openclaw.json. Kill lever anytime: `touch shared/a2a/HALT`.

## Stage 2 — layer the object-level remit (only after F26 PASS; each F26-verified before the next)
- 08:00 cross-agent digest cron; per-task cost tracker [P6-85]; SR-1 doc-currency ownership (**first task: reconcile the v7 PART 10 `openclaw-config`→`openclaw-docs` drift, [P6-141]**); infra-currency steward loop (propose-only; `browser-dep-repair.sh` after any update).

## ⛔ FLAGGED FOR ANDREAS / 6d build-content (design done, build is his object-level remit — do NOT attempt in interim)
Transactional last-mile (CAPTCHA [P6-39] + hosted-field card-fill [P6-135] + the `pay-fill.sh` PATH bug); browser backend gap (Playwright-Chromium swap; per-site recipes; `browser-dep-repair.sh` guardrail); hard code-level gates (pulse send-gate [P6-128] + marie@ approval-token); outbound-bounce/DSN sweep [P6-138]; the bloat/stick CURE; S9 monitoring suite. Full ledger: `PRE-COS-BACKLOG.md`.

## Safety rails
Reversible reads + host-side authoring + `git commit` run without prompting. **Surface irreversible/outward-facing:** the **`git push`** (both repos — confirm before pushing); the **openclaw.json `cos` block + gateway restart**; **BotFather pairing + 2FA**; the **billed F26 turn** (Sonnet only — Peter's cost-OK is given for a *Sonnet smoke-test*, re-confirm before firing); enabling A2A (`live` + HALT removal + tool grants). `openclaw.json` jq/mv/cp + raw `launchctl bootout/bootstrap/kickstart` + `op` mutations + `rm` + `docker exec` are DENY-listed → Peter runs via `!`, never route around. **Secrets [F21/F51]:** never Read openclaw.json / agent-creds/** — structural `config get`/`jq`-on-keys + `shasum` display-immune only. GUI/Aqua for restart + pairing + 2FA (D20).

## Carry-forward — ★ PATH TO 6d ([P6-76]), status
1. **✔ Acceptance gate PASS (6c.30); ✔ v7 CAPSTONE (6c.31); ✔ Andreas reversible foundation (6c.32, [P6-140]).**
2. **→ 6d pairing = THIS SESSION** (Stage 1) → then his object-level remit (Stage 2 + the ⛔ backlog) as billed capabilities land, each F26-verified.
- **★ [P6-143] Marie signature-on-drafts fix** — the signature only appends on the direct `send` verb; Marie's L1 `draft`→`send-draft` approve-flow sends UNSIGNED. Fix prepared: `scripts/apply-6c33-sig-on-draft.py` (sign marie@ drafts at draft-time). **Peter runs it via `!`** (deny-Read helper), then: verify a real marie@ send carries the signature + **re-sync the agent-tools mirror** if one is kept separately from the `agent-creds/gmail_helper.py` bind source.
- **P6-134 organic verify** — a real multi-cc send landing BOTH recipients on Marie's next such send (free-inspection).
- **[P6-141] doc-currency:** v7 PART 10 §SR-1 names `openclaw-config` for the docs (should be `openclaw-docs`) — Andreas's first doc-currency reconcile task; the live briefs are already corrected.
- **Optional reversible (Peter-run, non-blocking):** `CHAT_RESET_TOKENS`→~70000; periodic reindex; orphan 1Password item `efrhqyifslubmapond7wb6mmee`; MEMORY.md RATIONALE-rotation; Charlotte/Marie avatars.
