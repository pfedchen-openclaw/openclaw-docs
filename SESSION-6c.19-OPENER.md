# SESSION 6c.19 — Marie's capped card (host-mediated op-get) + verify the bootstrap fix held overnight ([P6-98/99])
_Opener · generated at 6c.18 close (2026-07-03). 6c.18 read the first real overnight verdict (automation PASSED 6/6; verifier greps fixed [P6-96]), **root-caused AND fixed [P6-93]** — the bootstrap injection budget was starving the briefs; raised `bootstrapTotalMaxChars` 75000→110000, restarted, and **verified zero truncation via a real Sonnet turn** ([P6-97/98]) — which also **resolved Charlotte's overnight breakdown** (truncated briefs + a batch-archive tooling gap, both fixed). **Run in a Screen-Sharing GUI terminal (D20)** for the card (op-admin + 2FA). Read the 6c.18 closure + `PHASE-6-DEVIATIONS-LOG.md` [P6-96/97/98] first._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`82a293d97aac`/10097B**, 600/staff (was `8a6e2a19d9d5`; the 6c.18 bootstrap raise). Pin **2026.4.22**. Sandboxes `capable-2026-06`. (Bash deny blocks `sha256sum`/`cat`/`jq`/`stat` **and any `openclaw.json`-naming command** — read it by structure via a script, e.g. `scripts/stage-bootstrap-raise.py` pattern; use `shasum -a 256`+`wc -c` only if the deny lets a bare path through.)
- **Deviations currency:** P6-98 present; re-anchor the log sha (re-anchored at 6c.18 commit).
- **★ VERIFY the bootstrap fix HELD through the 06:55 reset** — the real proof. `grep 'truncating in injected context' logs/gateway.err.log | grep '2026-07-04'` should be **empty** for `pa:main`/`pro:main` on the morning-fresh sessions. And check **Charlotte actually swept the Soveren inbox** on her first morning beat (a triage/archive artefact or a "nothing needs you" digest) — the function-level check, not just beat-liveness ([P6-98] blind-spot). If truncation recurs, the variable session-state consumer exceeded 110000 → consider a further modest raise or the optional slim.
- **Config repo clean?** 6c.18 committed `overnight-verify.sh` (`25077c9`) + `stage-bootstrap-raise.py` + `sync-lastgood.py`. `_session-docs` nested repo committed separately.

## Stage 1 — the work (GUI, Peter-paced)
1. **★ LEAD — Marie's capped card via host-mediated op-get ([P6-97] decision).** The card is **already in 1Password/OpenClaw**; **[F70] is resolved** (service-account unattended `op read`). Build: at the pre-payment screen the **host** pulls the card via `scripts/op-get.sh 'op://OpenClaw/<item>/<field>'` and fills the browser, **or** a **one-time** save into the warmed host Chrome autofill — single-source, no second copy, no `auth-profiles.json`, no restart. Keep the **2FA-approve-in-Revolut pause** after Marie initiates + the **Finance log**; £200/mo issuer cap is the hard ceiling. **Companion — Peter-supplied-credential actuation ([P6-95] item 1):** host `op-put` store → one-time portal login into the persistent Chrome profile. Both **irreversible-class first-use → confirm before the first live charge/login**, Peter present for the 2FA. Then graduate Marie's payment brief off pure prepare-and-surface for capped-card spends (keep the 2FA pause + surface-before-first-use).
2. **Battery re-run — now UNBLOCKED** (briefs inject fully post-raise). Cheapest model (Sonnet/Haiku, **never Opus**), ~$5 hard cap, stop-and-surface if trending over ([P6-51]). Convert T3/T6/digest FAIL→PASS through the **real Telegram** path ([embedded-cli]). **T1 reliability:** confirm the rental-agreement reminder fired **19:00 Fri 3-Jul** with proof-of-done (in the past by 6c.19 — check first).
3. **Optional (not urgent) — the slim half of "both".** The raise fixed truncation; a light slim only **reduces per-beat injected tokens** (cost). If done: trim early/large files (TOOLS lead), **mirror the [P6-98] batch-archive line into pa TOOLS.md**, Peter reviews the diff. Skip if the card + battery fill the session.

## Stage 5 — Verify (F26) + Stage 6 — Close
Card tested to the 2FA pause with Peter; battery re-runs through real Telegram. Close: deviations additive ([P6-99]…); `SESSION-6c.19-CLOSURE.md`; `SESSION-6c.20-OPENER.md`. Re-anchor shas; refresh sanitised snapshot if config changed; commit `_session-docs` (nested) + config repo.

## Safety rails (whole session)
Reversible build runs without prompting. **Surface irreversible/destructive:** the card first-use + credential first-login, real sends beyond the gated flow, OAuth/Workspace/GCP/billing, version-pin/D1, 1Password writes/deletes, depairing, reboots. **Config edits** reversible via last-good (F17/A-7) — verify on restart before declaring done. **Cost ([P6-51]):** inspection-first; battery within the ~$5 auth; single cheap smoke-tests only, never Opus/loops/batches. **Secrets ([F21/F51]):** never read secret values; openclaw.json by structure / staged scripts / display-immune. GUI/Aqua for restart/`op`-admin/card (D20). Stop at ~63% context for a clean close.

## Carry-forward
- **Acceptance gate** stays OPEN — decidable when the judgement class re-runs PASS (briefs now inject fully → fair) **and** the capability class is unblocked (card + credential actuation) **and** T1 holds. Then → author the 6d Andreas opener.
- **Andreas backlog:** **function-level agent-health check** (did the morning sweep produce an artefact — not just beat-liveness, [P6-98]); `:telegram:direct` compaction-pressure ([P6-98]); org-owned OAuth app (P6-90/95); per-task cost tracker ([P6-85]#1); context-wipe experiment quality-watch ([P6-95]#3); model/framework currency ([P6-74]). DMARC tighten. Travel POST-6d. 2026.4.22 pinned.
