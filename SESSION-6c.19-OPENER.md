# SESSION 6c.19 — Bootstrap slim+raise (fix "she forgets", verified) + Marie's card via host-mediated op-get ([P6-97/98])
_Opener · generated at 6c.18 close (2026-07-03). 6c.18 read the first real overnight verdict (automation PASSED 6/6; verifier had 3 false-negative greps, fixed+committed [P6-96]), and **root-caused [P6-93]** to the bootstrap **injection budget** (single total char budget consumed in file order → late files skipped = "she forgets"), with Peter's three decisions taken. **Run this in a Screen-Sharing GUI terminal (D20)** — the budget raise (config edit + gateway restart), `op` admin, and the 2FA card test are all GUI-bound. Read the 6c.18 closure + `PHASE-6-DEVIATIONS-LOG.md` [P6-96/97] before acting._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`8a6e2a19d9d5`/10096B**, 600/staff. Pin **2026.4.22**. Sandboxes `capable-2026-06`. (Bash deny blocks `sha256sum`/`cat`/`jq`/`stat` on `openclaw.json` — use `shasum -a 256` + `wc -c`; edit only via staged `.new` + atomic `mv` + `chmod 600 && chgrp staff`, F17.)
- **Deviations currency:** P6-97 present; re-anchor the log sha (was re-anchored at 6c.18 commit).
- **Verify the config repo is clean** (`git -C ~/.openclaw status`) — 6c.18 committed `overnight-verify.sh` (`9186db494779`, commit `25077c9`). `_session-docs` is a **nested repo** (committed separately at close).
- **★ Read the LIVE bootstrap budget first** — this SSH-deny couldn't `jq` openclaw.json; in the GUI session read `agents.defaults.bootstrapTotalMaxChars` / `bootstrapMaxChars` **by structure** (python/jq on the live file is fine here — it's a number, not a secret). Confirm whether it's the snapshot's 75000 or lower, and whether `loadExtraBootstrapFiles` adds to the injected set. This sets the raise target + slim depth.

## Stage 1 — the work (GUI, Peter-paced)
1. **★ LEAD — fix "she forgets" ([P6-97], Peter's "BOTH" decision). Do slim + raise TOGETHER and verify on the restart (F26).**
   - **Slim (Peter reviews the diff):** cut/consolidate the **early, large** bootstrap files first — every char off an early file frees budget for the late files that get skipped. Order is `AGENTS→SOUL→TOOLS→IDENTITY→…→USER`. Lead target **TOOLS.md** (pa 16.5K / pro 13.1K): dedupe the repeated "surface on error (P6-22)" reminders, move reference prose to the **PLAYBOOK** (late/skippable — fine for reference detail she can re-read), **keep every invocation + posture rule** (grep-verify the verb list survives). Then trim SOUL/AGENTS restated prose. Back up to scratch first; SHA-anchor before/after. Footprints to beat: pa 92,766 / pro 73,995 chars.
   - **Raise (F17 staged edit + restart):** lift `agents.defaults.bootstrapTotalMaxChars` to comfortably fit the slimmed footprint with headroom (e.g. ~95–100K if live is ~75K; higher if live is lower — read it first). **Modest** — it adds injected context to every turn (partly offsets the 120K cost-cut), so size it to *fit the briefs*, not more. Stage `openclaw.json.new` → atomic `mv` → `chmod 600 && chgrp staff`; keep `.last-good` byte-identical after a clean apply; **re-anchor the live sha** (A-7). **P6-24:** if pa/pro carry their own blocks, sync them.
   - **VERIFY (F26):** restart the gateway (GUI/D20), then confirm the **truncation canary clears** — `grep 'truncating in injected context' logs/gateway.err.log` should stop showing pa/pro `:main` events on fresh sessions; ideally confirm via a real cheap agent turn that Marie now holds a rule that previously lived in a skipped file. Refresh the sanitised snapshot after the config change (F72).
2. **★ Marie's capped card — host-mediated op-get ([P6-97] decision).** Card is **already in 1Password/OpenClaw**; F70 resolved. Wire: at the pre-payment screen the **host** pulls the card via `scripts/op-get.sh 'op://OpenClaw/<item>/<field>'` and fills the browser (or a **one-time** save into the warmed host Chrome autofill) — single-source, no second copy, no `auth-profiles.json`. Keep the **2FA-approve-in-Revolut pause** after Marie initiates + the **Finance log** each spend; £200/mo issuer cap is the hard ceiling. **Companion — Peter-supplied-credential actuation ([P6-95] item 1):** host `op-put` store → one-time portal login into the persistent Chrome profile. Both are **irreversible-class first-use → confirm before the first live charge/login**; Peter present for the 2FA. Then graduate Marie's payment brief off pure prepare-and-surface for capped-card spends (keep the 2FA pause + surface-before-first-use).
3. **Battery re-run — ONLY after the briefs inject fully** (else truncated briefs bias the judgement class). Cheapest model (Sonnet/Haiku, **never Opus**), ~$5 hard cap, stop-and-surface if trending over ([P6-51]). Convert T3/T6/digest FAIL→PASS through the **real Telegram** path (embedded CLI mis-tests — [embedded-cli]). **T1 reliability:** confirm the rental-agreement reminder fired **19:00 Fri 3-Jul** with proof-of-done (check first thing — it's in the past by 6c.19).

## Stage 5 — Verify (F26) + Stage 6 — Close
Truncation-canary clears post-restart (the real F26 for the bootstrap fix); card tested to the 2FA pause with Peter. Close: deviations additive ([P6-98]…); `SESSION-6c.19-CLOSURE.md`; `SESSION-6c.20-OPENER.md`. Re-anchor all shas (incl. the new live `openclaw.json`); refresh sanitised snapshot; commit `_session-docs` (nested) + config repo.

## Safety rails (whole session)
Reversible build runs without prompting. **Surface irreversible/destructive:** the card first-use + credential first-login, real sends beyond the gated flow, OAuth/Workspace/GCP/billing, version-pin/D1, 1Password writes/deletes, depairing, reboots. **Config edits** (the budget raise) are reversible via `.last-good` — proceed under F17/A-7, but **verify on the restart before declaring done**. **Cost ([P6-51]):** inspection-first; battery re-run within the ~$5 auth only. **Secrets ([F21/F51]):** never read secret values; openclaw.json + `agent-creds/**` by structure / staged edits / display-immune. GUI/Aqua for restart/`op`-admin/card (D20). Stop at ~63% context for a clean close.

## Carry-forward
- **Acceptance gate** stays OPEN — decidable only when the judgement class re-runs PASS (needs the bootstrap fix first) **and** the capability class is unblocked (card + credential actuation) **and** T1 holds. Then → author the 6d Andreas opener.
- **Andreas backlog:** org-owned OAuth app (Fedchenkov Workspace) to replace pfedchen `openclaw-pf` (P6-90/95); per-task cost tracker ([P6-85]#1); context-wipe experiment quality-watch ([P6-95]#3); model/framework currency ([P6-74]). DMARC `p=none`→quarantine once reputation builds. Travel = POST-6d. 2026.4.22 pinned.
