# SESSION 6c.40 — close out active-memory (fires-on-turn) → the frozen-core slice (THE Marie bloat cure, now UNBLOCKED) and/or the digest-curation fix; first session with A2A LIVE

_6c.39 was a big session — two headline stages shipped. **A2A is LIVE and validated end-to-end** ([P6-165]/[P6-168], `rc=0`, real 2722-char Charlotte reply relayed by Andreas — hub→meter→spoke→inline-reply→relay all proven). **active-memory applied the RIGHT way** ([P6-166]) — entry-hook, memory-core preserved, `memory_search` survived (the 6c.38 slot-swap failure mode avoided). Mid-session: **Andreas core-functionality incident** root-caused to a stale briefing brief (retired `shared/daily-digests/` path) + FIXED ([P6-167]); a **`--deliver` bug in the never-run live-dispatch path caught pre-spend + fixed** ([P6-168]); one truncation over-assertion self-corrected. **openclaw.json `24715f4045bd`→`0f57e26c1391`/11086B (A-7 clean — one deliberate change, the active-memory entry).** Deviations: P6-165..168._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live==last-good==**`0f57e26c1391`/11086B**, 600/staff. Pin 2026.4.22 (`00bd2cf`). `agents.defaults.contextTokens=120000`. `cos`=Andreas/opus-4-7/**A2A-LIVE**, `pa`=Marie/opus-4-7 (**active-memory recall on her direct turns**), `pro`=Charlotte/sonnet-4-6. `plugins.slots.memory` unset (memory-core owner); `plugins.entries.active-memory` live (`enabled, agents:[pa], haiku-4-5`).
- **★ A2A is now LIVE (posture CHANGED from INERT — verify it behaved).** `scripts/a2a-watch.py`==**`27a2ce99c9d0`** (`--deliver` removed [P6-168]); plist INSTALLED+LOADED (`~/Library/LaunchAgents/ai.openclaw.a2a-watch.plist`, RunAtLoad+60s, `A2A_MODE=live`), **HALT absent**. **Stage-0 must check A2A didn't misbehave overnight:** `bash scripts/apply-6c39-a2a-bringup.sh status` — watcher loaded + `last exit code = 0`; `tail shared/a2a/ledger.jsonl` sane (no runaway loop, no unexpected `allow` storms, no auto-`halt_tripped`); live budget counters (`shared/a2a/state/a2a-counters-live-*.json`) within caps. Kill lever `touch shared/a2a/HALT`; teardown `apply-6c39-a2a-bringup.sh down`. `a2a-watch-test.py`==`3308e2b09569` (26/26).
- **★ Dist patch persists:** `reply-media-paths.runtime-DegAVXTm.js`==**`6dfbfe762078`**/6847B. Re-apply `apply-6c34-screenshot-media-fix.sh` + restart only if it reverts.
- **Deviations currency:** P6-1..168 gap-free — re-run `scripts/carry-chain-audit.py` at open AND close.
- **Context/GUI ([P6-117]/D20):** confirm `SSH_CONNECTION` empty + `gui/502` before any restart/billed turn. Billed test = agree the envelope FIRST ([[no-unauthorized-test-spend]]).
- **Changed-brief anchors (6c.39):** cos/AGENTS.md `4c93299501af`, cos/COS-PLAYBOOK.md `3e0c5fe49da7`, cos/TOOLS.md `4a396dd13dbc`, pa/AGENTS.md `795ccd069dbc`, pro/AGENTS.md `6dea36edf60e`.

## Stage 1 — close out active-memory (£0, the quick gate to the frozen-core slice)
active-memory is applied + memory-core-safe, but its **pre-turn Haiku recall sub-agent has not yet been observed firing** (the Marie session before it loaded predated the 11:30 restart). Two £0 steps:
1. **Fires-on-turn verify (Peter-natural).** After a real Marie **direct** Telegram turn (post the 6c.39 11:30 restart), inspect for the sub-agent firing + injected recall + its Haiku cost (`logging:true`). Look in `logs/gateway.log` / the plugin's own log path (locate it — `logging:true` emits timing/result; it was not writing to `logs/gateway/gateway.log` on first look). **No dedicated billed test** — a real Marie turn exercises it.
2. **Reindex** — `memory status` shows `Dirty: yes` (pa 39/48). Now that recall fires every Marie direct turn, a stale index degrades it → reindex (`openclaw memory …` — verify the verb, A-12) and consider a periodic reindex job.
**Only once recall is confirmed firing is the frozen-core slice (Stage 2) safe to design.**

## Stage 2 — the frozen-core slice: THE Marie bloat cure (headline, now UNBLOCKED)
The #1 durable Marie root ([P6-153], `PRE-COS-BACKLOG §A.4 HEADLINE`). active-memory (auto pre-turn recall) means hot-core Marie content moved to retrievable memory is no longer unreachable during her real turns → the aggressive frozen-core brief-slice becomes safe. Target: Marie 128K/3-compactions/$0.99-per-turn → toward Andreas's 44K/0/$0.025 (same model+cap). **Architectural — scope ONE properly:** identify the safe-to-move hot-core sections, move them to memory, verify recall surfaces them on a fresh Marie turn (a billed verify — agree first), measure before/after bloat + cost. Spec: `MASTER-ARCHITECTURE-v7.md`, `PRE-COS-BACKLOG §A.4`.

## Stage 3 — the digest-curation fix (strong contender; the [P6-167] incident's real gap)
The Andreas incident's proximate cause: **no agent writes a curated `digest-<date>.md`** (last 16 Jul) and Charlotte's raw digest lands **08:17 — after the 08:00 cron** → thin/empty morning briefings. Fix: a curation-before-08:00 habit (Marie/Charlotte brief step or a pre-cron hook writing `workspace-<id>/memory/digest-<date>.md`) + `cos-briefing.py` timing/fallback hardening (wait-for / most-recent fallback). Higher immediate user-value than the slice; the brief is already correct ([P6-167]) so agents *know* the right path — they just aren't populating it pre-cron. **Pick Stage 2 vs Stage 3 with Peter** — slice = deeper architectural cure; digest = immediate briefing quality.

## Stage 4 / carried
- **A2A live — light supervision** (first real multi-hop days): rate governor / per-pair+daily budgets / 6c.9-sawtooth caution. Kill lever ready.
- **Delta-only flush prompt ([P6-154]) — STILL unapplied** (config `memoryFlush.prompt` empty; 11:30 restart didn't apply it). `apply-6c37-memory-flush-prompt.sh apply` at next restart (dedup autoheal holds meanwhile; low urgency).
- **Open-loops watcher → escalate flip ([P6-156])** once Marie registers a real loop.
- **AGENTS.md middle-truncation** — bloat-conditional, low-urgency; folds into the frozen-core slice (leaner briefs → no truncation even when bloated). Note, don't act standalone.
- **Lower-priority:** `@fedchenkov` deliverability (Andreas, DNS propose-only); in-place `draft-update` verb ([P6-158]); restaurant/place screenshot store (Travel-gated ~early Aug, [P6-147]); Andreas avatar (optional); cost-model reconciliation awaiting Peter's Admin API key ([P6-148]).

## Safety rails
Reversible reads/host-authoring/`git commit` run without prompting. **Surface irreversible/outward:** openclaw.json edits (F17 staged, Peter via `!`) + gateway restart (GUI/D20); **any billed turn (agree cost FIRST)**; frozen-core slice apply + its billed verify; real external sends. **A2A is LIVE** — envelopes now dispatch billed turns; the trust flip is already made, but a *widening* of A2A scope (new sender, higher caps, `sessions_spawn` enable) is a fresh surface. `openclaw.json` jq/mv/cp + raw `launchctl` + `op` mutations + `rm` + `docker exec` DENY-listed → Peter via `!`. **F17 lesson ([P6-163]):** `openclaw config validate` has no `--file` form — validate LIVE after atomic swap, roll back on failure; verify object *structure* (`additionalProperties`), not just the key path. **A-12 ([P6-168] win):** verify CLI flags against the pinned build before scripting a never-run path — the `--deliver` bug was caught this way, pre-spend. **Secrets [F21/F51]:** never Read openclaw.json / `agent-creds/**` — `config get`/`shasum` only. No inline `#` in blocks (A-10).

## Carry-forward — status
1. **✔ 6c.39 (big):** A2A LIVE + validated end-to-end [P6-165/168]; active-memory applied the right way, memory-core preserved [P6-166]; Andreas stale-briefing incident fixed [P6-167]; `--deliver` live-dispatch bug caught pre-spend + fixed [P6-168]; truncation over-assertion self-corrected. openclaw.json one clean change; A-7 clean; A2A now LIVE.
2. **→ THIS SESSION:** Stage 1 (close out active-memory fires-on-turn + reindex — £0 quick gate) → **pick Stage 2 (frozen-core slice — the headline bloat cure, now unblocked) OR Stage 3 (digest-curation fix — the incident's real gap, immediate briefing value)** with Peter. Stage 0 must confirm A2A behaved overnight now that it's LIVE.
