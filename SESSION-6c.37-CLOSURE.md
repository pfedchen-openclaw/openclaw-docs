# SESSION 6c.37 — CLOSURE: Marie reliability mitigation batch SHIPPED (Stage-1, structural Andreas-independent harm-reducers) — memory-flush dedup + reset retune + status-SLA + micro-skills; A2A track (Stage 2) not started

_The bounded Stage-1 batch landed. All five items delivered or consciously dispositioned; the four durable roots stay Andreas-owned by design. **£0 — no billed agent turns** (host-side tools, dryrun/inspection verification only). **openclaw.json UNTOUCHED (`24715f4045bd`/10748B, A-7 clean).** Peter ratified the three genuine decisions (open-loops mode = escalate; flush-prompt root = stage; draft-delete = drop). Deviations: **P6-154..158 new (additive-in-place)**. Stages 2 (A2A emitter) and 3 (Andreas roots) untouched — carried._

## Done
- **Stage 0 GREEN.** A-7 clean (live==last-good==`24715f4045bd`/10748B/600/staff); carry-chain PASS (P6-1..153 gap-free at open); GUI/D20 (`SSH_CONNECTION` empty, `gui/502` reachable, user `openclaw`); version `2026.4.22 (00bd2cf)`; dist patch persists (`reply-media-paths.runtime-DegAVXTm.js`=`6dfbfe762078`/6847B; `BsZ-hV3v` 193B = known orphan); Marie briefs matched opener (HEARTBEAT `10327ed4e00d`, TOOLS `a41c0723f939`); A2A INERT (HALT present, `a2a-watch.py`=`96aa0247e52a`, plist staged-not-loaded, `sessions_send/spawn` unset).
- **★ S1.2 memory-flush dedup ([P6-154]) — root-caused + fixed, £0.** Root: the pre-compaction flush regenerates a *cumulative* recap; the append-only write tool turns it into byte-identical duplicate sections (the [P6-119] finding, never put on a cadence). Built `scripts/memory-dedup.py` (information-preserving, invariant-guarded, backup-first, idempotent), verified loss-free, **applied corpus-wide — 92 dup sections / ~180KB removed** (pa 8 files; pro clean). Cadence wired into `memory-health.py` (daily) + `chat-bloat-reset.sh` (intra-day). Root prevention (delta-only `memoryFlush.prompt`) **staged** (`apply-6c37-memory-flush-prompt.sh`, jq-validated) — Peter chose apply-at-next-restart.
- **★ S1.3 reset-net retune ([P6-155]) — shipped script-only (no restart).** `CHAT_RESET_TOKENS` 130K→**90K** (below the 120K cap → resets before the compaction-stick, not after) + a **free** graceful pre-restart heads-up (default on, recent-activity-gated, `CHAT_RESET_NOTIFY=0` kills it). Live on the next 900s tick. Dry-run verified (correctly SKIP-active on Marie's live session).
- **★ S1.1 status-SLA + open-loops ([P6-156]) — DESIGNED (Peter-ratified) + BUILT inert.** `open-loops.json` (compaction-proof register) + `open-loops-watch.sh` (reply-watcher pattern; forces a status when an active loop goes silent past SLA, independent of the agent's context state). New **`escalate`** mode = free heads-up to Peter + billed capped poke of Marie (Peter's ratified mode). Dryrun tests PASS (SLA gating / dedup / re-arm). Brief §62 wired. Staged plist pinned dryrun for safe-bootstrap; **Peter bootstraps + flips to escalate** after a clean shakedown. Design doc: `OPEN-LOOPS-STATUS-SLA-DESIGN.md`.
- **★ S1.5 micro-skills ([P6-157]).** Seeded `workspace-pa/SKILLS.md` (consult-before-repeat / append-on-success, 2 grounded entries) + §Skills brief pointer. Flagged: reliable auto-recall rides the Andreas retrieval activation.
- **S1.4 draft-cleanup ([P6-158]) — dispositioned per Peter: NO delete.** Never-delete floor kept; the real pain (stale drafts cluttering the Drafts folder) handled by reinforcing "one clean draft — replace in place, never pile a stale copy" (§Working-a-task). In-place `draft-update` noted as the clean deferred non-delete option. (Source `agent-creds/gmail_helper.py` is F21-guarded + needs a container recreate anyway — inherently Peter-gated.)

## State (SHA-anchored)
- **openclaw.json** `24715f4045bd`/10748B, 600/staff, live==last-good (A-7 clean). **Untouched this session.**
- **Dist patch (persists):** `reply-media-paths.runtime-DegAVXTm.js`=`6dfbfe762078`/6847B.
- **New (6c.37):** `scripts/memory-dedup.py` **`4f5494c5c27d`**/6273B · `scripts/open-loops-watch.sh` **`84715da28689`**/8116B · `scripts/ai.openclaw.open-loops-watch.plist.staged` **`286490b8ac14`**/2903B · `scripts/apply-6c37-memory-flush-prompt.sh` **`9433dce0d192`**/3344B · `workspace-pa/SKILLS.md` **`134883b27619`**/2865B · `workspace-pa/memory/open-loops.json` **`657c9cf769f5`**/1158B · `_session-docs/OPEN-LOOPS-STATUS-SLA-DESIGN.md` **`a2235039071a`**/3534B · `_backups-memory-dedup/` (corpus backups).
- **Changed (6c.37):** `scripts/memory-health.py` →**`969c99ea31e8`**/12671B (autoheal) · `scripts/chat-bloat-reset.sh` →**`3a7bd2f71c15`**/6744B (90K + notify + dedup hook) · `scripts/ai.openclaw.chat-bloat-reset.plist.staged` →**`7e650a528c71`**/1922B (comment) · `workspace-pa/AGENTS.md` 18456→**`2721b4a909ff`**/19588B (§Skills + §62 open-loops + §66 drafts) · `workspace-pa/memory/*.md` (8 files deduped, backed up).
- **Agents:** `pa`=Marie/opus-4-7, `pro`=Charlotte/sonnet-4-6, `cos`=Andreas/opus-4-7/paired/**A2A-OFF**. Gateway healthy.
- **A2A resting posture:** INERT (unchanged this session).

## Peter-gated to apply (handoff — GUI/D20, Peter runs via `!`)
1. **Bootstrap the open-loops watcher** ([P6-156]) — starts dryrun (£0, safe), then flip to `escalate`:
   ```
   cp scripts/ai.openclaw.open-loops-watch.plist.staged ~/Library/LaunchAgents/ai.openclaw.open-loops-watch.plist
   launchctl bootstrap gui/502 ~/Library/LaunchAgents/ai.openclaw.open-loops-watch.plist
   launchctl kickstart -k gui/502/ai.openclaw.open-loops-watch      # verify: tail logs/open-loops-watch.log
   ```
   Flip to escalate after a clean shakedown: set `OPENLOOPS_MODE=escalate` in the plist → bootout/bootstrap/kickstart.
2. **Delta-only flush prompt** ([P6-154]) — apply with the next gateway restart you do:
   `./scripts/apply-6c37-memory-flush-prompt.sh apply` then `openclaw gateway restart` (verify: after a real flush, `python3 scripts/memory-dedup.py --agent pa --day $(date +%F)` = 0 removable).

## Carried → 6c.38
1. **★ A2A Stage 2 — agent-side envelope emitter (NOT started 6c.37).** The host gates (a)+(b) are done + proven ([P6-152]); no agent emits an envelope yet. Wire the AGENTS.md/TOOLS.md convention (hub mints `conversation_id` on the human turn; drop a JSON envelope into `.a2a-outbox/pending/`; **read replies from `results/`, never re-send**) into Andreas (+ Marie/Charlotte spokes); dryrun over real traffic; THEN Peter-gated live flip (`A2A_MODE=live` + remove HALT + grant `sessions_send`, hard-gate `sessions_spawn`). Kill lever: `touch shared/a2a/HALT`.
2. **★ Andreas track proper (Stage 3) — the four durable Marie roots ([P6-153]):** aggressive frozen-core brief-slice (§A.4 HEADLINE — bloat/latency/stick cure, quantified: Marie 222K med / 63% trunc today); engine live-chat keep-alive + bounded worker-session isolation; production retrieval recall activation ([P6-118]/[P6-137]) — also the enabler for [P6-157] micro-skills recall; hard pulse send-gate ([P6-128]) + per-task model routing.
3. **Confirm the mitigations bit in production** (next session, inspection): open-loops watcher fired clean once live; a real flush post-restart leaves `dup×1`; the 90K reset fired with the graceful heads-up.
4. **Andreas avatar** (first male portrait) — optional reversible. **Admin API key** (Peter) → cost-model reconciliation ([P6-148]). **Charlotte handoff** memory-log fallback vs dedicated `digest-<date>.md`. **Travel agent** next after Andreas (~early Aug, [P6-147]).

## Discipline
Cost: **£0** — host-side tools + dryrun/inspection only; no billed agent turns, no Opus test-loops, no re-index ([[no-unauthorized-test-spend]]). No secrets read (structural `config get`/`shasum`/`docker inspect`; `openclaw.json` by sha only, never Read; `agent-creds/` diff correctly denied by the guard). openclaw.json untouched (A-7 byte-clean). Every change SHA-anchored + reversible; the corpus dedup was backup-first + invariant-guarded + verified loss-free before any live-file mutation. The three genuine decisions surfaced (AskUserQuestion), not assumed. Deviations additive-in-place (P6-154..158 new). Carry-chain audit PASS at open; re-run at close.
