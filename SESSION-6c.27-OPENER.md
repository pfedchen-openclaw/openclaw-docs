# SESSION 6c.27 — Reply-watcher dryrun→notify flip (Peter-gated) + 0b F26 confirm + continue reset-net / media observation; aggressive slice + dreaming remain ANDREAS-ONLY

_Opener · generated at 6c.26 close (2026-07-06). 6c.26 shipped: **reply-watcher ⑧ DRYRUN-activated + verified live** (poll under launchd, 2 real matches, £0 — [P6-126]); **0b exec-form fix** (root cause = a pa/pro TOOLS.md asymmetry; Marie's brief lacked the direct-`python3` note Charlotte already had — added to pa, [P6-126]); **0e cred-save log-spin fixed**; **0a compaction held for Andreas** (A-12: no `compaction.timeout` key exists); reset-net still armed-not-fired (good case); media-caps after-window opening (partial). openclaw.json **UNCHANGED** (`eb08cfa7d7b6`/10654B; A-7 clean). **Read [P6-126] first**, then this. GUI/Screen-Sharing (D20) for any bootstrap/restart/browser/2FA._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`eb08cfa7d7b6`/10654B**, 600/staff. Pin 2026.4.22 (00bd2cf). Sandboxes `capable-2026-06`. (openclaw.json is Bash-deny-listed — A-7 via applied-lever `config get`, never a direct file hash; config edits go via `scripts/apply-*.sh` under explicit permission or Peter runs them. NEVER weaken the F21 backstop.)
- **Deviations currency:** **P6-1…126 gap-free**, no dangling refs (run the [P6-101] carry-chain check at close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA.
- **★ Reset net live?** `scripts/heartbeat-watchdog.sh` = **`183896a33dd2`/20438B**, `scripts/session-freshstart.sh` = **`55020ef7acd6`/6637B**. Self-check: `WATCHDOG_DRYRUN=1 zsh scripts/heartbeat-watchdog.sh` → healthy `:main` emits no RESET. Benign residue: `logs/heartbeat-watchdog.reset-pa.state`=`0` (does NOT suppress a real reset — leave it).
- **★ Reply-watcher NOW LIVE in dryrun (changed 6c.26):** `scripts/reply-watcher.sh` = **`7763696a30f8`/5840B**; plist **now copied live** to `~/Library/LaunchAgents/ai.openclaw.reply-watcher.plist` (`d3ed81e12992`/1858B). Confirm `launchctl print gui/502/ai.openclaw.reply-watcher` = **LOADED, last exit 0, mode=dryrun** (env pins `REPLYWATCH_MODE=dryrun`). It polls every 900s and **logs matches only — zero spend**. **Four** LaunchAgents now active (heartbeat-watchdog, memory-health, session-freshstart, reply-watcher). **Do NOT run reply-watcher.sh from this ops session** — its poll reads `agent-creds/**` (F21/F51 classifier-block); it is launchd-only by design ([P6-104/908]).
- **★ Media caps + memory index** unchanged from 6c.26: `snapshotDefaults.mode=efficient`, image 1024 / pdf 15p / 8MB, `contextTokens=120000`, compaction `{safeguard, keepRecentTokens 40000, reserveTokensFloor 40000, sonnet-4-6, memoryFlush, truncateAfterCompaction}`, hybrid mmr+temporalDecay; `toolResultMaxChars` **unset**. Index pa 35·125, pro 31·36 (Dirty no, Dreaming off, recall pa 12·0). Freshstart LIVE 06:55.

## ⛔ FLAGGED FOR ANDREAS — do NOT attempt in interim sessions (Peter, 6c.23-followup)
Unchanged. Both are quality-impacting substrate changes reserved for the CoS; do not attempt even though reversible:
- **Aggressive frozen-core slice** (verb-tables + procedural blocks → retrieval-only, ≤30K). With recall=0, moving hot content out is a regression risk until a **billed fresh-session turn** proves organic search. `PRE-COS-BACKLOG §A.4 item 3`.
- **Dreaming ON** (nightly episodic→semantic promotion). `PRE-COS-BACKLOG §A.4 item 5`.
- **The billed retrieval-usage verify turn** is coupled to the slice → also Andreas's.
- **Compaction lever for the un-reset `:telegram:direct` session** — 6c.26 confirmed there is **no clean `compaction.timeout` knob** ([P6-126]); the real fix (bound the unbounded session) is §A.4 item 1/2. Peter chose HOLD. Do NOT stage a config lever for this in interim sessions unless Peter re-opens it.

## Stage 1 — the LEAD: reply-watcher notify-flip (Peter-gated) + 0b F26 confirm + continue observation
1. **Reply-watcher dryrun→notify flip — Peter's call, after observing the dryrun.** ⑧ is live in dryrun. First **read `logs/reply-watcher.log`** for the accumulated match-rate + noise since 6c.26 (baseline: 2 clean matches, no promo noise). Then surface to Peter: (a) is the **default scope** right, or tighten (label / sender allowlist)? (b) flip **`notify`** (free Telegram heads-up) now? Flip = edit `REPLYWATCH_MODE` → `notify` in `~/Library/LaunchAgents/ai.openclaw.reply-watcher.plist`, then `launchctl bootout` + re-`bootstrap` + `kickstart` (GUI-bound, D20). **`poke` (billed Opus, capped 4/day) only once scope is tight + Peter authorises** ([P6-51]). **Do NOT flip unilaterally** — notify starts real outbound to Peter.
2. **0b billed F26 confirm — Peter cost-OK first.** A single cheap **Sonnet** turn (target `--agent pro` or a Marie-Sonnet-equivalent; **NEVER Opus for tests**, [P6-51]) to confirm the pa TOOLS.md exec-form note took — i.e. Marie now invokes helpers as a direct `python3 /path/file.py` under the exec-preflight guard, and surfaces (never fabricates) if a tool is unavailable. Verify by inspection where possible; the billed turn is the F26 clincher.
3. **Reset-net production F26 (free inspection)** — once a genuine intra-day `:main` breach occurs, confirm the self-heal fired (`reset applied` in the log → archive → sentinel epoch → Peter notified → `:main` lean). **If still no real breach, say so and keep watching.** Do NOT synthesise one.
4. **Media-caps before/after ([P6-120] item-1)** — read `state/memory-health/daily.log` once the 07-06+ `mode:efficient` rows accrue (6c.26 saw the pre-caps session roll out: median −22% / p95 −17%, partial); recalibrate §8 off the real "after".

## Stage 2 — carried (secondary — do not let these crowd out Stage 1)
- **Battery** — **T3** (Marie/Opus) via **Peter's own Telegram** (faithful path; never-Opus-for-tests bars me firing it) or a future authorised Sonnet-equivalent; **T1** reliability re-test; **T4/T5** capability/payment wall (autonomy decision). T6 = PASS ([P6-123]).
- **Payment — Wikimedia £10 donation** teed up as a real payments-F26: Marie drives `donate.wikimedia.org` to a £10 payment page in her isolated profile, then surfaces for Peter's card+2FA. Billed browser-heavy Opus turn → fire on Peter's explicit go. Irreversible → prepare-and-surface, never auto-execute.
- **generate-mode credential store** — round out [P6-115] (only `existing` proven live).

## Closed at 6c.26 (do not re-carry)
Reply-watcher **built→dryrun-activated + verified live** ([P6-126]); 0b exec-form note **added to pa** (F26 confirm carried, not the edit); 0e log-spin **fixed**; 0a compaction **held for Andreas** (no timeout key). Battery T6 = PASS ([P6-123]).

## Safety rails
Reversible observation/reads + host-side builds run without prompting. **Aggressive core-slice + dreaming + the compaction lever stay ring-fenced to Andreas — do NOT attempt.** **Surface irreversible/destructive:** openclaw.json changes + gateway restart (Peter-run script or explicit per-message permission — F21 backstop categorical), version-pin/D1, real charges (incl. the Wikimedia donation), 1Password writes/deletes, depairing, reboots, `rm` (deny-listed → Peter/`trash`), **the reply-watcher notify/poke flip** (starts real outbound / billed turns — Peter's go), **and any billed turn (Peter's cost-OK first, [P6-51] — and NEVER Opus for tests; target `--agent pro`/Sonnet).** Do NOT synthesise a live `:main` breach to "test" the reset net. **Do NOT run reply-watcher.sh from this session / PATH-stub `openclaw` ([P6-124]) — the real CLI + agent-creds read fire; it is launchd-only.** **Secrets ([F21/F51]):** op host-only; display-immune checks; never read openclaw.json / agent-creds/** into context. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN. Remaining signals: T3/T1 re-tests, T4/T5 capability/payment decision, organic sample, reply-watcher notify utility, + the Andreas-owned/billed memory signals (organic recall>0, media after-window falling, dreaming-on + week-clock).
- **Pre-Andreas:** memory architecture de-risked (retrieval live, dedup done, levers+caps applied, observability wired, conservative restructure shipped, intra-day + overnight `:main` bounding both live); reply-watcher ⑧ **now live in dryrun** (notify-flip pending). Remainder = aggressive slice + verify turn + dreaming + week-clock + the `:telegram:direct` session bounding (`PRE-COS-BACKLOG §A.4`). Then v7 capstone reconciliation ([P6-76]) → 6d Andreas.
- **Andreas mandate:** first-principles review/refactor of faulty foundations (Peter, extends P6-49) — inherits the full v2 plan + working retrieval + a measuring canary + de-risked baseline + the aggressive slice teed up + a self-healing (overnight+intra-day) `:main` reset net + a live (dryrun) reply-watcher + the un-bounded `:telegram:direct` compaction problem.
