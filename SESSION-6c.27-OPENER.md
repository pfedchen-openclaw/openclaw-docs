# SESSION 6c.27 — Reply-watcher dryrun→notify flip (Peter-gated) + generate-mode live-proof (drain/Peter) + continue observation; aggressive slice + dreaming + brief-budget remain ANDREAS-ONLY

_Opener · generated at 6c.26 close (2026-07-06). 6c.26 shipped: **reply-watcher ⑧ DRYRUN-live + verified** (2 real matches, £0); **0b exec-form fix + F26 CONFIRMED PASS** (1 authorised Opus turn — real data, zero refusals, zero fabrication, approve-first judgement); **brief-injection-ceiling found + fixed** (pa's TOOLS.md was at the 15765-char ceiling; relocated browser verbs to reference-tools.md, now clip-free); **generate-mode inspection-verified end-to-end** (live-proof walled off from the ops session — op mutation is launchd-drain/Peter territory); **0e log-spin fixed**; **0a compaction held for Andreas** (no `compaction.timeout` key). openclaw.json **UNCHANGED** (`eb08cfa7d7b6`/10654B; A-7 clean). **Read [P6-126] (incl. its UPDATEs) first**, then this. GUI/Screen-Sharing (D20) for any bootstrap/restart/browser/2FA. **Cadence: ride the context window — don't close at every task boundary; full handoff only near context pressure or a real stop ([[ride-context-dont-close-early]]).**

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`eb08cfa7d7b6`/10654B**, 600/staff. Pin 2026.4.22 (00bd2cf). Sandboxes `capable-2026-06`. (openclaw.json is Bash-deny-listed — A-7 via applied-lever `config get`, never a direct hash; config edits via `scripts/apply-*.sh` under explicit permission or Peter runs them. NEVER weaken the F21 backstop.)
- **Deviations currency:** **P6-1…126 gap-free**, no dangling refs (run [P6-101] at close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA.
- **★ Reset net live?** `scripts/heartbeat-watchdog.sh` = **`183896a33dd2`/20438B**, `scripts/session-freshstart.sh` = **`55020ef7acd6`/6637B**. Self-check: `WATCHDOG_DRYRUN=1 zsh scripts/heartbeat-watchdog.sh` → healthy `:main` emits no RESET. Benign residue: `reset-pa.state`=`0` (leave it).
- **★ Reply-watcher LIVE in dryrun:** `scripts/reply-watcher.sh` = **`7763696a30f8`/5840B**; plist copied live to `~/Library/LaunchAgents/ai.openclaw.reply-watcher.plist` (`d3ed81e12992`/1858B). Confirm `launchctl print gui/502/ai.openclaw.reply-watcher` = **LOADED, last exit 0, mode=dryrun**. Polls 900s, logs matches only (£0). **Four** LaunchAgents now. **Do NOT run reply-watcher.sh from this session** — its poll reads `agent-creds/**` (F21/F51 block); launchd-only by design.
- **★ Marie brief at injection ceiling (new 6c.26):** `workspace-pa/TOOLS.md` = **`ab2eb956582b`/15924B** (15745 chars — **20 under** the effective 15765 clip ceiling; `bootstrapTotalMaxChars=110000`). `workspace-pa/memory/reference-tools.md` = **`b76028131e5a`/3894B** (now carries the browser state-changing verb syntax). **Any addition to pa's brief displaces tail content** — brief-budget is an Andreas item. (These live outside the docs git repo — verify by direct sha.)
- **★ Media caps + memory index** unchanged: `snapshotDefaults.mode=efficient`, image 1024 / pdf 15p / 8MB, `contextTokens=120000`, compaction `{safeguard, keepRecentTokens 40000, reserveTokensFloor 40000, sonnet-4-6, memoryFlush, truncateAfterCompaction}`; `toolResultMaxChars` **unset**. Index pa 35·125, pro 31·36 (Dirty no, Dreaming off, recall pa 12·0). Freshstart LIVE 06:55.

## ⛔ FLAGGED FOR ANDREAS — do NOT attempt in interim sessions
Quality-impacting substrate reserved for the CoS; do not attempt even though reversible:
- **Aggressive frozen-core slice** (verb-tables + procedural blocks → retrieval-only, ≤30K), + its **billed retrieval-usage verify turn**. `PRE-COS-BACKLOG §A.4 item 3`.
- **Dreaming ON** (nightly episodic→semantic promotion). `§A.4 item 5`.
- **Compaction / `:telegram:direct` bounding** — no clean `compaction.timeout` knob ([P6-126]); real fix is bounding the unbounded session. `§A.4 item 1/2`. Peter chose HOLD.
- **Brief-budget policy (new)** — pa's always-injected brief is at its 15765-char ceiling; deciding what earns always-injected vs retrieval is a CoS call. Do NOT prune pa's brief in interim sessions without Peter.

## Stage 1 — the LEAD: health-review remediation ([P6-128]) + reply-watcher notify-flip (Peter-gated) + generate-mode live-proof + continue observation
0. **★ [P6-128] health-review remediation.** 6c.26 review found Marie's **background pulse autonomously sent a private-clinic confirmation with a fabricated "Peter will bring private medical insurance + pre-auth" claim** (he has none), fired twice + calendar-inserted, no approval — an instance of a general failure (making up facts about Peter) + an autonomy breach. **FIXED (net-neutral brief edits, Peter-steered):** (i) general principle in `AGENTS.md` — "never assert a fact about Peter not in the known set; ask him to confirm, never guess"; (ii) soft autonomy gate in `HEARTBEAT.md` — "a beat drafts, never sends; even if TASKS.md says send/confirm." Insurance-specific edits rolled back; correction-send / duplicate-check / injected-insurance-guardrail **discarded per Peter**. **Still structural (Andreas):** (a) a **hard code-level send-gate** on background pulses (the HEARTBEAT rule is soft + the `gmail_helper` choke-point is deny-listed to the ops session); (b) the **un-reset-session tool-attachment root** — foreground Marie fabricates/disavows because pulses run her staged scripts out-of-band. Charlotte clean.
1. **Reply-watcher dryrun→notify flip — Peter's call, after observing the dryrun.** First **read `logs/reply-watcher.log`** for accumulated match-rate + noise since 6c.26 (baseline: 2 clean matches). Then surface: (a) is the default scope right or tighten (label / sender allowlist)? (b) flip **`notify`** now? Flip = edit `REPLYWATCH_MODE`→`notify` in the live plist, `bootout` + re-`bootstrap` + `kickstart` (GUI-bound). **`poke` (billed Opus, capped 4/day) only once scope is tight + Peter authorises.** Do NOT flip unilaterally — notify starts real outbound.
2. **generate-mode credential-store live-proof ([P6-115]) — via the sanctioned path, NOT direct op.** The chain is inspection-verified; only the live 1P write remains, and `op` mutation is **walled off from the ops session** (`op vault list` deny-listed; F21/F51). Do it through the **launchd drain** (write a generate-mode request → kickstart `cred-save-watch` → verify the result file display-immune → clean up the throwaway) **or hand the `op item create`+delete to Peter.** Never route around the op deny via the bridge script.
3. **Reset-net production F26 (free inspection)** — once a genuine intra-day `:main` breach occurs, confirm the self-heal fired (`reset applied` → archive → sentinel → Peter notified → lean `:main`). If still none, say so and keep watching. Do NOT synthesise one.
4. **Media-caps before/after** — read `state/memory-health/daily.log` as 07-06+ `mode:efficient` rows accrue (6c.26: pre-caps session rolled out, median −22%/p95 −17%, partial); recalibrate §8 off the real "after".

## Stage 2 — carried (secondary — do not let these crowd out Stage 1)
- **Battery** — **T3** (Marie/Opus) via **Peter's own Telegram** or an authorised turn; **T1** reliability; **T4/T5** capability/payment wall (autonomy decision). T6 = PASS ([P6-123]).
- **Payment — Wikimedia £10 donation** as a real payments-F26: Marie drives `donate.wikimedia.org` to a £10 page in her isolated profile, surfaces for Peter's card+2FA. Billed browser Opus turn → Peter's explicit go. Irreversible → prepare-and-surface.

## Closed at 6c.26 (do not re-carry)
Reply-watcher **dryrun-live + verified**; **0b F26 = PASS** + brief-ceiling fix ([P6-126]); 0e log-spin **fixed**; 0a compaction **held**; generate-mode **inspection-verified** (live-proof carried). Battery T6 = PASS ([P6-123]).

## Safety rails
Reversible observation/reads + host-side builds run without prompting. **Aggressive core-slice + dreaming + compaction lever + brief-budget pruning stay ring-fenced to Andreas.** **Surface irreversible/destructive:** openclaw.json changes + gateway restart (Peter-run or explicit permission — F21 backstop categorical), version-pin/D1, real charges (incl. Wikimedia), **1Password writes/deletes** (op mutation is deny-listed to this session — use the drain/Peter, never route around), depairing, reboots, `rm` (deny-listed → Peter/`trash`), **the reply-watcher notify/poke flip** (real outbound / billed), **and any billed turn (Peter's cost-OK first, [P6-51] — NEVER Opus for tests unless Peter explicitly waives, as he did once for 0b; target `--agent pro`/Sonnet).** Do NOT synthesise a `:main` breach. **Do NOT run reply-watcher.sh / PATH-stub `openclaw` / drive `op` mutations from this session.** **Secrets ([F21/F51]):** op read/mutation host-bridge-only; display-immune checks; never read openclaw.json / agent-creds/** into context. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN. Remaining: T3/T1 re-tests, T4/T5 capability/payment decision, organic sample, reply-watcher notify utility, + Andreas-owned/billed memory signals (organic recall>0, media after-window falling, dreaming-on + week-clock).
- **★ PATH TO 6d — corrected sequencing ([P6-76]; Peter flagged 6c.26 that the capstone had been mis-folded into "Andreas"):**
  1. **Close the acceptance gate** — T3/T1, T4/T5 capability/payment decision, organic sample (still OPEN).
  2. **v7 CAPSTONE / pre-6d consolidation — the distinct named milestone, NOT an Andreas task ([P6-76], orig roadmap row 6c.10).** Author **ARCHITECTURE v6→v7** + reconcile **STATE-OF-NATION / RUNBOOK / ARCHITECTURE** from the Phase-6 deltas + fold **S11 / S6 / [P6-49] steward-remit / SR-1·D23 doc-sync** + judgement doctrine + capability decisions + correspondence-pattern freeze + generate the clean 6d opener. **⚠ OVERDUE + STALE:** ARCHITECTURE is still **v6/Phase-0** (v7 never authored); STATE-OF-NATION + ARCHITECTURE last reconciled **2026-06-11** (~25 P6-codes ago); it has floated in the carries since the 6c.10 crisis displaced it (indicative 6c.16 → now 6c.27). Do NOT build Andreas against unreconciled docs — that is exactly what [P6-76] guards against.
  3. **6d Andreas** — builds on the proven pair + the reconciled v7 spec.
- **Pre-Andreas status:** memory architecture de-risked; reply-watcher ⑧ **live in dryrun**; 0b reliability loop **closed + F26-proven**. The substrate refactors (aggressive slice + verify turn, dreaming, week-clock, `:telegram:direct` bounding, brief-budget — `PRE-COS-BACKLOG §A.4`) are **Andreas's build mandate**, informed by the v7 spec — not pre-capstone work.
- **Andreas mandate:** first-principles review/refactor of faulty foundations (Peter, extends P6-49) — inherits the reconciled v7 spec + full v2 plan + working retrieval + measuring canary + de-risked baseline + aggressive slice teed up + self-healing `:main` reset net + a live (dryrun) reply-watcher + the `:telegram:direct` compaction problem + pa's brief-at-ceiling budget question.
