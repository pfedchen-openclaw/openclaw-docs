# SESSION 6c.9 — Pre-CoS deviations audit + comms-hygiene + reliability carries
_Opener · generated at 6c.8 close (2026-06-13). 6c.8 delivered the reliability sprint (model access, fallback, tiering, heartbeat watchdog, Charlotte, memory-ENOENT, digest proof — all five exit criteria met). 6c.9 is the **pre-CoS housekeeping** session ([P6-48]): cross-read **both** deviation logs into one prioritised "must-cover-before-Andreas" backlog, run the **comms-hygiene review** ([P6-50], now F26-able), and clear the **6c.8 reliability carries** ([P6-57]). Read-heavy/low-mutation by nature. Can run **during** the empirical week; only 6c.10 (canonical reconciliation + ARCHITECTURE v7) must follow it._

**Delivers:** one prioritised pre-Andreas backlog from both logs · Marie/Charlotte comms-hygiene fixes applied + F26-verified (no-substance-no-message, no DM signature, show-working-without-technical-detail, + any other absorbed-but-unapplied feedback) · Colima socket durability hardened · Marie's session compaction · spend-visibility design. **Exit =** consolidated backlog doc · comms fixes live + verified on a real beat · Colima socket-health-check live · Marie's 92 MB session compacted · spend-visibility lever decided.

## Stage 0 — Ledger + sanity (no mutating work until ALL-GREEN)
- Deviations log currency: confirm **P6-43 CLOSED**, **P6-46/47 resolved**, **P6-53…57 present** (log `<re-anchor at open>` — was `7e45ca1bb30d`/128074B at 6c.8 close). No staged rows pending lift.
- Sanity: `openclaw health` ok; `18789/healthz`→200; pa/pro interactive `claude-opus-4-7`, **heartbeat model sonnet-4-6** (`status --json .heartbeat.agents[]` → pa 30m, pro 4h, both enabled); both containers `capable-2026-06`; **A-7** live==last-good (`6554d79e955d`/9782B); op SERVICE_ACCOUNT; pin **2026.4.22** (2026.6.6 available — do NOT bump; run `browser-dep-repair.sh` after any bump).
- **Reliability check (the 6c.8 deliverables held?):** `tail logs/heartbeat-watchdog.log` → recent `ok last-beat Nm ago` (no unexpected restarts/alerts); watchdog LaunchAgent loaded (`launchctl print gui/502/ai.openclaw.heartbeat-watchdog`); **confirm natural (non-restart) beats fired through the empirical week** (grep `[heartbeat] started` cadence ~30m for pa); fallback still `["openai/gpt-5"]` ([P6-58]) + gpt-5 loud-flag clean (no spurious `FALLBACK-TO-OPENAI` alerts in `heartbeat-watchdog.log`). Browser posture: trenitalia ok / 192.168.1.1 blocked.

## Stage 1 — Pre-CoS deviations audit ([P6-48] #1, [P6-50])
- **Cross-read BOTH logs:** the frozen `DEVIATIONS-LOG.md` (~498KB, D/F/S/M/SR — Phase-0→~6a/6b) **and** `PHASE-6-DEVIATIONS-LOG.md` (P6, 6c). Extract every **still-open** item + standing constraint into one prioritised **"must-cover-before-Andreas"** backlog (new doc, e.g. `PRE-COS-BACKLOG.md`). Clear what's cheap; tag the rest must-fix / nice-to-have / defer-past-6d.
- **Known standing items to fold in:** SR-2 (ICS-before-calendar-write); correspondence review; [P6-45] payments guardrail (6d); [P6-35] host-egress; [P6-32] Docs/Sheets API writes; [P6-33] signature-PNG; [P6-36] workspace cruft; PENDING-BATCH-APPLY (P6-11/12/14); [P6-20] Charlotte avatar; [P6-29] scheduling; [P6-27/30] chat-hygiene; [P6-44] per-site recipes; [P6-52] Maps/reservations (capability session, not here). + **the empirical-week findings.**

## Stage 2 — Comms-hygiene review ([P6-50]) — now F26-able (cap resolved)
- **Read** Marie's & Charlotte's accumulated `workspace-p{a,o}/` `memory/*.md`, `MEMORY.md`, `{PA,PRO}-PLAYBOOK.md`, `AGENTS.md`, `SOUL.md`, `IDENTITY.md`, the voice guide — surface absorbed-but-unapplied feedback + behavioural drift.
- **Seed fixes (Peter-flagged, recurring):** (1) **no-substance-no-message** — honour `HEARTBEAT_OK`, stop over-messaging; (2) **no signature in Telegram DMs** to Peter; (3) **show-working-without-technical-detail**. Generalise beyond these three. Apply to the agent docs; **F26-verify** on a real beat (cheap Sonnet, single — [P6-51]). _(See Claude Code memory `agent-comms-preferences`.)_

## Stage 3 — Reliability carries from 6c.8 ([P6-57])
- **Colima socket durability.** `ai.openclaw.colima.plist` `KeepAlive=false`. The symptom was *socket drops* (`Cannot connect to the Docker daemon ×12`) which can happen without the process dying → build a **socket-health-check** (watchdog-style: probe `docker info`, restart Colima on failure) rather than only flipping KeepAlive. Apply in a window (a Colima bounce restarts the sandbox containers — gateway brings them back). Reuse the `heartbeat-watchdog` LaunchAgent pattern.
- **Session compaction.** Marie's `agents/pa/sessions/` is **92 MB** (50 MB trajectory on `14b920c0`). Compact/rotate (cost + [P6-46] wedge). Check `agents.defaults.compaction` + any `openclaw` session-compact command (A-12).
- **Spend visibility.** `openclaw gateway usage-cost` reads $0 (no embedded spend). Design a real daily readout (Console API or per-session token sum) or accept Console-only.
- **Dev/test key ([P6-51]).** If Peter has provisioned the dev/test workspace+key (1Password), wire the dev auth profile and route `openclaw agent`/Claude-Code verification turns to it. Else carry.
- _(Cross-provider fallback DONE in 6c.8 — `openai/gpt-5` universal backup, [P6-58]. Watch the empirical week for any real failover episodes + their loud-flag alerts.)_

## Stage 4 — Close (three deliverables)
- DEVIATIONS-LOG.md additive (new codes for findings; append resolutions). **SESSION-6c.9-CLOSURE.md.** **SESSION-6c.10-OPENER.md** = canonical reconciliation + ARCHITECTURE v7 (Andreas-ready spec, incl. [P6-49] CTO/architect remit) — the designed milestone, **after** the empirical week.
- Re-anchor shas; record live `openclaw.json` sha; commit `_session-docs/`.

## Safety rails (whole session)
Reversible build steps run without prompting; surface irreversible/destructive (real sends, payments, **version-pin/D1 changes, billing/cap/workspace changes, reboots**, depairing, destructive migrations) as decisions. `openclaw config set` is the gate-clear config path (jq/cp/stat on `openclaw.json` stay F21/F51-gated; `shasum`/`python3` reads OK, `wc`/`ls`/`grep`/`cp` on it are guard-blocked — use `python3 os.path.getsize` for bytes, `cp` for A-7 re-anchor works in a paired chmod/chgrp block). Gateway **restart + LaunchAgent bootstrap are SSH-safe while a console session is active** ([P6-56]); only the no-console/FileVault state rejects them (D20). **Cost discipline ([P6-51], hard rule):** billed `openclaw agent` turns spend Peter's money — default to inspection; any F26 turn = single, short, cheapest sufficient model (Sonnet/Haiku, **never Opus for tests**), tight timeout, no loops/batches; **explicit authorisation before any testing >~$5 or Opus/looping/browser-heavy/batched.** Stop at ~63% context for a clean close.

## Carry-forward (deferred, not lost)
Empirical-week findings (this session feeds them) · cross-provider fallback (Peter's call) · dev/test workspace split · [P6-44] per-site recipes / deep pre-payment proof · [P6-45] payments (6d) · SR-2 ICS-before-calendar · [P6-32] Docs/Sheets writes · [P6-33] signature-PNG · [P6-35] host-egress · [P6-36] workspace cruft · [P6-29] scheduling · PENDING-BATCH-APPLY (P6-11/12/14) · [P6-20] Charlotte avatar · [P6-52] Maps/reservations (capability session) · 2026.6.6 update (pinned — surface before bump).
