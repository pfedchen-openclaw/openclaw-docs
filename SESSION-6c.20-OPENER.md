# SESSION 6c.20 — Real-charge F26 for autonomous payments (Marie turn, plain merchant) + 07-04 bootstrap-held check
_Opener · generated at 6c.19 close (2026-07-03). 6c.19 built + host-validated Marie's autonomous capped-card payments (shared host browser, op-get both cards by UUID, `pay-fill.sh`, briefs graduated, international N26 card wired) — but **no real charge / no agent turn drove it**, so per F26 it is **not yet done** ([P6-99]). Touch ID closed N/A ([P6-100]). **openclaw.json CHANGED at the 6c.19→20 interstitial** — Charlotte→Sonnet + Marie kept on Opus for her Peter-facing work ([P6-104]/[P6-106]), now `da191cf1920d`/10099B (was `82a293d97aac`). Read the 6c.19 closure + `AUTONOMOUS-PAYMENTS-DESIGN-6c19.md` + [P6-99/100] first. **GUI/Screen-Sharing (D20)** for the browser + Peter's 2FA/OTP._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`da191cf1920d`/10099B**, 600/staff (interstitial: pro→Sonnet [P6-104]; pa kept on Opus [P6-106]). Pin 2026.4.22. Sandboxes `capable-2026-06`. (Read openclaw.json by structure/script — Bash deny blocks its name + `cat`/`jq`/`sha256sum`/`stat`; agent-creds token path is deny-guarded too.)
- **Deviations currency:** P6-107 present (log `49b376e27209`/282350B); sequence P6-1…107 gap-free + no dangling refs (carry-chain audit, [P6-101]). Run the [P6-101] carry-chain check at THIS close too.
- **★ 07-04 bootstrap-held check (the deferred proof):** `grep 'truncating in injected context' logs/gateway.err.log | grep 2026-07-04` should be **empty** for `pa:main`/`pro:main` on the morning-fresh sessions; confirm **Charlotte actually swept** the Soveren inbox on her first morning beat (function-level, not beat-liveness — [P6-98]). If truncation recurred, consider a further modest `bootstrapTotalMaxChars` raise or the optional slim.
- **Build artefacts present?** `scripts/pay-fill.sh` `93f5134cb383` · `scripts/op-list.sh` `e125c22e49b0` · `shared/finance-ledger.md` `05d08b5c7956` · PA-PLAYBOOK `baa7e657ce6c` · AGENTS `aa17dad9bf22`. Config repo `_session-docs` committed at 6c.19 close.
- **⚠ Interstitial re-anchor ([P6-102], 2026-07-03, pre-6c.20):** a focused Marie behaviour patch landed *before* this session — expected shas now **AGENTS `a98199d0de6d`/15821B · PA-PLAYBOOK `7315954bdc47`/21205B · SOUL `3c37d4515bdb`/6382B · DECISIONS `dc3d67129b1b`/11500B · MEMORY `0c21747eadae`/8791B** (workspace-pa repo); deviations log **`67457868a114`/265604B, sequence P6-1…102** gap-free/no dangling refs. Not drift — see [P6-102]. _(No live gap: the 4 Arundel-rent reminders + La Traviata ARE on the calendar — batch-created via `create_events.sh`, 5 real event ids, Peter confirmed; my initial "never created" reading was an operator scan false-positive, corrected in [P6-102].)_
- **⚠ Interstitial re-anchor 2 ([P6-103/104/105], 2026-07-03, pre-6c.20):** a second, larger Marie/Charlotte patch landed after [P6-102] — behavioural (mirror rule, working-state legibility, append-only memory), email multipart+signature, media-delivery correction (route via the **`browser`-tool screenshot**, not hand-typed `MEDIA:/workspace/…`), **model-routing** (Charlotte→Sonnet; Marie **kept on Opus** for her Peter-facing work per Peter's [P6-106] pick — Sonnet only for her autonomous pulses; gateway restarted), and the **cost-notify** watcher (LaunchAgent `ai.openclaw.cost-notify`, live). Expected shas now — workspace-pa **AGENTS `3b967ca6f983`/17406B · PA-PLAYBOOK `45022cfe7876`/23137B · MEMORY `4c9f06fc13bb`/10219B · DECISIONS `5f20893ac60e`/13859B · SOUL `3c37d4515bdb`/6382B**; workspace-pro propagation commit `9355160`; openclaw.json `da191cf1920d`/10099B; deviations log `656935fcd633`/279944B (P6-1…106). Not drift — see [P6-104]/[P6-105]/[P6-106]. **Open threads for 6c.20:** (a) **Marie's model — RESOLVED** (Peter chose Opus for her Peter-facing work, [P6-106]); its cost is funded by **context-reduction levers** (`contextLimits.toolResultMaxChars`, `imageMaxDimensionPx`, `params.cacheRetention:"long"`, modest bootstrap trim — all verified available, **apply as a post-test tuning pass** so tonight's run isn't destabilised). (b) **reply-watcher activation (⑧)** — mechanism proven (`gog gmail search` + `openclaw agent … --deliver`) but **blocked on dead host gog Gmail OAuth** (`invalid_grant`, both accounts); needs Peter's decision: re-auth gog (interactive Google consent) OR a host poller reusing the agents' live cred. (c) **Marie awaiting Peter:** Thomas German cancellation (approve the drafted email) + Preply Jim reschedule (needs Peter's **Preply password** — not yet in 1P) + pay Thomas £45 (needs a channel).

## Stage 1 — the work (GUI, Peter-paced)
1. **★ LEAD — the real-charge F26 for autonomous payments.** Needs a **Peter-directed real purchase** (payee + amount — the payment principle; never invent one). On a **plain-input merchant** (NOT Stripe cross-origin iframes + hCaptcha — Claude Platform is the hard case, deferred). Run it as a **real Marie agent turn via the gateway/Telegram** ([embedded-cli] — NOT `openclaw agent` embedded CLI): Marie drives login+checkout → snapshots the card-field refs → surfaces "ready to pay" → **host** `pay-fill.sh --card uk|intl <refs>` → Peter **approves + 2FA** (in-app approve, or relays an OTP Marie inputs) → append `shared/finance-ledger.md`. Cost: cheapest model, ~$5 auth already given ([P6-51]) — cheap turns only, no loops. **This is what marks the capability done (F26)**; until then the brief flags it pending.
2. **Autonomous *login* via host-filled creds** (non-SSO sites) — designed, not built. Build + test the host filling a stored 1P credential (op-get) into a login form (session persists in the `openclaw` profile), Marie never handling it. Distinct from card-inject.
3. **Stripe/iframe merchants** — decide the approach for the common hard case: keystroke `type` into the Stripe iframe fields (snapshot `--frame`) vs. semi-manual card entry on such sites. Test the `type` path pre-submit (no charge) to see if it survives hCaptcha/Radar on the warmed residential profile.

4. **Carried from 6c.19 (deferred by the Peter-directed payments pivot — NOT dropped):**
   - **Battery re-run** (the *judgement class* of the acceptance gate) — briefs now inject fully post-raise, so re-run and convert **T3 / T6 / digest FAIL→PASS** through the **real Telegram** path ([embedded-cli], NOT the embedded CLI). Cheapest model (Sonnet/Haiku, **never Opus**), **~$5 hard cap**, stop-and-surface if trending over ([P6-51]).
   - **T1 reliability** — confirm the **rental-agreement reminder fired 19:00 Fri 3-Jul** with proof-of-done (now in the past — check the beat logs / the artefact first).
   - **Optional slim** (not urgent) — **mirror the [P6-98] batch-archive line into pa TOOLS.md** (pro already has it); optionally trim early/large brief files to cut per-beat injected tokens; Peter reviews the diff. Skip if the F26 + verifications fill the session.

## Stage 5 — Verify (F26) + Stage 6 — Close
Real charge completed through a Marie gateway turn on a plain merchant, with Peter's 2FA, logged to Finance. Close: deviations additive ([P6-101]…); `SESSION-6c.20-CLOSURE.md`; `SESSION-6c.21-OPENER.md`. Re-anchor shas; refresh sanitised snapshot only if config changed (none expected); commit `_session-docs` + config repo.

## Safety rails (whole session)
Reversible build runs without prompting. **Surface irreversible/destructive:** the real charge (payee+amount is Peter's; he does 2FA), credential first-login, real sends beyond the gated flow, OAuth/Workspace/GCP/billing, version-pin/D1, 1Password writes/deletes, depairing, reboots. **Never change a merchant's default payment method** without explicit OK (Claude Platform lesson). **Cost ([P6-51]):** inspection/host-drive first; agent turns only for what needs the agent (nav/snapshot/the real-path F26), cheapest model, no loops/Opus/batches. **Secrets ([F21/F51]):** op host-only; card value off-argv + out of sandbox; display-immune checks only. GUI/Aqua for browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN — the payments capability class is unblocked in mechanism but not F26-proven; decidable once the real charge lands + T1/judgement classes hold.
- **Pre-Andreas milestone — v7 capstone / reconciliation** ([P6-76], roadmap §6c.10): reconcile STATE-OF-NATION / RUNBOOK / ARCHITECTURE from the P6-77…P6-100 deltas + crystallise lessons into PHASE-0-LESSONS / TIER-1-KEY-PATTERNS — due **after** the acceptance gate PASSES, immediately before the 6d Andreas opener. _(Was silently dropped from the 6c.19/6c.20 carry — re-instated 2026-07-03 in the carry-chain audit. The stale `AGENTS.md:16` "Drive READ-ONLY" line it used to track is already fixed — [P6-83].)_
- **Consciously parked** (were silently dropped at the 6c.16→6c.17 transition — dispositioned here, not re-lost): **[P6-78] full Telegram back-scroll** = a Telegram Bot-API limitation (no history backfill), **not buildable** → parked unless the platform changes; **[P6-79] per-turn tool/token *ceiling*** = no config knob in 2026.4.22, **handled by the heartbeat-watchdog cost/context canary** as the detective stand-in → parked, revisit at model/framework-currency review ([P6-74]).
- **Andreas backlog:** function-level agent-health check ([P6-98]); `:telegram:direct` compaction-pressure; org-owned OAuth app (P6-90/95); per-task cost tracker (**v1 shipped [P6-105]**; **real billed $ wired [P6-107]** — activates when Peter drops an Anthropic Admin API key in env/`agent-creds/anthropic-admin.key`; relocate `cost-notify.py` into the platform); **revert Charlotte→Opus** at her v2 operational activation, i.e. external drafting/sending in Peter's voice ([P6-107]); **workspace hygiene** — purge old/temp files (screenshots, captcha b64 dumps, probe scripts) on a keep-docs/memory/trackers/ledger policy, dry-run manifest for Peter before any delete ([P6-106]#2); **context tuning pass** — apply the verified [P6-106] context levers + measure ([P6-85]); context-wipe experiment quality-watch ([P6-95]#3); autonomous-login build; Stripe-merchant fill strategy; model/framework currency ([P6-74]); DMARC tighten. Travel POST-6d. 2026.4.22 pinned.
