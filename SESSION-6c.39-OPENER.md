# SESSION 6c.39 — A2A dryrun-ENABLE (flip the "off" posture → watcher-up-dryrun → verify Andreas emits) → live; active-memory correct-approach; then the Andreas track

_6c.38 was a big session. **A2A convention: authored + hardened + institutionalised + proven over REAL launchd** ([P6-160/161], 26/26 tests; hop_spoof refined so honest omit isn't flagged). The **real-agent dryrun ran** (one Andreas turn, ~£0.30) and did its job twice: it surfaced (a) a **sandbox-path bug** — the recipe used host paths not the agent's `/workspace/…` — now **FIXED** ([P6-164]); and (b) that **Andreas correctly won't emit while his brief says "A2A is off"** (no-fake-done + accurate posture — the system working right). **active-memory: TRIED + REVERTED** — the `plugins.slots.memory` swap is exclusive and evicts memory-core (breaks `memory_search`); reverted cleanly, Marie whole, correct approach identified ([P6-163]). **openclaw.json back to `24715f4045bd`/10748B (A-7 clean); A2A still INERT.** Deviations: P6-160..164._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live==last-good==**`24715f4045bd`/10748B**, 600/staff (active-memory reverted → back to pre-6c.38-B baseline). Pin 2026.4.22 (`00bd2cf`). `cos`=Andreas/opus-4-7/**A2A-OFF**, `pa`=Marie/opus-4-7, `pro`=Charlotte/sonnet-4-6. `memory-core` is the memory-slot owner (default); `memory status` healthy (pa 39/47·314 chunks).
- **★ Dist patch persists:** `reply-media-paths.runtime-DegAVXTm.js`==**`6dfbfe762078`**/6847B (the `BsZ-hV3v` 193B chunk = known orphan). Re-apply `apply-6c34-screenshot-media-fix.sh` + restart only if `DegAVXTm` reverts.
- **Deviations currency:** P6-1..164 gap-free — re-run `scripts/carry-chain-audit.py` at open AND close.
- **Context/GUI ([P6-117]/D20):** confirm `SSH_CONNECTION` empty + `gui/502` before any restart/billed turn. Billed test = agree the cost envelope FIRST ([[no-unauthorized-test-spend]]); one Andreas turn ≈ £0.30.
- **★ A2A resting posture INERT.** `shared/a2a/HALT` PRESENT; plist staged-not-loaded; `a2a-watch.py`==**`ec873a68177e`** (hop_spoof refine [P6-161]); `a2a-watch-test.py`==**`3308e2b09569`** (26 checks); `sessions_send`/`sessions_spawn` OFF; real ledger 5 lines (3 dryrun-smoke + 2 convtest `would-allow`).
- **★ 6c.38 emitter convention (path-FIXED to `/workspace/.a2a-outbox/`):** `COS-PLAYBOOK.md`==**`d0a8612266b8`**, `cos/TOOLS.md`==**`805ee7035bb3`**, `pa/PA-PLAYBOOK.md`==**`7e9b55d3464f`**, `pro/AGENTS.md`==**`af12ec191c0a`**, `pa/AGENTS.md`==`226681c1420f` (spoke guardrail, no path). Host-side dryrun script `apply-6c38-a2a-convention-dryrun.sh`==`dc0edebe71c5`. Convention is a permanent regression test (TEST 6).
- **6c.37 mitigations (still live):** `chat-bloat-reset.sh`==`e4a6bd9a4b40`, `memory-dedup.py`==`4f5494c5c27d`, `open-loops-watch.sh`==`84715da28689` (LIVE dryrun), `memory-health.py`==`969c99ea31e8`. `open-loops.json` register `[]` unless Marie populated one.

## Stage 1 — A2A dryrun-ENABLE → verify Andreas emits → live (THE immediate step; mostly ready)
The 6c.38 dryrun proved the convention host-side but revealed **a well-behaved agent won't emit while its brief says A2A is off** — and its precondition ("mailbox + L1–L5 built + proven in code") **is now MET** ([P6-152] gates + the convention, 26/26 + launchd proof). So enabling A2A is legitimate. Sequence (a real trust/posture change — SURFACE at the top; then reversible):
1. **Flip the brief posture (Claude, reversible).** In `COS-PLAYBOOK §S11` (+ Marie/Charlotte spoke sections) change "A2A stays OFF until built/proven" → **"A2A is ACTIVE (host-metered dryrun→live); use the emitter for genuine specialist asks."** Keep the caps + kill-lever language. Back up first.
2. **Bring up the watcher in DRYRUN + remove HALT (Peter via `!`, GUI/D20).** `cp` staged plist → LaunchAgents; `launchctl bootstrap gui/502 …` (leave `A2A_MODE=dryrun`); `rm shared/a2a/HALT`. Dryrun = validate + budget + **ledger, NEVER dispatch → £0, no Marie turn.**
3. **Drive one real ask (billed, ~£0.30).** Peter messages **Andreas on Telegram** ("get Marie to check if I'm free Fri PM") — his interactive turn loads the fixed COS-PLAYBOOK. **Verify (Claude, £0):** an envelope lands at `workspace-cos/.a2a-outbox/pending/` (host view) well-formed per the convention (from:cos, to:pa, conversation_id, origin, task_ref, message, **no** hop/path), and `ledger.jsonl` logs a `would-allow`.
4. **THEN live (Peter-gated).** Flip plist `A2A_MODE=live` (envelopes now dispatch → billed Marie turns). Kill lever unchanged: `touch shared/a2a/HALT`. `sessions_send` grant is NOT needed (file-based emit; host dispatches via `openclaw agent --deliver` — [P6-163]).

## Stage 2 — active-memory the RIGHT way (the frozen-core bloat-cure enabler) — offline first
6c.38 proved the SLOT approach is wrong: `plugins.slots.memory="active-memory"` is **exclusive** → evicts memory-core → kills `memory_search`/index/CLI ([P6-163]). **Correct approach (verify £0 before any apply):** enable the plugin as an **entry with `.config` but do NOT set the memory slot** — `plugins.entries.active-memory = {enabled:true, config:{enabled:true, agents:["pa"], model:"anthropic/claude-haiku-4-5", allowedChatTypes:["direct"], thinking:"off", logging:true}}`, leaving memory-core the slot owner. **Open question to resolve first (dist-read):** does active-memory run its pre-reply injection sub-agent as a **hook** without owning the memory slot, or does it REQUIRE the slot (in which case coexistence isn't possible and the bloat-cure needs a different route)? If it works as a non-slot hook → scoped Marie/Haiku test → observe recall fires + cost + `memory_search` intact → THEN the aggressive frozen-core slice becomes safe. Staged script `apply-6c38-active-memory-scoped.sh`==`90578cb0c4c5` currently sets BOTH slot+entry — **must be edited to drop the slot line** before re-use.

## Stage 3 — the Andreas track proper (deep Marie roots, if A2A + active-memory are parked)
**The four durable Marie roots ([P6-153]):** aggressive frozen-core slice (gated on active-memory-done, above); engine live-chat keep-alive + bounded worker-session isolation ([P6-149]); production retrieval recall ([P6-118]/[P6-137]); hard pulse send-gate ([P6-128]) + per-task model routing ([P6-28]/[P6-144]). Spec: `MASTER-ARCHITECTURE-v7.md` + `PRE-COS-BACKLOG §A.4`. Architectural — scope one properly.

## Stage 4 / carried
- **Mitigations still-settling (inspection):** open-loops watcher fires clean once Marie registers a loop → then flip to **escalate** ([P6-156]); a real flush post-restart leaves `dup×1` once flush-prompt applied.
- **Peter-gated applies pending:** delta-only flush prompt ([P6-154], next restart); open-loops escalate flip.
- **Lower-priority:** `@fedchenkov` deliverability (Andreas, DNS propose-only); in-place `draft-update` verb ([P6-158]); restaurant/place screenshot store (Travel-gated ~early Aug, [P6-147]); Andreas avatar; cost-model reconciliation awaiting Admin API key ([P6-148]); Charlotte `digest-<date>.md`.

## Safety rails
Reversible reads/host-authoring/`git commit` run without prompting. **Surface irreversible/outward:** openclaw.json edits (F17 staged, Peter via `!`) + gateway restart (GUI/D20); **any billed turn (agree cost FIRST)**; **removing `shared/a2a/HALT`** + **flipping the A2A brief posture to "active"** + **A2A live** (real trust/posture changes); real external sends; open-loops escalate. `openclaw.json` jq/mv/cp + raw `launchctl` + `op` mutations + `rm` + `docker exec` DENY-listed → Peter via `!` (6c.38: `rm -rf` and `docker exec` both correctly refused). **F17 lesson ([P6-163]):** `openclaw config validate` has **no `--file` form** — validate the LIVE config after an atomic swap, roll back on failure; and verify the object *structure* (`additionalProperties`), not just the key path. **Secrets [F21/F51]:** never Read openclaw.json / `agent-creds/**` — `config get`/`jq`-on-keys + `shasum` only (avoid even naming `openclaw.json.*` files — the guard denies it). Keep config checks on their own line; no inline `#` in blocks (A-10).

## Carry-forward — status
1. **✔ 6c.38 (big):** A2A convention authored + hardened + institutionalised + launchd-proven [P6-160/161]; real-agent dryrun surfaced+fixed the sandbox-path bug [P6-164] + revealed the brief-posture gate; active-memory tried+reverted, correct approach found [P6-163]; active-memory mechanism corrected [P6-162]. Marie whole; A-7 clean; A2A INERT.
2. **→ THIS SESSION (pick, ask Peter):** (a) **A2A dryrun-enable → live** (Stage 1 — closest to done, the brief-posture flip is the key move); (b) **active-memory the right way** (Stage 2 — entry-hook-not-slot, offline verify first); (c) the **Andreas track** (Stage 3 roots). Stage 1 is the natural continuation.
