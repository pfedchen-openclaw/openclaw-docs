# SESSION 6c.16 — Overnight first-test scheduled + acceptance gate authored, locked & opened — CLOSURE
_2026-07-02. **GUI/Screen-Sharing (Aqua; D20 satisfied).** Two deliverables: (1) the 6c.15-amber overnight automation test could not be *read* yet — it ran the same evening, before the 23:00 trigger — so it was **scheduled** via a self-removing one-shot morning verifier that pushes a verdict to Peter's Telegram; (2) Peter chose the **acceptance gate** as the Stage-1 lead — authored, decisions locked (hybrid/strict-100%/authorised), scorecard built, window opened. New code **[P6-91]**. `openclaw.json` **UNCHANGED** (`8a6e2a19d9d5`/10096B, A-7 clean). **Zero billed agent turns** — all inspection, scripts, and doc-authoring._

## Stage 0 — GREEN (one item not-yet-due, correctly)
- **A-7 clean:** live==last-good==**`8a6e2a19d9d5`/10096B**, 600/staff; pin **2026.4.22**; both sandboxes `capable-2026-06`. Deviations current (P6-90 @657; log was `8b7e51c124dd`). 1Password SA `openclaw-agents` (`6LYBZQU6…`) green.
- **WS-4 re-anchored cost-free:** cred `43353284e50f`/416B; `gmail_helper.py` `5bc5d7b73a4e` (both mirrors); `SEND_IDENTITIES={marie@fedchenkov.com}` floor intact (guard @156); pa mounts `/creds/marie-send.json`, pro doesn't; docs `8fa873cd295c`/`a6aba8f23e51`.
- **★ Overnight first-test = TEMPORALLY NOT-YET-DUE (not a regression).** It's **20:28 the same evening** as the 6c.15 close; the 23:00 NIGHT trigger was ~2.5h out. The opener assumed a morning-after run. Everything armed for tonight — both LaunchAgents loaded (exit 0), schedules correct, and `pmset` shows the Mini won't sleep (`sleep 0`/`standby 0`) so launchd fires on time.

## What shipped — [P6-91] (full detail in the deviations log)
- **Morning verifier (Peter chose "schedule a re-check").** `scripts/overnight-verify.sh` (`57f9883a654c`/5767B, exec) + `ai.openclaw.overnight-verify.plist` (`6c3d7bf6f816`/1304B) — bootstrapped `gui/502`, fires **07:15 03-Jul** (schedule confirmed Hour=7/Min=15/watching=1). It inspects the night-of-02→03 evidence (nightwindow 23:00-suppress + 07:00-restore non-DRYRUN; `heartbeat.night` cleared; `resumed-at` mtime ~07:00; freshstart 06:5x RESET pa+pro; morning archives), writes PASS/PARTIAL/FAIL to `logs/overnight-verify.log`, **pushes the verdict to Peter's Telegram** via the watchdog's `openclaw message send` (a direct bot send — **not** a billed turn), then **self-removes** (rm plist → bootout). Durable, cost-free, no reliance on any live Claude session. Verified `zsh -n` + `VERIFY_DRYRUN=1` (6 checks parse, summary formats, send/self-destruct neutralised, exit 0).
- **Acceptance gate [P6-76] authored + opened.** `ACCEPTANCE-GATE.md` (`47d045b44da3`/9166B): the bar (end-state — done+verified or a precise block — zero fake-dones, zero discretion breaches), four doctrine-mapped scored dimensions (owns-outcome / verifies-done / resourceful / no-surprises), two cardinal auto-fails, a six-task capability-covering battery (T1 durable-task, T2 email-as-marie@, T3 research-from-own-sources, T4 book-to-pre-pay, T5 account+1Password, T6 Charlotte triage) + organic tasks; F26-faithful; **scoring by inspection = zero marginal cost**. **Peter's locked decisions:** HYBRID window · STRICT-100% bar · battery AUTHORISED (~$5 cap). `ACCEPTANCE-GATE-SCORECARD.md` (`8f4426e9585c`/4201B) — copy-paste seed prompts + score rows — **window is OPEN**, Peter-paced over ~5–7 days.

## End-state (anchors)
- **`openclaw.json`: `8a6e2a19d9d5`/10096B** — A-7 clean, live==last-good, 600/staff. Pin 2026.4.22; sandboxes `capable-2026-06`. **No config mutation this session.**
- **New host files:** `scripts/overnight-verify.sh` `57f9883a654c`/5767B (exec); `~/Library/LaunchAgents/ai.openclaw.overnight-verify.plist` `6c3d7bf6f816`/1304B (loaded gui/502, one-shot 07:15).
- **New docs:** `_session-docs/ACCEPTANCE-GATE.md` `47d045b44da3`/9166B; `_session-docs/ACCEPTANCE-GATE-SCORECARD.md` `8f4426e9585c`/4201B. **Deviations:** [P6-91] appended additive → log now **`78a6053c71d7`**/224568B.

## Carried → 6c.17 (deliberately, not lost)
- **Read the overnight verdict:** at open, read `logs/overnight-verify.log` + confirm the 07:15 Telegram verdict reached Peter and the one-shot self-removed (`launchctl list | grep overnight-verify` should be empty; plist gone). If it FAILED/PARTIAL, diagnose (bootstrap/sleep-wake/script) and fix.
- **Advance the acceptance-gate scorecard** from whatever tasks Peter fired; remediate any `~`/`F` and re-run (strict-100%).
- **v7 capstone / reconciliation** — the other pre-Andreas milestone (not led this session); fold P6-77…P6-91 into the snapshot docs; fix the stale `AGENTS.md:16` Drive-read-only line.
- Standing: Andreas-owned org OAuth app (PRE-COS-BACKLOG); DMARC tighten; Travel post-6d; 2026.4.22 pinned; model/framework currency → Andreas.

## Honest note
The headline Stage-0 gate turned out to be un-runnable this session — not through any fault, but because the session opened the same evening the automation was armed, hours before its first 23:00 fire. The wrong move would have been to declare it verified (it wasn't) or to punt it to the next opener again (the same defer that produced this carry). Instead I built the smallest durable thing that closes the loop without a human in it: a one-shot job that reads the evidence at 07:15, tells Peter the result on Telegram through the already-proven non-billed send channel, and deletes itself. On the gate, Peter picked the strict bar — which is the right instinct for a pair whose defining failure was *claiming* done; a 100% bar with fake-done as an auto-fail makes "done" mean done. The one deliberately-unfinished part is honest: the empirical window is Peter's real use, so it can't be completed by me in an evening — it's now *open and instrumented*, not deferred.

---
_Attachments for 6c.17: this closure + `SESSION-6c.17-OPENER.md` + `PHASE-6-DEVIATIONS-LOG.md` (through P6-91) + `ACCEPTANCE-GATE.md` + `ACCEPTANCE-GATE-SCORECARD.md` + `JUDGEMENT-DOCTRINE.md`._
