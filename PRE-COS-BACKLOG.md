# PRE-COS-BACKLOG — must-cover-before-Andreas
_Generated 6c.9 (2026-06-15). The single prioritised backlog of everything still-open or standing across **both** deviation logs — `DEVIATIONS-LOG.md` (frozen Phase-0→6a/6b, D/F/S/M/SR) and `PHASE-6-DEVIATIONS-LOG.md` (P6, sha `0127c93e9ba1`/139569B at this session's open) — plus the 6c.8→6c.9 empirical-week findings. Purpose: nothing load-bearing falls through the cracks when we author ARCHITECTURE v7 (6c.10) and build **Andreas** the Chief-of-Staff (6d)._

**How to read this.** Three lenses, kept separate on purpose:
- **§A Gating items** — must be DONE or DECIDED before the named milestone (6c.10 spec-authoring or 6d build-start). These are the real blockers.
- **§B Standing constraints** — invariants Andreas must *inherit and respect* (encode in his AGENTS.md/TOOLS.md/guardrails). Not "work", but a wrong call here is costly.
- **§C 6d build-content** — items that *are* the CoS's job (he does them once he exists); design in v7, build in 6d. Listed so we don't mistake them for blockers.
- **§D Maintenance cadences** (M-codes) — Andreas tracks, Peter executes.
- **§E Defer-past-6d** — explicitly out of scope until a real consumer exists.

Tag legend: **[MUST]** before 6d · **[NICE]** opportunistic/cheap · **[DEFER]** past 6d.

---

## ★ 6c.31 MILESTONE RECONCILIATION (v7-capstone boundary — [P6-101] re-fold)
_Additive overlay. The §A–§E lenses below are the 6c.9 record (preserved). This block reconciles their status to 6c.31 close and folds the still-open set forward into a single 6d-ready list. The v7 capstone landed this session, so all the "gate is 6c.10 / author-v7" items are now **CLEARED**._

**CLEARED at 6c.31 (were §A gating blockers):**
- **P6-48 — ARCHITECTURE v7 authored** ✔ (`MASTER-ARCHITECTURE-v7.md`, supersedes v6 additively, all Phase-6 subsystems P6-anchored).
- **S11 — inter-agent loop-protection DESIGNED** ✔ (v7 PART 9 §S11 — 5-layer envelope/budgets/circuit-breaker/kill-switch/observability; numeric budgets surfaced as tunable defaults). Build = 6d, before A2A is enabled.
- **SR-1 / D23 — doc-autopilot DESIGNED** ✔ (v7 PART 10 — scripted-audit vs judgement split; commit-at-close, push surfaced). Publish-target decision folded to 6d.
- **P6-49 — Andreas engineering-steward remit formalised** ✔ (v7 PART 11 + `BRIEF-DERIVATION-ANDREAS.md`).
- **P6-26 — Sophie + Andreas briefs DERIVED from the CURRENT Marie/Charlotte pair** ✔ (`BRIEF-DERIVATION-SOPHIE.md` / `BRIEF-DERIVATION-ANDREAS.md`). Discharge-at-build, rule honoured.
- **S6 — schema-vs-live reconcile** — PARTIAL: v7 records the known 2026.4.22 obsolescence (no `params.cacheRetention`, no `compaction.timeout` key, `snapshotDefaults.mode` binary-not-enum, per-agent `contextTokens` absent) and does NOT re-embed a stale config template. Residual full-sweep → 6d before authoring Andreas's config.
- **Memory/Context Architecture v2 (§A.4 headline)** — SHIPPED + gate **PASS** (recalibrated, [P6-137]); retrieval LIVE, canary LIVE, media caps + compaction levers applied. The **aggressive frozen-core slice + dreaming-on** remainder stays Andreas-only.
- **Acceptance/usefulness gate — PASS on all agent-quality dimensions** (6c.30). Sole residual = the transactional last-mile (CAPTCHA + hosted-field), Andreas infrastructure.
- **§A.1 (6c.9) items** — comms/chat-hygiene (P6-50/27/30), reliability carries (P6-57), watchdog sawtooth commit (P6-59) — all long since cleared.

**STILL-OPEN → folded to 6d (the Andreas-ready set; = v7 PART 11 + the opener ⛔ list):**
1. **Transactional last-mile** — CAPTCHA-solver (P6-39) + hosted-field cross-origin card-fill (P6-135, `pay-fill.sh` multi-target) + **the `pay-fill.sh` PATH bug** (verify+fix before payment work).
2. **Browser backend gap** — Playwright-launched-Chromium swap for `refs=aria`/`efficient`; per-site recipes (P6-44); `browser-dep-repair.sh` post-update guardrail (P6-40/42); benign `browser`-unavailable validation warning (P6-132).
3. **Hard code-level gates** — pulse send-gate (P6-128) + marie@ approval-token stop (soft brief rules = coping-not-cure).
4. **Robust outbound-bounce/DSN sweep** — extend reply-watcher (P6-138; HEARTBEAT rule = interim).
5. **Bloat/stick CURE** — aggressive frozen-core slice (verb-tables→retrieval, ≤30K) + dreaming-on + `:telegram:direct` compaction bounding (`CHAT_RESET_TOKENS`→~70000; `compaction.model`/`keepRecentTokens`); needs a billed fresh-session verify. chat-bloat-reset = proven backstop.
6. **Prereqs (§A.3, unchanged):** F2/F26 Andreas pairing; re-enable descoped launchd jobs (F64/F68/F70/D21-D24); F44 OAuth Internal-flip; F59 `GOG_KEYRING_PASSWORD` rotation; P6-45 payment-layer guardrail as a design constraint.
7. **S9 monitoring suite + P6-85 per-task cost tracker + the foundational-refactor mandate** (§C — Andreas's object-level job).
8. **Standing constraints (§B)** — inherit verbatim into Andreas's guardrails (secrets/config-hygiene/sandbox/launchd/posture invariants).
9. **Milestone hygiene (§D/§E)** — maintenance cadences; DR dry-run (S5); container retention (F31); Python 3.13 (D18); OAuth org-ownership (P6-90); the daily context-wipe experiment (P6-95); Charlotte/Andreas/Sophie avatars.
- **Optional reversible follow-ups (Peter-run, non-blocking):** `CHAT_RESET_TOKENS`→~70000; periodic reindex job; clean the orphan 1Password item `efrhqyifslubmapond7wb6mmee`; MEMORY.md RATIONALE-rotation.

---

## §A — Gating items (real blockers; do/decide before the named gate)

### A.1 — Clearing in THIS session (6c.9)
- **P6-50 / P6-27 / P6-30 — comms & chat-hygiene** **[MUST]** — no-substance-no-message, no-signature-in-owner-DMs, show-working-without-technical-detail (+ suppress success tool-trace, P6-27 — config key still A-12-unverified). _Stage 2._
- **P6-57 — reliability carries** **[MUST]** — Colima socket-health-check, Marie 92 MB session compaction, dev/test key wiring, spend-visibility decision. _Stage 3._
- **P6-60 — wire the 4 key zones** **[MUST]** — `pa`→zone1 (no-op), `pro`→zone2, route Claude-Code/test turns to zone4; zone3 stays dormant→Andreas at 6d. _Stage 3 / build step 1._
- **P6-59 — watchdog sawtooth fix** **[MUST→commit]** — diagnosed+fixed+live (16h stable) but the script is **uncommitted**; commit + record. _Stage 3._

### A.2 — Gate is 6c.10 (must be settled to author the v7 Andreas spec)
- **P6-49 — Andreas CTO / engineering-steward remit** **[MUST]** — formalise in v7: PROPOSE-not-EXECUTE separation, respects the version pin, owns the SR-1 doc autopilot, must know the P6-40/P6-42 update-wipe. The spine of the v7 spec.
- **P6-48 — author ARCHITECTURE v7** **[MUST]** — the Andreas-ready CoS spec; this whole backlog is its input.
- **S6 — reconcile MASTER-ARCHITECTURE Part 5 vs live 2026.4.22 schema** **[MUST]** — ≥4 known obsolescence instances; do *before* authoring Andreas's config off the (partly stale) template, else we copy drift.
- **S11 — inter-agent rate-limiting / loop-protection design** **[MUST]** — **highest-risk gap.** Undesigned. A CoS that fans out to other agents is precisely the trigger case (counter location, ping-pong/fanout/burst detection, circuit-breaker, recovery). Must at least be *designed* in v7 before a fan-out-capable agent goes live. _The 6c.9 sawtooth is a live cautionary tale: a supervisor that can act on the system it watches must be unable to harm it (it starved pro's 4h beat for a day)._
- **SR-1 / D23 — doc-currency autopilot + GitHub-publish decision** **[MUST]** — STATE-OF-NATION + RUNBOOK ownership transfers to the CoS; the sync mechanism (push-on-change vs scheduled) and publish target are undecided. Resolve before the doc-ownership handoff.
- **P6-9 / P6-11 / P6-12 / P6-14 — lock voice/signature/wording conventions** **[MUST]** — the *conventions* (em-dash restraint, signature final-form, wording) must be locked so Andreas's authored prose inherits them. (The batch-*apply* to existing text is CoS-owned post-roster, P6-10 — that part is not a blocker.)
- **P6-26 — derive Andreas's AGENTS.md/TOOLS.md from CURRENT Marie/Charlotte** **[MUST]** — not a stale template; pull live helper-blocks, P6-22 loud-failure rule, capable image, F26 def-of-done, the locked voice conventions. Discharge at build, but the rule is set now.

### A.3 — Gate is 6d build-start (must be true before unattended Andreas runs)
- **F2 / F26 — Andreas device-pairing + end-to-end routing** **[MUST]** — pair his Telegram bot with the 4-scope profile (`operator.read/write/approvals/talk.secrets`); not live until exercised through a real turn (F26).
- **P6-45 — payment-layer guardrail** **[MUST as design constraint]** — gate value at the card issuer, not in the agent; bake into the autonomy-ladder design when L1 relaxes for Andreas.
- **P6-40 / P6-42 — encode `browser-dep-repair.sh` after every `openclaw update`** **[MUST]** — update wipes the playwright-core symlink + the empty-allowlist guard patch; Andreas owns the pin, so this must be in his guardrails.
- **F64 + F68 + F70 + D21/D24 — re-enable the descoped launchd jobs** **[MUST]** — calendar-backup + security-audit are `.disabled-D24`, blocked on: F64 fail-fast on token-refresh hang, F68 launchd-PATH env gap (`node`/abs paths), F70 `op` unauthenticated in launchd (no `OP_SESSION`). Unattended automation Andreas would own is blocked until these clear.
- **F44 / Phase G OAuth posture** **[MUST]** — 7-day refresh-token expiry under External/Testing is a live weekly-breakage burden; the Workspace→Internal flip (~30d grace) materially de-risks Andreas's Google reliability.
- **F59 — `GOG_KEYRING_PASSWORD` rotation** **[MUST]** — plaintext-exposed, rotation deferred by Peter; should land before Andreas runs unattended.

### A.4 — Surfaced 6c.14→6c.20 (the memory/context substrate is now the HEADLINE pre-Andreas blocker)
- **Memory / Context Architecture v2 — THE CRUX [DESIGNED + Step-1 SHIPPED 6c.21; remainder = Andreas's improvement track].** Peter (4-Jul): ~10 sessions of memory/context/bloat/compaction/cost failures are **one flaw, not many** — stop patching, refactor. **6c.21 designed v2 and corrected the review's two premises empirically ([P6-118]):** (a) the retrieval layer is **not missing — OpenClaw ships it (hybrid embedding+FTS `memory_search`/`memory_get`, promotion, REM "dreaming", recency decay, idempotent flush); it was switched OFF** (index 0/34); (b) the review's config keys are all under `agents.defaults.*`, and `toolResultMaxChars` is **already 16 K** (not the "biggest lever" — the churn driver is the un-reset long-lived session + cache cadence). **Authoritative design + full cited research + validation matrix + staged migration + measurable acceptance gate: `MEMORY-ARCHITECTURE-v2.md` (supersedes the REVIEW doc as the build spec).** **Step 1 DONE + verified:** retrieval turned ON (index built pa 34/34·216 chunks, pro 31/31·36; `memory_search` proven returning the exact [P6-116] Porsche content at 0.667 that had returned `count:0`). **Upstream of Andreas** — a fan-out CoS on this substrate multiplies cost + unreliability (S11 in another guise). Folds in [P6-95] (daily context-wipe experiment, §E) and [P6-115] (MEMORY self-cap).
  - **Andreas's not-yet-implemented v2 backlog (Peter 5-Jul: "make sure the unimplemented research is available to Andreas") — all specced in `MEMORY-ARCHITECTURE-v2.md` §4/§6, research cited §2:**
    1. **Un-reset long-lived `:telegram:direct` session [highest churn lever].** It is excluded from the daily freshstart → grows unbounded → every turn cache-reads the whole history (the real source of the 355 K–1.5 M-token turns). Periodic **structured** compaction/checkpoint of it (not a blunt wipe — Peter values chat continuity, [P6-93]/[P6-95]).
    2. **Measured config pass (Peter-gated: sanctioned `openclaw.json` path + GUI restart; measure each per [P6-106]/[P6-109], don't batch blind).** `compaction.memoryFlush.model`→cheap/local (stop per-interaction flush on Opus); `keepRecentTokens` (protect active task through compaction); `truncateAfterCompaction` (bound the 12.5 MB JSONL); `identifierPolicy` stays `strict`; `contextInjection:"continuation-skip"` (cache-stable prefix); weigh `params.cacheRetention` (note: 1 h TTL won't cover the 2 h heartbeat gap — marginal). **Do NOT blindly lower `toolResultMaxChars` (already 16 K; risks truncating browser snapshots).**
    3. **File restructure to the four-store split (CoALA):** a small **frozen always-on core** (≤~30 K: IDENTITY+SOUL+lean AGENTS hot-rules + a `MEMORY.md` that is an **index not a blob**); move procedural detail (`TOOLS`/`PLAYBOOK`) + episodic to **retrieval-only**; **procedural ≠ semantic** (the brief is procedural, change-cautiously via batch-apply [P6-10]). **Status [P6-120]:** the *conservative* slice shipped 6c.23 (core 50K→48.7K, −1.3K, zero-risk; TOOLS rationale → indexed `memory/reference-tools.md`; all hot rules + verbs kept injected). **The AGGRESSIVE remainder (verb-tables/procedural → retrieval-only, target ≤30K) is ANDREAS-ONLY — do NOT attempt in interim sessions (Peter, 6c.23-followup: "no aggressive reductions/changes that can impact quality; flag and leave for Andreas").** It also needs a billed fresh-session verify turn (recall=0 → moving hot content out is a regression risk until she's proven to search).
    4. **Idempotent flush into a structured running-state, not freeform re-append** (Mem0 ADD/UPDATE/DELETE/NOOP) — the exact fix for the **9×-duplicated** daily files. Dedup the existing bloated dailies + enable `mmr` (recall is currently noisy) + re-index. Overlaps [P6-106]② workspace hygiene.
    5. **Consolidation ON:** enable `dreaming` (`0 3 * * *`, cheap model) — **only after** the dedup + reviewing the first `DREAMS.md` output (promoting from 9×-dup sources would pollute MEMORY.md). **Status [P6-120]:** dedup DONE (6c.22) but dreaming is currently **unset** (`Dreaming: off`, no `DREAMS.md` yet). **ANDREAS-ONLY — do NOT enable in interim sessions (Peter, 6c.23-followup):** it auto-promotes episodic signals into Marie's semantic memory nightly and its promotion behaviour lives in an opaque `phases` object with **no obvious review-gate** — a quality-impacting change. Andreas enables it *with* a proven review-gate (generate `DREAMS.md`, human-review before promotion is trusted).
    6. **Recency + relevance recall:** enable `temporalDecay` (`halfLifeDays` ~14–30). Importance-scoring (Generative-Agents poignancy) is not native — the promotion score-gates approximate it; a possible enhancement.
    7. **Supersession over deletion** for changing facts (Zep temporal-validity) — enhancement.
    8. **Observability canary (§P6 in the doc):** per-turn tokens (median/p95), compaction/hour, truncation/day, daily-file duplicate-heading count, index freshness (`indexed==total`), cache read:write ratio. Extend `cost-notify`/watchdog; alarm on breach. Pairs with the [P6-85] per-task cost tracker (§C).
    9. **Checkpoint-before-compact as a structural contract** (deliver + memory pointer before the boundary — MemGPT save-then-evict / Anthropic "ASSUME INTERRUPTION"); make the [P6-116] coping rules structural, not just brief text.
    10. **Options to evaluate:** `memorySearch.experimental.sessionMemory` (index transcripts for recall); **local GGUF embedder** (privacy — no external key/spend; currently OpenAI text-embedding-3-small per D22); the sub-agent-isolation vs single-thread debate (Anthropic vs Cognition — reconcile per workload: isolate exploration, centralise decisions).
    11. **The measurable acceptance gate (doc §8)** is the reliability gate that now blocks the [P6-76] usefulness gate — Andreas drives v2 to it: zero truncation, zero lost deliverables, median per-turn tokens/compaction-freq/daily-cost under target, index freshness green, retrieval hit-rate > 0.
- **★ Bloat/stick self-heal SHIPPED as a BACKSTOP, not the cure (6c.27, [P6-129]/[P6-130]).** `chat-bloat-reset.sh` archives+resets `:telegram:direct` when bloated(≥130K)+idle(≥20m)+cooldown — self-heals the "Marie goes quiet" stick (compaction hits a **hardcoded 60s `compactionTimeoutMs`, no config key**). **The CURE is still item 1 above** (bound the un-reset session + the ~25K/turn brief injection via the **aggressive frozen-core slice**). **Tunable levers Andreas has** (were the alternatives Peter declined for the auto-reset): `agents.defaults.compaction.model` (live `sonnet-4-6` → a faster model completes inside the 60s window) and `keepRecentTokens` (live 40000). Generate-mode of the credential bridge is now also live-proven+CLOSED ([P6-129]).
- **Credential take-over mechanism — SHIPPED [P6-115]** (was the [P6-108]① gap). Host-mediated `cred-save` bridge (generate + existing modes) + per-agent gitignored `.cred-queue` + `ai.openclaw.cred-save-watch` LaunchAgent; F26-proven live (Amazon). Andreas inherits it — keep the **non-sensitive-only** + **secret-host-side-only** invariants (encode in §B).
- **Payment-form automation reliability [→ §C track, not the memory session] ([P6-117]).** Hard gateways (Wikimedia / Trust-Payments / Stripe) defeat auto-fill (hidden custom inputs, SPA handlers, cross-origin iframes). **v1 honest posture: Marie drives to the page + surfaces the last mile; Peter completes card + click** — never claim full auto-fill on arbitrary SPAs.

---

## §B — Standing constraints Andreas must inherit (encode in his docs/guardrails)

**Security / secrets:** F21 (materialise-once key rotation, never trust in-place `.last-good`) · F25 (auth-profiles materialise-once) · F51 (`op` prints secrets — display-immune SHA-12+len checks only) · F52 (`op://` ASCII-only titles; reference by item-id) · F61 (memorySearch embedding cred in per-agent `auth-profiles.json`; provision `openai:default` on the right path) · F38 (bot-token SHA = 46-byte canonical, display-immune).

**Config hygiene:** F17 (staged same-dir edit + atomic `mv` + paired `chmod 600 && chgrp staff`) · F18 (hot-reload vs restart-trigger key classes) · F60/F62 (`.last-good` drifts; immutable timestamped backup is the only trusted rollback) · F14 (chat URL-rewriter mangles dotted names → shell vars / `jq --arg`) · F20 (`agents/main/` is the inheritance template, keep it) · F72 (re-run the F59-safe sanitiser + push after any live config change) · D22 (memorySearch locked to OpenAI text-embedding-3-small) · D2 (ClawHub skill source-review + allowlist before install).

**Sandbox / runtime:** F27 (Colima socket, not Docker Desktop) · F34 (containers run as host 502:20 → workspace `chown openclaw:staff`) · F58 (gateway auto-provisions memory DBs + auth skeletons) · P6-24 (tools in TOOLS.md + bound binary; per-agent sandbox-block drift — CoS owns sandbox-block currency from 6d) · P6-22 (surface tool failures loudly, never silent — also constrains any chat-suppression knob).

**External actuation / launchd:** F45/D20/F67 (gateway launchd + LaunchAgent ops are GUI-session-bound; run from Screen-Sharing for restarts/bootstraps; P6-56 refinement: SSH-OK *while a console session is active*, rejected only in the no-console/FileVault state) · F55 (launchd not cron for scheduled jobs; `cron` deny-listed except for cos/Andreas, P6-49) · F63 (launchd `gui/<uid>` jobs need explicit `EnvironmentVariables`/PATH) · F69 (parse `launchctl` with `| head -3`, never `head -1`) · F46 (calendar I/O via direct Google API, never `gog calendar events list`).

**Posture:** P6-42 (host-browser = empty allowlist + IP-class SSRF filtering, route by injection-trust not site-list; matches Peter's security philosophy) · P6-35 (sandbox→host egress boundary holds via token-gating + browser guard; **verify whether host-VM egress hardening option-B ever shipped** — bundled to 6c.7 but not recorded as applied) · P6-51 (no-unauthorised-test-spend HARD RULE; cost-tiering; dev/test ring-fence) · P6-33 (CoS-first sequencing; Sophie + non-owner Telegram + signature-PNG deferred past 6d).

---

## §C — 6d build-content (this *is* the CoS's job; design in v7, build in 6d)
- **S9 monitoring/automation suite** — (1) Sunday 04:00 auto-update+validate+auto-rollback [subsumes M4]; (2) daily 04:00 workspace git push **with secret-scanning before push**; (3) hourly self-modification watch on SOUL/AGENTS/DECISIONS/TOOLS/HEARTBEAT [S9(3)/M7]; (4)/(6) nightly 90-day session archive + weekly `openclaw backup` to Drive [needs Drive-write OAuth scope]; (5) 2-hourly Sophie/WhatsApp health-check [Month 2+]; (7) morning-window memory_pressure monitoring [relevant once Andreas adds a 3rd heavy heartbeat].
- **D19** — CoS owns maintenance-cadence + scheduled-work tracking; audits/reminds, never auto-executes. _(This is Andreas's core mandate.)_
- **P6-85 — PER-TASK cost tracker (Peter, 2026-07-02) [MUST — CoS-owned]** — Andreas **builds, owns, and iteratively improves** a tracker that attributes Anthropic spend **per task**, so each task's cost is visible and correctable while usage patterns are still being tuned; he decides format/output/cadence. Supersedes/absorbs the [P6-57]#3/[P6-82] spend-visibility carry (the daily token-volume canary — local `usage-cost`=$0, Console-authoritative). Rationale: the [P6-85] steady-state cost fix (heartbeat cadence/context) was diagnosed blind, by inspection; a per-task readout makes future tuning empirical instead.
- **S5** — disaster-recovery dry-run on different hardware + author DR runbook.
- **S7 / S4** — Agent Documentation System (storage, tier framework, shared KB scaffold, 1Password Note cleanup) + ongoing currency of STATE-OF-NATION/RUNBOOK.
- **M7** — security hygiene cycle (hourly CVE watch, daily audit, weekly `npm outdated -g`, monthly key/OAuth/skills/MEMORY review, stolen-device protocol).
- **★ Cross-site browsing / form-filling / payment capability [Andreas ONGOING REMIT — Peter, 6c.27, [P6-130]]** — agents must reliably work across a **WIDE set of websites**; Peter: "we keep bumping into problems again and again." The recurring payment/browse "you failed me" incidents (20 Jun AbeBooks, 2 Jul iGolf, **8 Jul Wikimedia**) are **one foundation smell, not isolated bugs** — Andreas OWNS this as a **standing reliability track**, not scattered on-demand patches (this is the [P6-49]/§C foundational-refactor mandate applied to the browse/pay surface). **Consolidates the previously-scattered threads:** host `browser` tool technique (**efficient-snapshot + interact BY-REF — never aria/CSS**; aria needs a Playwright-*launched* Chromium, we run CDP-to-system-Chrome, [P6-129]/[P6-130]); **payment-form hard gateways** (SPA / custom-styled / cross-origin-iframe — Wikimedia/Stripe/Trust-Payments, [P6-117]); **`pay-fill.sh`** card handoff (host fills the card **by ref** post-approval, value never in-context); **per-site recipes** (P6-44, React/Vue date-pickers + multi-step forms, build-when-hit → build a durable library); **in-sandbox Chromium** everyday-site failures (P6-37); and the **post-update `browser-dep-repair.sh`** guardrail (P6-40/P6-42 — playwright-core symlink + empty-allowlist SSRF patch, wiped by every `openclaw update`). Pairs with [P6-45] gate-value-at-the-card + autonomous-payments F26 ([P6-99]). **v1 honest posture until robust: drive-to-page + surface the last mile — never claim full auto-fill on arbitrary SPAs.**
- **Foundational review & refactor mandate (Peter, 4-Jul) [CoS-era, extends [P6-49]]** — once fully operational, Andreas periodically **goes back to first principles and rebuilds faulty foundations** rather than accreting patches. The memory/context saga ([P6-116]) is the exemplar: ~10 sessions patching a substrate that was missing a component. His engineering-steward remit explicitly includes **recognising the patch-on-flaw signature** (recurring incidents clustered in one area = a foundation smell) and **proposing a principled rebuild** — PROPOSE-not-EXECUTE, respects the version pin, surfaces cost/benefit. Peter's framing: "sometimes we keep patching a faulty foundation instead of going back to principles and rebuilding it so it works."

---

## §D — Maintenance cadences (Andreas tracks, Peter executes — all standing)
M1 quarterly Python google-* dep upgrade (first ~Aug 2026) · M2 gogcli/Brew upgrades (schema-touching = ceremony) · M3 macOS major updates twice-yearly (never automated) · M4 OpenClaw quarterly eval (pinned **2026.4.22**; 2026.6.6 available — surface before bump, run `browser-dep-repair.sh` after) · M5 `HOMEBREW_NO_AUTO_UPDATE=1` during ceremonies · M6 delete 2 inert 0.13.0 keyring files [NICE] · S10 Soveren.io email 8-month sunset migration (Phase G).

---

## §E — Defer-past-6d (no action until a real consumer exists)
- **P6-32 / SR-2-for-writes** [DEFER] — Google Docs/Sheets **writes** need an owned GCP project + verified OAuth client (gog's published project has those APIs disabled; reads work). SR-2 (ICS-before-calendar-write) is a **standing precondition not yet triggered** — the helper has no calendar-insert verb, so no write has shipped; fires only when write capability is built. _Caveat: only primary calendars ICS-captured; Soveren Team/CS non-primary not yet exported._
- **P6-33 signature-PNG** [DEFER] — build against real document examples, on-demand.
- **P6-44 per-site browser recipes** [DEFER/on-demand] — ~6 real sites (React/Vue date pickers, multi-step forms); build a recipe when an agent actually hits the site.
- **P6-52 Maps + restaurant reservations** [DEFER] — scoped, largely unblocked (P6-42/P6-32); reservation = L1 prepare-and-surface; needs a per-site recipe. Capability session, not pre-6d.
- **P6-46** — long-turn → gateway-timeout → stale session-lock; mitigation = keep deep flows short + gateway restart; `skipWhenBusy` unavailable in 2026.4.22. Watch.
- **P6-37** — in-sandbox Chromium everyday-site failures; mostly absorbed by the host browser; confirm closure.
- **D18 / S3 — Python 3.9.6 EOL → Homebrew ≥3.13** (target Sept–Oct 2026) [NICE→dated] — silent-breakage debt for Python tooling.
- **F31 — sandbox container retention/cleanup policy** [NICE] — adding Andreas grows the container population with no policy.
- **D27 — Colima VM auto-start plist** [NICE] — partly addressed by P6-2 activation; unattended reliability wants VM persistence (the 6c.9 socket-health-check is the nearer-term lever).
- **P6-36 workspace `.cache`/`.config` cruft** [NICE] — one-time delete-and-surface of stale 11-Jun dirs in both workspaces.
- **P6-20 Charlotte avatar** [NICE] — style locked; `/setuserpic` + `set-identity` application pending; Andreas avatar = final 6d step.
- **P6-90 OAuth-app org ownership** [NICE→Andreas] (Peter req, 6c.15; **reaffirmed 6c.17 via /btw**) — Marie's `marie@fedchenkov.com` send OAuth **reuses the pfedchen-owned `openclaw-pf` client** (durable, External/In-production, now admin-trusted in the fedchenkov.com Workspace). Cleaner long-term: a NEW OAuth app **owned by the fedchenkov.com org** with an **Internal** consent screen (no cross-org external-app dependency, no unverified warning). **Peter's exact framing (6c.17):** at some point it may make sense to **recreate our Google app inside a Fedchenkov Workspace**, *not* bolt `fedchenkov.com` onto the current pfedchen-owned app — **Andreas decides whether it's worth changing** (cost/benefit of the migration vs leaving the working app). Minor while the current app works.
- **P6-95 daily context-wipe = logged experiment** [→Andreas] (Peter req, 6c.17 via /btw) — the daily `session-freshstart.sh` context reset ([P6-85]/[P6-82]) that starts the long-lived `:main` sessions lean each morning is to be treated as an **explicit experiment**, not settled design. **Watch whether agent quality suffers** from the daily wipe (it interacts with the [P6-93] bootstrap-truncation finding — context management is delicate: too much bloats/truncates the brief, too aggressive a wipe may drop working context the agent needed). **Andreas owns improving it** if quality degrades — e.g. smarter carry-forward of live task/thread state across the reset, or tuning which sessions reset. For now: keep the reset, monitor the truncation-events/day canary + agent-quality signals, and hand Andreas the mandate + this note. (Human `telegram:direct:*` chats are already excluded from the reset — so Peter's own chat continuity is preserved; the experiment is about the `:main` beat sessions.)

---

## Top-9 shortlist (genuinely still-open *work*, priority order)
1. **Memory / Context Architecture v2** ([P6-116], `MEMORY-CONTEXT-ARCHITECTURE-REVIEW.md`) — **the crux of reliability AND cost; upstream of everything. Dedicated session 6c.21 (next).**
2. **S11** inter-agent loop-protection design _(reinforced by the 6c.9 sawtooth)_ — design in v7.
3. **S9 suite** — the bulk of what the CoS does; design in v7, build in 6d.
4. **SR-1 / D23** doc-sync mechanism + publish decision — before doc handoff.
5. **F64 + F68 + F70 + D21/D24** — re-enable the descoped launchd jobs (fail-fast + env + `op`-auth).
6. **F44 / Phase G OAuth** — kill the 7-day refresh-token weekly breakage.
7. **S6** architecture-vs-live-schema reconcile — before authoring Andreas's config.
8. **P6-49 → P6-48** — Andreas remit + ARCHITECTURE v7 (the 6c.10 deliverable).
9. **F31 / S5 / D18** — container retention, DR dry-run, Python 3.13 — system-surface debt to retire as Andreas adds load. _(Plus the standing autonomous-payments F26 + payment-form-automation track, [P6-99]/[P6-117].)_

_(Items already being cleared in 6c.9 — §A.1 — are omitted from this shortlist.)_

---
## 6c.32 addendum — S11/SR-1 BUILT (inert); must-resolve-before-A2A-live gates ([P6-140]/[P6-142])
Shortlist #2 (S11 design) + #4 (SR-1) are now **built inert** (6c.32): the host mailbox/watcher, all 5 layers, the sessions_spawn budget, and `carry-chain-audit.py`. The pre-push adversarial self-audit ([P6-142]) fixed the robustness bugs (mailbox-wedge, dryrun-HALT, hop coercion, counter durability, fail-open audit) and flagged these **hard gates that MUST be resolved before A2A is enabled live (the 6d wiring session):**
1. **Envelope construction + topology (#1 gate).** Decide hub-synchronous (`--deliver` returns) vs peer-relay; define the agent-side constructor (mints conversation_id / increments hop / appends path / whether a reply resets it); reconcile the L1 cycle-check vs the per-pair budget. Without this A2A may not function.
2. **Provenance hardening.** Host-reconstruct path/hop from `ledger.jsonl` per conversation_id (robust vs a mis-constructed envelope), or accept + document the accidental-loop-only threat model.
3. **L3 real £/Console hook** (a turn-count proxy today) + **midnight reset** of conversation-scoped budgets.

Meanwhile the numeric budgets (conv 8 / pair 2 / hop 4 / fanout 2 / daily 30·10 / fleet 60 / spawn 3-per-hr) are the robust primary guard; A2A stays OFF in config. Full open-list: `shared/a2a/README.md §Open`.

## 6c.32 addendum (2) — Andreas flagship: precise per-ACTION cost model + Marie screenshot-delivery fix ([P6-144])
- **★ PRECISE PER-ACTION COST MODEL (Peter, 14 Jul — Andreas flagship stewardship deliverable, sharpens [P6-85]).** The current local estimate is **~20× off** (Marie's per-message meter read ~$4.55 on a ~€100 Aviva day). Build a model that attributes **real $ to every action** (turn + tool call), decomposed into drivers (in/out tokens, cache write [dominant] / read, image tokens, memory-flush turns) and attributed per task/workstream/agent. Real $ = Anthropic Cost/Admin API ([P6-107]/[P6-109]); **reconcile to the Console day-total ≈±15%** or flag untrusted. Purpose = **track AND optimise**: surface expensive patterns (Opus operational grinds, idle cache re-writes, screenshot image tokens, flush turns) each paired with a measured lever (toolResultMaxChars / imageMaxDimensionPx / cacheRetention / **per-task Opus↔Sonnet routing** [P6-28]) at before/after $. Propose-only (never self-edits openclaw.json). Full spec: `workspace-cos/COS-PLAYBOOK.md §Stewardship(b)`. Retire Marie's bad local proxy once this is trustworthy.
- **★ Marie screenshot-delivery fix ([P6-144]④) — payments/booking-critical.** Capture works; delivery to Peter's Telegram was closed on false evidence (P6-103 payload-inference, never F26-verified). **Diagnostic result (6c.32): Peter SAW the host-side `message send --media` test (msg 1685) → the Telegram channel delivers fine; the bug is in the AGENT-TURN media-attachment path** (how the browser-screenshot structured media reaches the reply). Fix + F26-verify (real Marie turn → Peter confirms receipt) — bundle into the 6c.33 Sonnet F26. Check `imageMaxDimensionPx=1024` / `snapshotDefaults.mode=efficient` as contributors.
- **✔ Screenshot-delivery fix DONE (6c.34, [P6-146], F26 PASS)** — sandbox reply-media containment dropped `media/browser`; 2-edit design-faithful dist patch + brief fix; native Tier-1 delivery proven.
- **✔ Cost model BUILT (6c.34, [P6-148]): `scripts/cost-breakdown.py`** — per-action driver decomposition (cache ≈90%), reconciliation hooks. Trustworthy for **relative** attribution + optimisation.
  - **○ OPEN IMPROVEMENT — Andreas owns the decision (NOT next-session; Peter 6c.34).** Absolute-$ **undercounts ~4×** vs the **agents-only** Console total (workspace is agents-only; Peter's Claude Code is on Claude Max, separate — so Console MTD $514 ≈ agent cost, model reads ~$127). Prime suspect = **coverage**: the tool reads only live `sessions/*.trajectory.jsonl` and misses **rotated/archived** ones (gzipped `*.trajectory.jsonl.gz` / `sessions/archive/<date>/*.jsonl`) → Jul 1–3 read $0 vs ~$140 real. **Fix path (when/if Andreas judges it worth it):** (1) read gzipped + archived trajectories; (2) re-sum vs the $514 agents-only figure; (3) *then* judge whether `rates.json` needs recalibration. No Admin/Cost API on the account (confirms [P6-109]) → reconcile via a per-agent-workspace Console export, not automated. Relative attribution is unaffected; this is absolute-$ accuracy only.

## 6c.36 addendum — Marie reliability deep-audit + Peter's directives (routing) ([P6-153])
Peter commissioned a deep audit of Marie's "goes quiet / forgets / unclear status" failures (Telegram, 16 Jul Aviva instance analysed live + the full P6 history mined). **Core read: the behavioural rules are already correct ([P6-149] wrote near-perfect prose 24h before it failed again); the durable roots are ARCHITECTURAL and were designed-then-deferred to Andreas — this is Peter's standing P6-116 mandate ("stop patching a fundamentally flawed system") still open.** Directive: *fix what's safe now, surface the rest to Andreas without losing context.*

**★ THE FOUR DURABLE ROOTS — Andreas-owned (today's evidence = the case to prioritise these):**
1. **Aggressive frozen-core brief slice** (cut per-turn injected brief tokens so retrieval carries the rest) — the actual bloat/stick cure. Already the §A.4 HEADLINE blocker; **today quantified it**: Marie's `telegram:direct` = 128K tokens / 3 compactions-per-turn / $0.99 per turn vs Andreas 44K / 0 compaction / $0.025 — **SAME model + cap; the entire latency + cost + "sticks/goes quiet" delta is bloat.** Andreas is fast only because he's new; he hits the same wall without this. (Note 5 root.)
2. **Engine-level live-chat keep-alive / fast re-wake** ([P6-149] residual). The discrete-turn model kills any live chat at the turn boundary; a brief keep-alive can't hold it. This is the sharpest un-fixed "goes quiet" root — the 16 Jul 68-min Aviva silence (chat dropped ~11:04, surfaced only when Peter poked at 12:14) is the exemplar. Needs a host-driven ~25s keep-alive against a **bounded worker session** (isolate live-chat/browser tool-noise OUT of the human `telegram:direct` session — same principle as the A2A host-mailbox).
3. **Production retrieval recall = 0** ([P6-118]/[P6-137]). The retrieval/episodic stack is ON but agents never call `memory_search` on routine turns → still 100% wholesale injection → "forgetting". Activate real recall-in-production (active-memory plugin or the slice). This is ALSO the enabler for Note 6 (reusable micro-skills) below.
4. **Hard code-level pulse/turn send-gate** ([P6-128]/[P6-138]) — background beats can still auto-send external commitments; the HEARTBEAT "drafts-never-sends" rule is soft. + **per-task model routing** ([P6-28]/[P6-144], already listed) so Marie can run Sonnet on operational grinds.

**★ PETER'S 6c.36 DIRECTIVES — routing:**
- **(1) Marie monitors her own `marie@` inbox, not just Peter's — ✔ DONE NOW (6c.36).** Root cause found: her read helper was hardwired to `/creds/google.json` (Peter's box) and the brief scoped `marie-send.json` to send-only, though its `gmail.modify` scope reads. Fixed surgically: HEARTBEAT beat step + TOOLS now have her scan `marie@` every beat via `--creds /creds/marie-send.json list`. (She'd "forgotten" marie@ a day earlier.)
- **(2) Regular stale-task/draft cleanup (Peter's box + hers) — POLICY DECIDED; build sequenced.** TASKS.md is 22.5KB/~27 stale done-rows; drafts accrete in both mailboxes. **Peter's decision (6c.36):** authorise Marie to **DELETE her own `#PA-xxx`-tagged stale/superseded drafts** (in his drafts folder + marie@) + archive done TASKS.md rows on a weekly hygiene beat. **Build dependency found:** `gmail_helper.py` has NO draft-delete verb — needs a `draft-delete` verb (`users().drafts().delete(id)`) + sandbox re-bind ([P6-23] container ordering) before the delete path works; archiving + TASKS.md hygiene work with the existing `modify` verb today. Lift the brief's "never delete in Peter's box" floor **only** for her own tagged drafts. Robust recurring host hygiene sweep → Andreas.
- **(3) Optimise `@fedchenkov.com` domain deliverability (raise delivery rate) → ANDREAS.** SPF/DKIM/DMARC exist (PA-PLAYBOOK L80) but Peter wants delivery-rate improved: verify DMARC policy/alignment, DKIM key strength, SPF completeness, warm-up/reputation, postmaster tools, and per-recipient deliverability testing. Andreas owns as a stewardship task (touches DNS — propose-not-execute; Peter applies DNS).
- **(4) Feed Marie screenshots of favourite restaurants/places (London + worldwide) → DEFERRED, event-gated on Travel agent (~early Aug, [P6-147]).** When Travel lands, Marie ingests Peter's curated place/restaurant screenshots into a durable, retrievable preferences store (a "places" knowledge base) for booking/recommendation. Needs the retrieval layer (root #3) to be reusable. Park until Travel exists (§E-style defer).
- **(5) Marie slower than Andreas — root-caused (Note 5): it's the bloat (see root #1), not the model.** No separate fix; closes with root #1. Flagged that the gap will *invert* (Andreas slows as he accretes) without the slice.
- **(6) Marie learns + logs reusable micro-skills (e.g. "how to reach a live operator") → NOW-lightweight + ANDREAS-robust.** My read (Peter invited pushback): the lightweight version is tractable and worth it — a small `SKILLS.md`/playbook of reusable micro-procedures she appends on success and consults before a repeat task; it dovetails with the retrieval stack (becomes indexed/searchable). BUT it's only *reliably reusable* once production retrieval is live (root #3) — until then it's a static file adding brief-budget pressure with weak recall. Recommend: seed the file + append-on-success discipline now (cheap), and the "reliably retrieved when relevant" property rides the retrieval activation (Andreas). Not too complicated at the lightweight tier; the auto-generalise/dreaming tier is Andreas.

**★ AUDIT NOW-FIXES (safe, non-Andreas — sequenced; some pending Peter's go):**
- **Memory-flush duplication bug** — today's `memory/2026-07-16.md` re-appended its whole earlier block byte-identical despite the "append-only" flush instruction (P6-120 dedup not holding). Diagnose (flush prompt vs dedup-tool cadence) + fix. Direct "forgetting"/budget-waste contributor.
- **Reset-net retune** — `CHAT_RESET_TOKENS` 130K is ABOVE the 120K cap (thrash starts 10K before a reset is considered). Lower below cap + graceful in-conversation "tightening context" instead of a silent stick. **Pair with the memory-durability fix** (leaner reloads only safe if memory captures reliably) — don't ship in isolation.
- **★ Status-SLA + compaction-proof open-loops file (the real structural NOW-win, design-first per Peter).** A small `open-loops` state (what / who's-waiting / last-status-to-Peter ts / next-action) that survives every reset + a host watcher (reply-watcher pattern) that FORCES a status when an active loop goes silent past a threshold. Converts "never go silent on an active task" from prose-that-failed-today into host-enforced. Generalises to all agents. Design → Peter → build.
- **Noise-vs-status bright-line** — replace Marie's self-authored "poll completions → NO_REPLY" (which suppressed the 16 Jul chat-drop signal) with "a poll carrying a STATE CHANGE on an active loop always surfaces"; ideally enforced by the open-loops worker, not left to mid-flood judgement.
