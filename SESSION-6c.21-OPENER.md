# SESSION 6c.21 — Real-charge payments F26 (plain merchant) + first REAL credential store via the new queue
_Opener · generated at 6c.20 close (2026-07-04). 6c.20 shipped the **credential take-over mechanism** end-to-end ([P6-110/114/115]) — host-mediated `cred-save` bridge (generate + existing modes), per-agent gitignored `workspace-<id>/.cred-queue/`, the `ai.openclaw.cred-save-watch` LaunchAgent (live, gui/502), briefs wired, and an **F26 pipeline PASS** on a throwaway store — and **root-caused + fixed a multi-day Gmail-helper outage** hitting both agents ([P6-112/113]). The **real-charge payments F26 did NOT land** (the one live payment, Banya, was the Trust-Payments iframe hard case → Peter paid manually, [P6-111]). openclaw.json **UNCHANGED** `da191cf1920d`/10099B (A-7 clean). Read the 6c.20 closure + [P6-115] first. **GUI/Screen-Sharing (D20)** for the browser + Peter's 2FA/OTP._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`da191cf1920d`/10099B**, 600/staff. Pin 2026.4.22. Sandboxes `capable-2026-06`. (Read openclaw.json by structure/script only — Bash deny blocks its name + `cat`/`jq`/`sha256sum`; agent-creds token path deny-guarded.)
- **Deviations currency:** P6-115 present; sequence **P6-1…115 gap-free**, no dangling refs (run the [P6-101] carry-chain check at THIS close too). Pre-existing 21/22 dupes are historical — leave them.
- **Build artefacts present?** `scripts/cred-save.sh` `389c46e89c10` · `scripts/cred-save-drain.sh` `4e714117e36c` · `scripts/op-put.sh` `50a56225df19` · `scripts/pay-fill.sh` `93f5134cb383` · LaunchAgent `ai.openclaw.cred-save-watch` (`launchctl print gui/502/…` → run interval 45, exit 0). Brief shas — PA-PLAYBOOK `65bf3fd04199`/24136B · MEMORY `e2e028cf3c64`/5707B · AGENTS `3b967ca6f983`. `_session-docs` + config repo committed at 6c.20 close.
- **★ MEMORY self-cap holding?** `wc -c workspace-pa/MEMORY.md` should stay **< 5909** (the [P6-115] self-cap rule targets ~5300). If it regrew past 5909, Marie's one-in-one-out rotation isn't firing — trim + consider the mechanical canary (below).

## Stage 1 — the work (GUI, Peter-paced)
1. **★ LEAD — real-charge payments F26 (still the acceptance-gate blocker).** A **Peter-directed real purchase** (payee + amount — never invent one) on a **PLAIN-input merchant** (NOT Stripe/Trust-Payments cross-origin iframes + captcha — those are the deferred hard case, [P6-111]). Run as a **real Marie gateway/Telegram turn** ([embedded-cli] — NOT `openclaw agent` embedded CLI): Marie drives login+checkout → snapshots card-field refs → surfaces "ready to pay" → **host** `pay-fill.sh --card uk|intl <refs>` → Peter approves + 2FA → append `shared/finance-ledger.md`. Cheapest model, ~$5 auth ([P6-51]), no loops. **This marks the capability done (F26).**
2. **First REAL credential store via the new queue (quick, exercises [P6-115] live).** Peter re-sends a real login (Amazon was the intended one) over Telegram with the message-id available; Marie drops a `mode:existing` request into `/workspace/.cred-queue/pending/` (incl. `tg_chat_id`+`tg_message_id`); the **live watcher** files it to 1Password, shreds the request, deletes Peter's TG message, and writes the receipt. Verify the 1P item + `results/…json` (`status:"stored"`), and that the TG message actually deleted. Then a **generate-mode** store to prove account-provisioning through the same queue.
3. **③ Reply-watcher** — build on the agents' **live Gmail credential** (`agent-creds/pfedchen-gmail.json`, stable — the one the fixed `gmail_helper` uses); **skip gog** (dead OAuth, decided [P6-109]). Host poller → `openclaw agent … --deliver` on a new relevant reply. Unblocks Banya-style auto-action.
4. **④ Context-lever cost tuning pass (measure-then-tune, biggest open cost lever).** Apply ONE lever at a time with before/after measurement (calibrated Console rates, [P6-109]): `contextLimits.toolResultMaxChars` (caps browser-snapshot bloat — likely biggest), `params.cacheRetention:"long"`, `imageMaxDimensionPx`, `compaction.memoryFlush` tuning. Do NOT apply blind. Keeping Marie on Opus ([P6-106]) depends on this.

## Stage 2 — smaller carries
- **Delete the 6c.20 throwaway 1P item** `agm4bg3fupvstogchryz75uwri` ("ZZ-TEST-cred-pipeline", Agent-Provisioned) — `Bash(op item delete:*)` is deny-listed for me; **Peter deletes it**, or authorises a one-off.
- **op-put username data-repair** — any 1P items mis-stored before the [P6-110] `$USERNAME` fix (Opera / Body4Life) hold `openclaw` instead of the email in the username field. `op item edit` is deny-listed → **Peter's call** to fix or re-create.
- **MEMORY-size mechanical canary** — fold a `wc -c MEMORY.md > 5909` alert into the overnight-verify sweep so a regrowth is caught by a detective, not just the behavioural self-cap ([P6-115]).
- **Battery re-run (judgement class)** — convert T3/T6/digest FAIL→PASS through the **real Telegram** path; cheapest model (Sonnet/Haiku, never Opus), ~$5 hard cap ([P6-51]).
- **Charlotte inbox archive** — confirm the 6c.20 archive sweep (task `bm6t3l521`) completed and relay the tally; if it stalled on a session-lock again, re-run when her session is idle. Fold in the [P6-98] function-level morning-sweep check.

## Safety rails (whole session)
Reversible build runs without prompting. **Surface irreversible/destructive:** the real charge (payee+amount is Peter's; he does 2FA), credential first-login, real sends beyond the gated flow, OAuth/GCP/billing, version-pin/D1, **1Password writes/deletes** (note `op item delete`/`edit`/`vault`/`service-account` are deny-listed — never route around; surface to Peter), depairing, reboots. **Cost ([P6-51]):** inspection/host-drive first; agent turns only for what needs the agent, cheapest model, no loops/Opus/batches. **Secrets ([F21/F51]):** op host-only; card + credential values off-argv, out of sandbox; display-immune checks only. **Credential queue ([P6-115]):** existing-mode is NON-SENSITIVE logins only — `sensitive:true` and card/token/pin/otp are refused by design; keep it that way. GUI/Aqua for browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN — payments capability is mechanism-proven but not F26-charged; decidable once the real plain-merchant charge lands + the judgement classes hold.
- **Pre-Andreas milestone — v7 capstone / reconciliation** ([P6-76], roadmap §6c.10): reconcile STATE-OF-NATION / RUNBOOK / ARCHITECTURE from the P6-77…P6-115 deltas + crystallise lessons into PHASE-0-LESSONS / TIER-1-KEY-PATTERNS — due **after** the acceptance gate PASSES, immediately before the 6d Andreas opener.
- **Consciously parked** (not re-lost): [P6-78] Telegram back-scroll = Bot-API limitation, not buildable; [P6-79] per-turn tool/token ceiling = no 2026.4.22 knob, covered by the heartbeat canary.
- **Andreas backlog:** function-level agent-health check ([P6-98]); MEMORY-size canary; `:telegram:direct` compaction-pressure; org-owned OAuth app (P6-90/95); per-task cost tracker (real billed $ activates when Peter drops an Anthropic Admin API key — [P6-107]); revert Charlotte→Opus at her v2 activation ([P6-107]); workspace hygiene purge (dry-run manifest first, [P6-106]#2); autonomous-login build; Stripe/Trust-Payments iframe fill strategy ([P6-111]); model/framework currency ([P6-74]); DMARC tighten. Travel POST-6d. 2026.4.22 pinned.
