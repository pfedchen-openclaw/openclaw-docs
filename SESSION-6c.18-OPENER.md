# SESSION 6c.18 — Overnight verdict (real, first) + act on Peter's transactional-autonomy answer + re-run the remediated battery ([P6-92/93/94])
_Opener · generated at 6c.17 close (2026-07-02). 6c.17 mined both agents' 2-week transcripts, remediated the judgement-class frustrations in-brief (check-Sent-before-surfacing, digest-as-decision-aid, archive-opt-in, not-interested ledger, de-dup, transactional expectation-setting — all placed in the truncation-protected AGENTS.md), found + partly fixed a bootstrap-truncation regression ([P6-93]), scored the acceptance-gate battery NOT-PASS, and surfaced Marie's transactional-autonomy decision. Read the 6c.17 closure + `PHASE-6-DEVIATIONS-LOG.md` P6-92/93/94 + the updated `ACCEPTANCE-GATE-SCORECARD.md` before acting._

## Context / can this run over SSH?
Mostly SSH-safe (log reads, scorecard scoring by inspection, doc reconciliation, brief edits). **GUI/Aqua** if Peter chose "provision the card" (auth-profiles + 2FA build) or any gateway restart / config change / `op`-admin (D20). Billed turns only for the battery re-run Peter authorises (inspection-first; the ~$5 gate cap still applies — [P6-51]).

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`8a6e2a19d9d5`/10096B**, 600/staff. Pin **2026.4.22** (do NOT bump). Both sandboxes `capable-2026-06`. (Bash deny blocks `sha256sum`/`cat`/`jq`/`stat` on `openclaw.json` — use `shasum -a 256` + `wc -c`.)
- **Deviations currency:** P6-94 present; re-anchor the log sha (was re-anchored at 6c.17 commit).
- **★ Overnight verdict — now genuinely due (first real read).** Read **`logs/overnight-verify.log`** for the **07:15 03-Jul** PASS/PARTIAL/FAIL of the night-of-02→03 run (the first with the automation armed). Confirm it **reached Peter's Telegram** and the one-shot **self-removed** (`launchctl list | grep overnight-verify` empty; `~/Library/LaunchAgents/ai.openclaw.overnight-verify.plist` gone). If PARTIAL/FAIL, diagnose from nightwindow/freshstart logs + markers (P6-94). If the verifier didn't fire (log still only the 6c.17 header line), check `.launchd.err.log` and whether the Mini slept.
- **Anchor the TOOLS.md-slim SHAs (6c.17 carry):** pa/pro `TOOLS.md` + both `PLAYBOOK` were slimmed (verbose prose → PLAYBOOK, dup removed, invocations preserved). Confirm no capability lost — `grep` the key verbs (`gmail_helper`, `google_helper.py`, `calendar-insert/-update/-cancel`, `drive-upload`, `docs_helper.py`, `browser_helper.py`, `op-get/op-put`) survive in each TOOLS.md — and record before→after SHAs in the log.
- **Truncation canary:** re-run the events/day check (`grep 'truncating in injected context' logs/gateway.err.log | cut -c1-10 | sort | uniq -c`) — post-slim it should fall from the 2-Jul peak of 11. If it persists, escalate the [P6-93] config options.

## Stage 1 — the work (Peter-paced)
1. **★ Act on Peter's transactional-autonomy answer ([P6-92 §surfaced]).** If **"provision the card"** → the deferred L3+ build: card into per-agent `auth-profiles.json` (host-side via 1Password, never in a workspace file), the 2FA-approve-in-Revolut pause, Finance log; GUI/1Password (D20). If **"golf items"** → set up the England-Golf/iGolf + Pro Agenda golf-lesson logins (warmed Chrome + 1Password) so Marie can drive them to pre-pay. If **"keep hand-off / free-signup-only"** → no build; the 6c.17 expectation-setting brief already covers it.
2. **★ Re-run the remediated battery through the real Telegram→gateway→sandbox path** (embedded CLI mis-tests — [P6-81]/[embedded-cli]) to convert the **judgement-class** FAILs → PASS under STRICT-100%: T3 (no false clash; check-before-surface), T6 (Charlotte triage without resurfacing Tim / unwanted offers), and a digest/what-needs-you check. Cheapest sufficient model (Sonnet/Haiku, never Opus for tests), ~$5 gate cap, stop-and-surface if trending over. The **capability class** (T4/T5) waits on the autonomy decision; **T1 reliability** = confirm the rental-agreement reminder fired on time (due Fri 3 Jul 19:00) with proof-of-done.
3. **v7 capstone / reconciliation (if room).** Fold P6-77…P6-94 into STATE-OF-NATION / RUNBOOK / ARCHITECTURE; crystallise the truncation + digest + check-Sent lessons into PHASE-0-LESSONS / TIER-1-KEY-PATTERNS.

## Stage 5 — Verify (F26, strict cost) + Stage 6 — Close
Battery re-runs through the real Telegram path; cheapest model; ~$5 cap; stop-and-surface if trending over. Close: deviations additive; `SESSION-6c.18-CLOSURE.md`; `SESSION-6c.19-OPENER.md`. Re-anchor shas; commit `_session-docs/`.

## Safety rails (whole session)
Reversible build runs without prompting; **surface irreversible/destructive:** real sends beyond the gated flow, OAuth/Workspace/GCP/billing, version-pin/D1, 1Password writes/deletes, depairing, reboots, the card-provisioning (Peter's explicit go). **Cost ([P6-51]):** inspection-first; battery re-run within the ~$5 auth. **Secrets ([F21/F51]):** never read secret values; `agent-creds/**` + `openclaw.json` by structure / display-immune / staged edits. GUI/Aqua for consent/restart/LaunchAgent/`op`-admin/card-provisioning (D20). Stop at ~63% context for a clean close.

## Carry-forward (deferred, not lost)
- **Acceptance gate** stays OPEN — decidable only when the judgement-class re-runs PASS **and** the capability class is unblocked (autonomy decision) **and** T1 reliability holds. Only on all-pass → author the 6d Andreas opener.
- **Truncation [P6-93]:** file-slim done; watch the canary; config-side (cap-raise / session-compaction) is Peter's cost/quality call if it persists.
- Andreas backlog: org-owned OAuth app (Internal consent) to replace pfedchen-owned `openclaw-pf` (PRE-COS-BACKLOG); per-task cost tracker ([P6-85] item#1). DMARC `p=none`→quarantine once reputation builds. Travel = POST-6d. Model/framework currency ([P6-74]) → Andreas. 2026.4.22 pinned.
