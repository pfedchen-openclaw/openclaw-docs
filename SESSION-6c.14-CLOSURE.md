# SESSION 6c.14 — Marie calendar EDIT-rights shipped (P6-89); WS-4 deferred to a GUI run — CLOSURE
_2026-07-02. **SSH session** (`SSH_CONNECTION` set ∴ D20: gateway-restart / LaunchAgent / consent ops NOT available). Peter opened by redirecting: the 6c.14 headline (WS-4 agent email) is GUI/browser-bound and can't run over SSH, but he flagged a real gap — "Marie doesn't have edit rights to the @gmail.com calendar — fix now or before Andreas." That became the session. New code **P6-89**. **`openclaw.json` UNCHANGED** (`a8783dcd17dc`/9993B). No billed test turns — all verification was free (Google API calls) + real-sandbox `docker exec`._

## Stage 0 — GREEN
A-7 clean (live==last-good==`a8783dcd17dc`/9993B via `shasum`; a Bash deny-rule blocks `sha256sum openclaw.json` but byte length 9993 + `shasum` match confirm it); pin **2026.4.22** (no bump); both containers `capable-2026-06` (up 5d); google_helper RO-bind intact in both; OAuth alive (calendar read exit 0). Deviations currency: P6-86/87/88 present; log re-anchored `41fd5c2e59e8` at open. (Overnight heartbeat/fresh-session Stage-0 checks from the opener were not run — the session pivoted off the WS-4/restart path to the calendar fix; flagged for the next GUI open.)

## What shipped (reversible, live, verified) — [P6-89]
- **Diagnosis (scope was never the issue).** Full `calendar` scope granted since [P6-81]; the gap was the helper — read + a surface-only `calendar-prepare` (never inserted), **no execute-insert / update / delete** — and stale docs (PA-PLAYBOOK §Calendar still called the write path a future "Week 2 build item"). Q1 resolved by inspection: pfedchen `primary` is Marie's "exclusively" (AGENTS.md), work/Soveren read-only → **no Google sharing-ACL needed**; pure helper+docs fix.
- **Built** (`agent-creds/google_helper.py`, [F51] copy-roundtrip, no secret in context): `calendar-insert` (create), `calendar-update` (partial `events().patch` — reschedule/edit), `calendar-cancel` (snapshot → `events().delete` → Trash, restorable ~30d + ICS backup; single event, never bulk). `--send-updates` defaults **`none`** — attendees never emailed unless explicit. `b38f267ded20`/17648B → **`46a9243f954a`/24083B** (644). **RO-bind → `/opt/google_helper.py` in BOTH containers ⇒ live with no `sandbox recreate`, no gateway restart** (the [P6-83] pattern).
- **Posture (Peter approved all three recs):** **act-directly** for Marie's own **no-attendee, non-recurring** events on `primary` (create/reschedule — internal artefact, like Drive/Docs); **prepare-and-surface** for attendees / recurring / non-primary / **every cancel**; a reschedule `calendar-get`s first to check no-attendee/non-recurring. **No** write beyond `pfedchen primary` (work cal stays read-only).
- **Verified 3 layers, zero billed turns ([P6-51]):** (1) `py_compile` + argparse `--help`; (2) **free host-side full lifecycle** vs real pfedchen creds — insert → get → update(reschedule/retitle/relocate) → get → cancel → get(`status=cancelled`), all asserted; far-future no-attendee test events, `sendUpdates=none`, auto-removed; (3) **real sandbox runtime** — `docker exec` both `pa` and `pro` list all four calendar verbs in bound `/opt/google_helper.py`; in-container read executes. **F26:** Peter confirmed a real prepared/approved calendar write works end-to-end through the gateway ("I confirm it all works now").
- **Docs reconciled** to the decided posture (Open→Decided): `workspace-pa/TOOLS.md` `b407763d591a`→**`09149235201d`/17283B**; `workspace-pa/PA-PLAYBOOK.md` §Calendar `f106201d9c84`→**`cfe9c034b2cf`/13824B** (rewrote the stale fix-plan; kept all guardrails — no bulk, confirm >2, recurring scope-explicit, work-cal read-only).

## End-state (anchors)
- **`openclaw.json`: `a8783dcd17dc`/9993B — UNCHANGED.** A-7 clean, 600/staff. Pin **2026.4.22**; both containers `capable-2026-06`; gateway not restarted.
- **Changed files (SHA-anchored):** `agent-creds/google_helper.py` `46a9243f954a`/24083B (644); `workspace-pa/TOOLS.md` `09149235201d`/17283B; `workspace-pa/PA-PLAYBOOK.md` `cfe9c034b2cf`/13824B. **Deviations:** P6-89 appended additive-in-place (build + Peter decisions + F26, one entry). _Note: workspace-pa + agent-creds are live-runtime, not tracked by the top-level repo; only `_session-docs` is committed._

## Carried (deliberately, not lost) → 6c.15
- **WS-4 agent email `marie@fedchenkov.com`** — still the headline; needs a **Screen-Sharing GUI terminal** (Workspace signup + Cloudflare DNS + OAuth consent + gateway restart). Decisions still locked (peter@ super-admin + marie@ standard; Cloudflare; Business Starter). Phase-5 wiring: `6c11-OAUTH-FLIP-BRIEF.md` Path B; identity swap-vs-second-mailbox still to decide.
- **Next GUI open should also run the deferred Stage-0 overnight checks** (2h beats held; 06:55 fresh-session fired; watchdog clean through the night; `heartbeat.night` absent by day) — carried from the 6c.14 opener, unexercised this SSH run.
- **Peter action:** delete the retired 1Password SA (`WUE7…`) if not already done ([P6-88]).
- Standing: acceptance gate + v7 capstone (pre-Andreas, [P6-76]); Travel = post-6d; [F59]/[P6-66] rotations; 2026.4.22 pinned.

## Honest note
Peter's opening line looked like a scope problem ("no edit rights") but wasn't — the scope had been full since P6-81; the missing piece was three helper verbs and a stale playbook. I verified that empirically before touching anything, which meant the fix was purely additive and SSH-safe (no re-consent, no restart). I shipped it in two passes: a conservative prepare-and-surface build first (no trust line crossed while Peter was AFK), then the act-directly posture + gated cancel once he approved. The one genuinely destructive verb (cancel) is single-event, snapshot-first, Trash-recoverable, and always surfaced — deliberately not the gmail/drive "never delete" floor, because a calendar cancel is normal PA work and Peter asked for it. Everything was proven against his real calendar with throwaway far-future events and zero test spend. WS-4 remains the right thing to run next, in a GUI session where its OAuth/restart steps are actually available.

---
_Attachments for 6c.15: this closure + `SESSION-6c.15-OPENER.md` + `6c11-OAUTH-FLIP-BRIEF.md` (WS-4 Phase-5) + `JUDGEMENT-DOCTRINE.md` + `PHASE-6-DEVIATIONS-LOG.md` (through P6-89) + `CAPABILITIES-ROADMAP-TO-6D.md`._
