# SESSION 6c.29 — Billed re-tests on the healed lean session (T3 responsiveness + Wikimedia payment + 3-task multi-message), Peter-gated; then observe steady-state. Aggressive slice + dreaming + brief-budget + v7 capstone remain ANDREAS/milestone

_Opener · generated at 6c.28 close (2026-07-09). 6c.28 shipped: **chat-bloat-reset FIRED its first live cycle** — self-heal F26-confirmed end-to-end (SKIP-while-active @11:16 → RESET-when-idle @11:31; archive-first recoverable; gateway restarted; cooldown armed; `telegram:direct` cleared → lean session now re-injects the fixed briefs); the **multi-task enumeration brief note** shipped net-neutral into `AGENTS.md §Tasks` (−150 chars, TOOLS.md headroom preserved); the **multi-message lever pinned** (`messages.queue.mode`, unset→default; no numeric debounce key — A-12 corrects the [P6-131] guess); **payment fill-fix holds by inspection** (zero browser errors since the 11:04 fix); **reply-watcher notify reviewed** (9 clean sends, 0 noise, no poke). **Read [P6-132] first**, then this. openclaw.json **UNCHANGED** → `3267edd8b6aa`/10489B, last-good synced (A-7 clean). GUI/Screen-Sharing (D20) for restart/browser/2FA. **Cadence: ride the window ([[ride-context-dont-close-early]]).**

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`3267edd8b6aa`/10489B**, 600/staff. Pin 2026.4.22 (00bd2cf). Levers `contextTokens=120000`, `browser.snapshotDefaults.mode=efficient`. (openclaw.json Bash-deny-listed — A-7 via applied-lever `config get` + `shasum` on the file; structural jq/python for edits only, never a raw dump; NEVER weaken the F21 backstop.)
- **Deviations currency:** **P6-1…132 gap-free**, no dangling refs (run [P6-101] at close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA.
- **★ Marie briefs (live, outside docs git — verify by direct sha):** `AGENTS.md` **`f68b9fc5bc38`/16526B** (16310 chars — multi-task §Tasks note added, redundant §Memory-Protocol boilerplate collapsed; net −150 so the injected core shrank); `TOOLS.md` **`f4167b1c15c7`/15935B** (15758 chars, **7 under** the 15765 clip ceiling — payment fill/type shape [P6-131]; **tight — any pa-brief growth needs displacement**); `reference-tools.md` `c3594d05c767`/4338B; `HEARTBEAT.md` `6b9d50031098`.
- **★ LaunchAgents:** reply-watcher **notify** (`9e6b476f2de7`/2218B, 900s, exit0); **chat-bloat-reset** (`scripts/chat-bloat-reset.sh` `93bb06e42841`/4939B, plist `497ccbd67145`/1623B, 900s, exit0, **cooldown-armed since 6c.28 11:31 → next reset earliest ~13:31**); reset-net `heartbeat-watchdog.sh` `183896a33dd2` + `session-freshstart.sh` `55020ef7acd6` loaded/exit0. **Do NOT run reply-watcher.sh / chat-bloat-reset.sh with side-effects from this session** (F21/F51 + launchd-only by design — dry-run/inspect only).
- **★ Marie session health:** expect `:main` lean + a **fresh/low `telegram:direct`** (the 6c.28 reset cleared the old 136K bloat). If `telegram:direct` climbs toward 130K again, chat-bloat-reset heals it on the idle+cooldown window — observe, don't intervene.

## Stage 1 — the LEAD: the billed re-tests, on the healed session (Peter-gated)
_All three are BILLED interactive turns → **Peter's explicit cost-OK first ([P6-51]); NEVER Opus for tests**. They validate fixes already verified-by-inspection in 6c.28; the point is real-runtime end-to-end (F26)._
1. **★ T3 responsiveness re-test** — message Marie on the lean session; confirm she answers promptly with no stick/quiet (the [P6-129] pathology that the reset just cured). Cheap: one short message, observe the reply.
2. **★ Wikimedia £10 payment re-test** — with the corrected brief now injected (efficient-refs snapshot + `type {ref,text}`/`fill {fields:[{ref,value}]}` + `pay-fill.sh` card handoff), retry the donation to the pre-pay screen. Watch `logs/gateway.err.log` for recurrence of `fill requires fields` / stale-ref (`e7 not found`) / `locator.click timeout`. **Irreversible charge → prepare-and-surface, Peter's card+2FA — never auto-execute.** The DEEP per-site robustness (recipe/library, SPA last-mile) stays **[P6-130] Andreas**, not a one-off patch here.
3. **★ Controlled 3-task multi-message test ([P6-131] item d)** — Peter fires 3 distinct tasks in one message; confirm all 3 are enumerated to `TASKS.md` and handled+confirmed one-by-one, none merged/dropped (validates the 6c.28 brief note). If they DO drop, the root is bloat/queue-mode — `messages.queue.mode` (unset→default; enum steer|collect|queue|…) is the pinned lever, but tuning it is an Andreas call.
4. **Observe chat-bloat-reset steady-state** — it fired once cleanly (6c.28); confirm over coming days it SKIPs while healthy and respects the 120m cooldown (no restart loop). Free inspection of `logs/chat-bloat-reset.log`.

## ⛔ FLAGGED FOR ANDREAS / milestone — do NOT attempt in interim sessions
- **Aggressive frozen-core slice** (verb-tables + procedural blocks → retrieval-only, ≤30K) + its billed verify turn — **the REAL bloat/stick cure** (~25K/turn brief injection drives the 150K sessions); chat-bloat-reset is the proven backstop, not the elimination. `PRE-COS-BACKLOG §A.4`.
- **Dreaming ON**; **`:telegram:direct` compaction bounding** (60s hardcap has no config knob, but `compaction.model`/`keepRecentTokens` ARE tunable — [P6-129]/[P6-130]); **brief-budget policy** (pa's brief at ceiling); **Playwright browser-backend swap** (deferred 6c.27). §A.4.
- **★ Cross-site browsing / form-filling / payment robustness — Andreas ONGOING REMIT ([P6-130] §C).** Host-`browser` efficient+by-ref technique · payment hard-gateways · `pay-fill` handoff · per-site recipes · in-sandbox Chromium · post-update `browser-dep-repair.sh` · **+ the benign `tools.allow (browser) unavailable` validation warning** (24×, since 19-Jun, non-blocking — the tool dispatches fine). Standing reliability track, not scattered patches.
- **Hard code-level pulse send-gate** ([P6-128]) — still soft (HEARTBEAT rule only); a real gate is Andreas.
- **v7 CAPSTONE / pre-6d consolidation ([P6-76]/[P6-127]) — OVERDUE milestone gate before Andreas** (author ARCHITECTURE v6→v7 + reconcile STATE-OF-NATION/RUNBOOK from Phase-6 deltas; canonical docs stale since 2026-06-11). Peter to prioritise timing.

## Safety rails
Reversible observation/reads + host-side builds + host-side `openclaw browser` inspection + launchd drains run without prompting. **Surface irreversible/destructive:** openclaw.json changes + gateway restart (prefer `openclaw gateway restart` / `session-freshstart.sh` — raw `launchctl bootout/bootstrap/kickstart` + `op` mutations + `rm` are DENY-listed → Peter runs via `!`, never route around), version-pin/D1, real charges (incl. Wikimedia), 1Password writes/deletes, depairing, reboots. **Any billed turn = Peter's cost-OK first ([P6-51]); NEVER Opus for tests** ([[no-unauthorized-test-spend]]). **Secrets [F21/F51]:** never read openclaw.json / agent-creds/** into context; structural jq/python only; display-immune checks. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward
- **Acceptance gate** stays OPEN — remaining: the Stage-1 billed re-tests (T3/T1/payment/multi-task), T4/T5 capability/payment decision, organic sample.
- **★ PATH TO 6d ([P6-76]):** 1) close the acceptance gate (the Stage-1 re-tests are the last live evidence needed); 2) **v7 CAPSTONE** (author ARCHITECTURE v6→v7 + reconcile canonical docs — OVERDUE, do NOT build Andreas against unreconciled docs); 3) **6d Andreas** on the proven pair + reconciled spec.
- **Pre-Andreas status:** reply-watcher ⑧ live in **notify** (reviewed clean); generate-mode cred store **CLOSED**; browser + payment path **fixed + guidance-corrected + holds-by-inspection**; chat-bloat self-heal **LIVE + PROVEN-IN-THE-WILD** (first real fire 6c.28); multi-task enumeration **briefed**. The bloat/stick ROOT (compaction hardcap + brief injection) is Andreas's aggressive-slice mandate.
