# SESSION 6c.37 — **Marie reliability mitigation batch** (structural harm-reducers), THEN resume the Andreas/A2A track

_Sequencing decision (6c.36 post-close, Peter): the Marie deep-audit ([P6-153]) root-caused her "goes quiet / forgets / unclear status" failures to **architecture, not behaviour** — and the durable cures ARE the Andreas-deferred roots (aggressive brief-slice, live-chat keep-alive, retrieval activation, hard send-gate). Rather than a big separate Marie track (= duplicate Andreas + re-patch), do a **tight, bounded mitigation batch first** — only the structural, Andreas-independent harm-reducers — to buy daily reliability DURING the multi-session Andreas build, then resume Andreas. `marie@` monitoring was already fixed 6c.36. 6c.36 also landed the A2A host gates (a)+(b) — coded/tested/dryrun-bootstrapped, A2A INERT ([P6-152]) — and verified the morning flip's first live 08:00 ([P6-150]). openclaw.json UNCHANGED (`24715f4045bd`/10748B, A-7 clean)._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live==last-good==**`24715f4045bd`/10748B**, 600/staff. Pin 2026.4.22 (`00bd2cf`). `cos`=Andreas/opus-4-7/**A2A-OFF**, `pa`=Marie/opus-4-7, `pro`=Charlotte/sonnet-4-6.
- **★ Dist patch persists:** `reply-media-paths.runtime-DegAVXTm.js` == **`6dfbfe762078`**/6847B. **NB — don't re-panic:** the second `reply-media-paths.runtime-BsZ-hV3v.js` (193B, Apr 24) is an **unrelated orphan chunk**, not a reinstall. Re-run `apply-6c34-screenshot-media-fix.sh` + restart only if `DegAVXTm` itself reverts.
- **Deviations currency:** P6-1..153 gap-free — re-run `scripts/carry-chain-audit.py` at open AND close.
- **Context/GUI ([P6-117]/D20):** confirm `SSH_CONNECTION` empty + `gui/502` before any restart/billed turn. Billed test = Sonnet, one short turn, re-confirm first ([[no-unauthorized-test-spend]]).
- **Marie briefs (6c.36 marie@ fix):** `HEARTBEAT.md`==**`10327ed4e00d`**/3106B, `TOOLS.md`==**`a41c0723f939`**/16160B (both now scan `marie@` via `/creds/marie-send.json`). Brief budget healthy (~59K/110K — the old "at ceiling" flag predated the 6c.35 MEMORY rotation).
- **★ A2A resting posture INERT (verified 6c.36).** `shared/a2a/HALT` PRESENT; plist staged-not-loaded; `a2a-watch.py`==**`96aa0247e52a`**/512L; `sessions_send`/`sessions_spawn` OFF.

## Stage 1 — Marie reliability mitigation batch (structural, Andreas-INDEPENDENT harm-reducers only)
Full diagnosis + routing: **[P6-153]** + `PRE-COS-BACKLOG.md §6c.36 addendum`. Scope discipline: **build ONLY the structural fixes below — do NOT rebuild the deep roots (slice/keep-alive/retrieval/send-gate); those are Andreas.** Each fix touches a live agent → **F26-verify through a real Marie turn (Sonnet, one short turn, Peter-authorised)**, not inspection-only.
1. **★ Status-SLA + compaction-proof open-loops file (highest leverage — design-first, show Peter).** A small `open-loops` state (what / who's-waiting / last-status-to-Peter ts / next-action) that survives every reset + a host watcher (reply-watcher pattern) that **forces a status when an active loop goes silent past a threshold**. Converts "never go silent on an active task" from prose-that-failed (16 Jul 68-min Aviva silence) into **host-enforced**. Generalises to all agents. This is the fix Peter's [P6-116] "stop patching" mandate endorses (enforcement layer, not another rule).
2. **Memory-flush dedup** — the pre-compaction flush re-appended its whole earlier block byte-identical (`memory/2026-07-16.md`); diagnose (flush-prompt wording vs the P6-120 dedup-tool cadence) + fix. Direct "forgetting"/budget-waste contributor.
3. **Reset-net retune** — `CHAT_RESET_TOKENS` **130K→below the 120K cap** (~90K) + a **graceful in-conversation "tightening my context, one sec"** instead of a silent stick. **Ship PAIRED with #2** (leaner reloads are only safe once memory captures reliably) — not alone.
4. **Cleanup wiring (Note 2 — Peter authorised delete-own-drafts).** Add a `draft-delete` verb to `gmail_helper.py` (`users().drafts().delete(id)`) + sandbox re-bind ([P6-23] ordering); brief: lift "never delete in Peter's box" **only** for her own `#PA-xxx` stale drafts; archive done TASKS.md rows (works with existing `modify`). Weekly hygiene beat.
5. **Micro-skills — lightweight tier (Note 6).** Seed a small `workspace-pa/SKILLS.md` (reusable micro-procedures, e.g. "reach a live operator") + append-on-success + consult-before-repeat discipline. Reliable retrieval of it rides the Andreas retrieval activation — flag that; don't over-build.

## Stage 2 — A2A agent-side envelope emitter (resume the Andreas/A2A track)
The **host** gates are done + proven; no agent emits an envelope yet.
1. **Emitter convention (AGENTS.md/TOOLS.md).** The hub (Andreas) mints `conversation_id` on the originating **human** turn; an agent needing a specialist drops a JSON envelope into `workspace-<id>/.a2a-outbox/pending/` (plain file-write — **no** openclaw.json grant); it **reads the reply from `results/`** and **never re-sends** (gate (a): replies are inline). Give Andreas the hub emitter + Marie/Charlotte the spoke read-path. Derive from the CURRENT COS/PA/PRO playbooks (P6-26).
2. **Dryrun over REAL traffic.** Bootstrap the plist `A2A_MODE=dryrun` (GUI/D20, Peter-run), drive one real hub→spoke ask (single cheap Sonnet turn if authorised) → verify `ledger.jsonl` shows the host-authoritative envelope over genuine agent output.
3. **THEN live (Peter-gated, GUI, openclaw.json change + restart — SURFACE it).** Flip `A2A_MODE=live` + remove `shared/a2a/HALT` + grant `sessions_send` (hard-gate `sessions_spawn`). Kill lever: `touch shared/a2a/HALT`. **Do NOT flip live** unless the real-traffic dryrun is clean AND Peter authorises.

## Stage 3 / carried — the Andreas track proper (the deep Marie cures live here)
- **★ Andreas-owned durable Marie roots ([P6-153]):** aggressive frozen-core brief-slice (§A.4 HEADLINE — kills bloat/latency/stick); engine-level live-chat keep-alive + bounded worker-session isolation; production retrieval recall activation ([P6-118]/[P6-137]); hard pulse send-gate ([P6-128]) + per-task model routing ([P6-28]/[P6-144]).
- **`@fedchenkov` deliverability optimisation** (Note 3) → Andreas stewardship (DNS = propose-only).
- **Restaurant/place screenshot preferences store** (Note 4) → deferred, event-gated on Travel (~early Aug, [P6-147]); needs the retrieval layer.
- **Andreas avatar** (first male portrait, §e spec) — optional reversible.
- **Cost-model** absolute reconciliation awaits Peter's **Admin API key**; coverage fix (archived/gzipped trajectories) → Andreas ([P6-148]).
- **Charlotte handoff** — dedicated `workspace-pro/memory/digest-<date>.md` if Peter wants her content verbatim in the briefing (currently memory-log fallback).

## Safety rails
Reversible reads/host-authoring/`git commit` run without prompting. **Surface irreversible/outward:** openclaw.json edits (F17 staged, Peter via `!`) + gateway restart (GUI/D20); any billed turn (Sonnet-only, re-confirm); **enabling A2A live** (`sessions_send` grant + HALT removal); real external sends; **any Marie draft-DELETE first run** (mutation on Peter's mailbox — dry-run + confirm before the first real delete). `openclaw.json` jq/mv/cp + raw `launchctl` + `op` mutations + `rm` + `docker exec` DENY-listed → Peter via `!`. **Secrets [F21/F51]:** never Read openclaw.json / agent-creds/** — structural `config get`/`jq`-on-keys + `shasum` only. (6c.36 note: a Bash line that merely *names* `openclaw.json` alongside other ops can trip the guard — keep config checks on their own line.)

## Carry-forward — status
1. **✔ Acceptance gate (6c.30); ✔ v7 CAPSTONE (6c.31); ✔ Andreas foundation (6c.32); ✔ PAIRED+F26 (6c.33); ✔ Marie screenshot fix (6c.34); ✔ digest→CoS-flip + MEMORY rotation + SR-1 currency (6c.35); ✔ CoS morning flip VERIFIED live + A2A host gates (a)+(b) (6c.36, [P6-150]/[P6-152]); ✔ Marie reliability deep-audit + marie@ fix + routing (6c.36, [P6-153]).**
2. **→ THIS SESSION:** the Marie mitigation batch (Stage 1 — status-SLA + memory-flush dedup + reset-retune + cleanup + skills-lightweight), **then** resume the A2A emitter (Stage 2) and the Andreas track (Stage 3). The deep Marie cures live in the Andreas track, not here — Stage 1 is harm-reduction to buy reliability during the build.
