# SESSION 6c.32 — **6d ANDREAS PAIRING.** The pre-6d gate is fully cleared, the v7 capstone is committed+pushed, AND the 13 Andreas surfaced decisions were RESOLVED at 6c.31 close. Nothing bridging remains — this session BUILDS Andreas (Chief of Staff) from `BRIEF-DERIVATION-ANDREAS.md` + v7, against the resolved parameters. **Hard ordering: build S11 + the sessions_spawn per-window budget BEFORE any A2A/`sessions_send` goes live.**

_**Resolved at 6c.31 (Peter) — build from these, do not re-litigate:** control-plane = `sessions_send`+`cron`+**hard-gated `sessions_spawn`** granted, `gateway` **denied**; A2A cost envelope + S11 = the v7 proposed defaults (hop 4 / per-conv 8 / per-pair 2 / fan-out 2 / ~30 billed A2A turns/day), tunable; engineering-steward = **Andreas's facet**; work-inbox = **digest-only** (no mailbox grant); id `cos`; model `opus-4-7`; Google **read-only** v1; conservative cadence + 08:00 cron briefing; publish = `openclaw-docs` at close._

_Opener · generated at 6c.31 close (2026-07-09). **6c.31 landed the whole v7 capstone** ([P6-139]): **MASTER-ARCHITECTURE-v7.md** authored (`55d73fa17a1a`/41696B — supersedes v6 additively, 12 PARTs, every Phase-6 subsystem P6-anchored); **S11 inter-agent loop-protection DESIGNED** (v7 PART 9 — 5-layer defence, the highest-risk previously-undesigned gap) and **SR-1 doc-autopilot DESIGNED** (PART 10); **STATE-OF-NATION** (`1aecffebf3c5`/42007B) + **RUNBOOK** (`cf155bf72237`/67468B) reconciled from the P6-1…138 deltas (colima-autostart contradiction fixed, ~12-job launchd fleet inventoried, cold-boot caveat added); **Sophie + Andreas briefs derived** from the CURRENT live pair ([P6-26], `BRIEF-DERIVATION-{SOPHIE,ANDREAS}.md`, zero workspace writes); **PRE-COS-BACKLOG re-folded** at the milestone boundary. **£0 billed** (pure doc session, Claude-Code subagents only). **openclaw.json UNCHANGED** (`3267edd8b6aa`/10489B, A-7 clean). **Read [P6-139] first**, then this. Cadence: [[ride-context-dont-close-early]]._

## Stage 0 — Ledger (no mutating work until GREEN)
- **A-7:** live == last-good == **`3267edd8b6aa`/10489B**, 600/staff. Pin 2026.4.22 (00bd2cf). Levers `contextTokens=120000`, `browser.snapshotDefaults.mode=efficient`. pa model **`anthropic/claude-opus-4-7`** (unchanged). (openclaw.json Bash-deny-listed for jq/mv/cp — A-7 via `shasum` + `config get`; any change is Peter-run via `!`, revert from `openclaw.json.last-good`.)
- **Deviations currency:** **P6-1…139 gap-free**, no dangling refs (`re.findall` audit clean at 6c.31 close; deviations-log diff was pure-additive, 0 deletions; run [P6-101] again at this close). Known-historical 21/22 header dupes — leave them.
- **Context check first ([P6-117]):** confirm GUI vs SSH empirically (`SSH_CONNECTION` empty, `gui/502` reachable) before any restart/browser/2FA. This session is largely £0 (commit + decision-surfacing) unless Peter greenlights the 6d build.
- **★ Canonical docs are now CURRENT (6c.31 reconcile):** `MASTER-ARCHITECTURE-v7.md` `55d73fa17a1a`/41696B (authoritative for Phase-6; v6 = tombstone); `PROJECT-STATE-OF-NATION.md` `1aecffebf3c5`/42007B; `PROJECT-RUNBOOK.md` `cf155bf72237`/67468B; `PRE-COS-BACKLOG.md` `e6303b6e9558`/29858B (carries the 6c.31 6d-ready overlay); `TIER-1-KEY-PATTERNS.md` `dbcd9d9f00a7`/38313B. Derivation drafts: `BRIEF-DERIVATION-SOPHIE.md` `cb9df4ac4687`/40427B, `BRIEF-DERIVATION-ANDREAS.md` `1a53bf0fd09b`/44207B.
- **★ Docs committed + pushed** (6c.31 close, `a8e317a` on `openclaw-docs.git` main, in sync with origin). The Andreas draft carries the resolved surfaced decisions (§(d) RESOLVED block). Nothing doc-side is outstanding.
- **★ Marie briefs (verify by direct sha, unchanged from 6c.30):** AGENTS.md `2445fd82d3a1`/16671B (at ceiling — any growth needs displacement); TOOLS.md `f4167b1c15c7`/15935B (tight); PA-PLAYBOOK `7eb2d59fbe42`/27401B; MEMORY.md `4cb72ea15dcd`/5547B; HEARTBEAT.md `f580d9152b1e`/2918B. `gmail_helper.py` bind source `f14489444597`/13596B (sha-only, deny-Read).
- **★ LaunchAgents:** the ~12-job fleet (now inventoried in RUNBOOK §2.15) — all loaded/exit0. Do NOT run reply-watcher.sh / chat-bloat-reset.sh with side-effects (launchd-only; dry-run/inspect).

## Stage 1 — the LEAD: 6d ANDREAS PAIRING (the real build; multi-session — scope it with Peter first)
_The v7 capstone is committed+pushed and the 13 decisions are resolved (above). This is the build. It is NOT £0 — pairing + F26 need real turns; get Peter's cost-OK + confirm GUI/D20 before any billed turn. Sequence matters._
1. **★★ Derive the LIVE `workspace-cos/` briefs from `BRIEF-DERIVATION-ANDREAS.md` (P6-26 discharged at build).** Re-derive against the **then-current** Marie/Charlotte briefs (re-read them fresh — if they've moved since 6c.31, reconcile). Author AGENTS.md/TOOLS.md/HEARTBEAT.md + the shared canon + the net-new §Stewardship/§Close-discipline. Respect the brief-injection ceiling ([P6-98]/[P6-126]) — Andreas's always-injected core must fit the budget. Wire the resolved tool grants: `sessions_send`+`cron`+hard-gated `sessions_spawn`, `gateway` denied; Google read-only; digest-only inbox (no mailbox).
2. **★★ BUILD S11 + the sessions_spawn per-window budget BEFORE enabling A2A (the forbidden ordering).** The host-mailbox + the 5-layer caps (v7 PART 9 §S11) at the proposed defaults (hop 4 / per-conv 8 / per-pair 2 / fan-out 2 / ~30/day) + the kill-switch + the cost circuit-breaker + the A2A ledger/observability. `sessions_send`/`sessions_spawn` stay OFF until these are enforced in code.
3. **★★ Pair Andreas's Telegram bot (F2/F26)** — new bot via BotFather, the 4-scope profile, `agents set-identity`, avatar (generate at docs-lock per the template stub — first male portrait). Not live until exercised through a real turn (F26). openclaw.json changes + gateway restart = GUI/D20 + surfaced/Peter-run.
4. **★ Wire Andreas's object-level remit incrementally** (his facet, per the resolution): the 08:00 cross-agent digest assembly (cron), the per-task cost tracker ([P6-85]), doc-currency/SR-1 ownership, the infra-currency steward loop (propose-not-execute; `browser-dep-repair.sh` after any update). Each capability F26-verified through a real turn before the next.
- **NB** this is a genuine multi-session build. Do Stage 1.1–1.3 as the spine (briefs → S11/caps → pair+F26), then layer §Stewardship. Don't enable fan-out until S11 is proven.

## ⛔ FLAGGED FOR ANDREAS / 6d build-content (do NOT attempt in interim sessions — design is done, build is 6d)
- **★ Transactional last-mile** — CAPTCHA-solver ([P6-39]) + hosted-field cross-origin card-fill ([P6-135], `pay-fill.sh` multi-target/per-iframe) + **the `pay-fill.sh` PATH bug** (hardcoded PATH missing `~/.npm-global/bin`, same class as the cred-fill bug fixed 6c.30 — verify+fix before the payment work).
- **★ Browser backend gap** — Playwright-launched-Chromium swap for `refs=aria`/`efficient`; per-site recipes ([P6-44]); `browser-dep-repair.sh` post-update guardrail ([P6-40]/[P6-42]); the benign `browser`-unavailable validation warning ([P6-132]).
- **★ Hard code-level gates** — the pulse send-gate ([P6-128]) + the marie@ approval-token stop (the soft brief rules are coping-not-cure).
- **★ Robust outbound-bounce/DSN sweep** ([P6-138]) — extend reply-watcher (the HEARTBEAT bounce-check clause is the interim).
- **Bloat/stick CURE** — aggressive frozen-core slice (verb-tables → retrieval-only, ≤30K) + dreaming-on + `:telegram:direct` compaction bounding; needs a billed fresh-session verify. chat-bloat-reset is the proven backstop.
- **S9 monitoring suite + [P6-85] per-task cost tracker + the foundational-refactor mandate** (Andreas's object-level job).
- Full 6d build-content ledger: `PRE-COS-BACKLOG.md` (6c.31 milestone overlay).

## Safety rails
Reversible reads + host-side doc-authoring + `git commit` in `_session-docs/` run without prompting. **Surface irreversible/outward-facing:** the **`git push` to `openclaw-docs.git`** (outward-facing — confirm before pushing); openclaw.json changes + gateway restart; the 6d Andreas pairing + **all 13 Andreas surfaced decisions** (trust/scope/cost — Peter's calls); real charges; 1Password writes/deletes; version-pin/D1. `openclaw.json` jq/mv/cp + raw `launchctl bootout/bootstrap/kickstart` + `op` mutations + `rm` + `docker exec` into sandboxes are DENY-listed → Peter runs via `!`, never route around. **Any billed turn = Peter's cost-OK first ([P6-51]); NEVER Opus for tests** ([[no-unauthorized-test-spend]]). **Secrets [F21/F51]:** never read openclaw.json / agent-creds/** into context; structural `config get`/`jq`-on-stdin + `shasum` display-immune checks only. GUI/Aqua for restart + browser + 2FA (D20).

## Carry-forward — ★ PATH TO 6d ([P6-76]), status
1. **✔ Acceptance/usefulness gate — PASS on all agent-quality dimensions (6c.30).** Sole residual = the transactional last-mile (CAPTCHA/hosted-field), Andreas infrastructure, mechanisms proven.
2. **✔ v7 CAPSTONE — DONE (6c.31, [P6-139]).** ARCHITECTURE v7 + S11 + SR-1 + STATE/RUNBOOK reconcile + Sophie/Andreas derivation + backlog re-fold. Docs are current; do not build against drift.
3. **→ 6d build-start = THIS SESSION.** Prereq gate cleared; docs committed+pushed; the 13 Andreas decisions RESOLVED (6c.31). Remaining = derive live `workspace-cos/` briefs + build S11/spawn-budget before A2A + pair Andreas (F2/F26), then re-enable descoped launchd jobs + F44 OAuth flip + the payment/browser-repair guardrails + the [P6-135]/CAPTCHA transactional work as his object-level remit lands.
- **P6-134 organic verify** (only free-inspection thread still open) — a real multi-cc send landing BOTH recipients on Marie's next such send.
- **Optional reversible follow-ups (Peter-run, non-blocking):** `CHAT_RESET_TOKENS`→~70000; periodic reindex job; orphan 1Password item `efrhqyifslubmapond7wb6mmee` cleanup; MEMORY.md RATIONALE-rotation; avatars.
